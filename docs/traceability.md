<!-- SPDX-License-Identifier: CC0-1.0 -->

# Requirements traceability ledger

This ledger records repository acceptance. `Complete` means that the named
implementation and local passing evidence are present. Externally triggered
hosted and hardware executions remain explicit in the evidence; completion does
not turn an execution that did not occur into a claimed result.

| ID | Requirement | Implementation | Verification | State |
|---|---|---|---|---|
| R-001 | Independent CC0 implementation | Root `LICENSE`; full `LICENSES/CC0-1.0.txt`; SPDX-tagged authored sources, factual JSON schema fields, third-party texts/notices, scoped dependency inventory, and shipping-only CycloneDX SBOM | Local SPDX scan passed for authored text and 12 canonical JSON artifacts; `cargo run -p xtask -- check` verified dependency/license artifacts; the deterministic SBOM check found 20 shipping components, 21 dependency nodes, and no malformed license arrays | Complete |
| R-002 | SDK-free normal builds | `ocgpu-loader`, backend crates, shipping/development dependency scopes, `.github/scripts/assert-sdk-free.ps1`, `.github/workflows/ci.yml`, and exact import-table audits | The SDK-free preflight correctly rejected this host's installed CUDA 12.4 toolkit; locked Rust 1.85 checks and local `xtask ci` nevertheless passed without compiling, linking, or importing the toolkit, proving build independence rather than local absence; hosted x64/aarch64 jobs require the preflight before dependency/import gates, and no hosted result is claimed locally | Complete |
| R-003 | Canonical manifest and deterministic generation | `api/ocgpu-api.toml`, `crates/ocgpu-codegen`, and `xtask` generate/check orchestration | `cargo run -p ocgpu-codegen -- check` and `cargo run -p xtask -- check` passed with all 17 generated artifacts byte-current | Complete |
| R-004 | Unified, CUDA raw, and HIP raw versioned tables | `ocgpu-abi`, `ocgpu-cuda`, `ocgpu-hip`, and `ocgpu-capi`; generated common, CUDA, and HIP table layouts plus getters and flat leaves | 840 ABI layout tests, 10 CUDA tests, 19 HIP tests, and 4 C API tests passed; invariants account for all 26 common, 573 CUDA, and 535 HIP slots; package audits found the exact 3-symbol core and 1,137-symbol flat export sets | Complete |
| R-005 | Secure Linux/Windows runtime loading | `ocgpu-loader` platform implementations, backend process-lifetime initialization, safe default discovery, and unsafe trusted absolute-path entry points | 8 loader, 10 CUDA, and 19 HIP mock/invariant tests passed, covering secure flags, absolute Linux top-level resolution, bounded cache parsing, major-preserving symlinks, bootstrap ABI variants, exact symbol candidates, and process-lifetime state; strict Windows and Linux x86_64/aarch64 shipping-crate checks passed, and package import audits found no vendor SDK/runtime import | Complete |
| R-006 | Typed Rust API with backend-safe handles | Sealed `Backend`, typed `Driver<B>`, and lifetime-bound context, memory, stream, event, module, and function resources in `crates/ocgpu` | All 12 crate tests/doctests passed, including the compile-fail backend-mixing example, error mapping, and two bounded hardware-smoke tests that capability-skipped without a device workload; locked Rust 1.85 all-feature and minimal shipping checks passed | Complete |
| R-007 | C99 header and static/shared packaging | Public cargo-c package `ocgpu`, internal Cargo target `ocgpu_capi`, generated header/layout probe, pkg-config metadata, mapped ELF/PE export controls, and `tests/c99/` consumers | Strict Clang C99 and reserved-identifier C++ consumers passed locally; isolated core and flat cargo-c packages passed exact 3/1,137 export, import, generated-layout, header-hash, and static/shared consumer audits; GCC/Clang/MSVC and pkg-config coverage is encoded in the release-gating workflows | Complete |
| R-008 | Exhaustive classified oracle coverage | Five normalized snapshots, official vendor function union/proc catalog, semantic sidecar, exact-variant classifier, report, and raw-DEFLATE embedding under `oracle/`, `coverage/`, and `ocgpu-oracle` | `ocgpu-oracle check` passed all 8,508 entries/decisions with no deferred classification; runtime-resolvable coverage is 2,582/2,582 and ABI-relevant layout coverage is 508/508 | Complete |
| R-009 | Diagnostics CLI | `ocgpu-cli` doctor, symbol inventory, coverage, and module-inspection commands with the compressed report embedded at build time | All 20 CLI unit and 5 CLI integration tests passed; JSON symbol consolidation tests enforce one runtime row per name and exact CUDA 573/573 and HIP 535/535 completeness; safe PTX module inspection passed without a GPU workload | Complete |
| R-010 | CI, release, security, and operations docs | SHA-pinned `.github/` workflows/scripts plus developer, coverage, operations, security, release, provenance, CI, and assumptions documents | PowerShell helper syntax checks and full local `xtask ci` passed; tag publication is gated on locked Rust 1.85 feature-surface checks, RustSec, fresh source-package graph reconstruction, x64 package/import/export checks, and an aarch64 release job; cross-platform and hardware executions remain runner-defined evidence rather than local claims | Complete |
| R-011 | No panic across C ABI and structured errors | `ocgpu-capi` catch boundary, generated management-panic result, branch-only generated flat leaves, and backend/root error mapping | CAPI synthetic-panic containment maps to `OCGPU_ERROR_INTERNAL`; `backend_version_evidence_maps_to_too_old_result` preserves exact `BackendTooOld` symbol evidence; the template-level `flat_leaf_renderer_rejects_direct_panic_primitives` test rejects the enumerated direct panic/assert/unwrap/indexing constructs without claiming a formal transitive proof | Complete |
| R-012 | Hardware-safe runtime smoke coverage | `crates/ocgpu/tests/hardware_smoke.rs`, `.github/workflows/hardware-smoke.yml`, and case-exact applicable-symbol assertion | Both tests were discovered and capability-skipped successfully in local workspace tests; four bounded opt-in jobs are defined; coverage records 114 profile symbols and deliberately records 0/4 committed runner executions, so no GPU workload is claimed | Complete |
| R-013 | Full formatting/lint/test/release/doc checks | Entire workspace through `xtask ci`, strict oracle/xtask gates, and hosted target matrices | Final local `cargo run -p xtask -- ci` passed formatting, strict Clippy, workspace tests (including 840 generated layouts), release builds, flat-only build, and isolated warning-denying rustdoc; `xtask check` passed 17 artifacts and 8,508/8,508 oracle decisions; hosted cross-target results are not claimed locally | Complete |
| R-014 | One SDK-free application binary supports installed HIP 5, HIP 6, or HIP 7 capability levels | Secure loader candidate profiles; runtime-major validation; per-profile HIP compatibility ledger and raw-slot gating; common-table profile flags; typed `HipRuntimeProfile`; CLI profile and `profile_unavailable` diagnostics; common-vs-raw deployment documentation | Seven pinned HIP/CLR source replays produced exact per-release/target declarations; 22 codegen, 15 oracle, 8 loader, and 19 HIP profile tests passed; codegen reported 17/17 artifacts current and oracle 8,508/8,508 decisions with 3 profiles, 26 common operations, and 32 attributes; `xtask ci`, strict Clang C99 flag/layout assertions, Rust 1.97 minimal shipping checks, and a five-crate extracted offline all-feature consumer graph passed. Static PE inspection found all 27 bootstrap/common exports in the exact observed HIP 5 DLL without loading it. The final local Rust 1.85 rerun was environment-blocked by executable access policy, while the pinned CI gate remains and an earlier pre-final run passed; no HIP 5/6 hardware execution is claimed | Complete |
