<!-- SPDX-License-Identifier: CC0-1.0 -->

# Requirements traceability ledger

This ledger records repository acceptance. `Complete` means that the named
implementation and local passing evidence are present. Externally triggered
hosted and hardware executions remain explicit in the evidence; completion does
not turn an execution that did not occur into a claimed result.

| ID | Requirement | Implementation | Verification | State |
|---|---|---|---|---|
| R-001 | Independent CC0 implementation | Root `LICENSE`; full `LICENSES/CC0-1.0.txt`; SPDX-tagged authored sources, factual JSON schema fields, third-party texts/notices, scoped dependency inventory, and shipping-only CycloneDX SBOM | Local SPDX scan passed for authored text and 12 canonical JSON artifacts; `cargo run -p xtask -- check` verified dependency/license artifacts; the deterministic SBOM check found 21 shipping components, 22 dependency nodes, and no malformed license arrays | Complete |
| R-002 | SDK-free normal builds | `ocgpu-loader`, backend crates, shipping/development dependency scopes, `.github/scripts/assert-sdk-free.ps1`, `.github/workflows/ci.yml`, and exact import-table audits | The SDK-free preflight correctly rejected this host's installed CUDA 12.4 toolkit; locked Rust 1.85 checks and local `xtask ci` nevertheless passed without compiling, linking, or importing the toolkit, proving build independence rather than local absence; hosted x64/aarch64 jobs require the preflight before dependency/import gates, and no hosted result is claimed locally | Complete |
| R-003 | Canonical manifest and deterministic generation | `api/ocgpu-api.toml`, `crates/ocgpu-codegen`, and `xtask` generate/check orchestration | `cargo run -p ocgpu-codegen -- check` and `cargo run -p xtask -- check` passed with all 17 generated artifacts byte-current | Complete |
| R-004 | Unified, CUDA raw, HIP raw, and RTC versioned tables | `ocgpu-abi`, `ocgpu-cuda`, `ocgpu-hip`, `ocgpu-rtc`, and `ocgpu-capi`; generated common, CUDA, HIP, common-RTC, NVRTC, and HIPRTC table layouts plus getters and flat leaves | 840 generated ABI layout tests, 10 RTC layout tests, 10 CUDA tests, 20 HIP tests, 11 RTC tests, and 4 C API tests passed; invariants account for all 26 common, 573 CUDA, 535 HIP, 11 common-RTC, 21 NVRTC, and 18 HIPRTC slots; export controls contain the exact 6-symbol core and 1,140-symbol flat sets | Complete |
| R-005 | Secure Linux/Windows runtime loading | `ocgpu-loader` platform implementations, backend process-lifetime initialization, safe default discovery, and unsafe trusted absolute-path entry points | 10 loader, 10 CUDA, and 20 HIP mock/invariant tests passed, covering secure flags, independent driver/compiler state, absolute-path trust boundaries, bounded cache parsing, major-preserving symlinks, bootstrap ABI variants, exact fixed symbol/library candidates, and process-lifetime state; strict Windows and Linux x86_64/aarch64 shipping-crate checks passed, and package import audits found no vendor SDK/runtime import | Complete |
| R-006 | Typed Rust API with backend-safe handles | Sealed `Backend`, typed `Driver<B>` and `Compiler<B>`, and lifetime-bound context, memory, stream, event, module, function, and RTC program resources in `crates/ocgpu` | All 22 top-level crate tests/doctests passed (12 unit, six driver-hardware harness, one RTC-hardware harness, two runtime-profile integration, and one doctest), including compile-fail backend mixing, RTC state-machine bounds, and error mapping; hardware harnesses remain opt-in and capability-skip without explicit selection; locked Rust 1.85 all-feature and minimal shipping checks passed | Complete |
| R-007 | C99 header and static/shared packaging | Public cargo-c package `ocgpu`, internal Cargo target `ocgpu_capi`, generated header/layout probe, pkg-config metadata, mapped ELF/PE export controls, and `tests/c99/` consumers | Strict Clang C99 and reserved-identifier C++ consumers passed locally, including exact RTC table layout and pointer-return calling-convention assignments; release workflows require isolated core and flat cargo-c packages to pass exact 6/1,140 export, import, generated-layout, header-hash, and static/shared consumer audits across GCC/Clang/MSVC and pkg-config | Complete |
| R-008 | Exhaustive classified oracle coverage | Five normalized snapshots, official vendor function union/proc catalog, semantic sidecar, exact-variant classifier, report, and raw-DEFLATE embedding under `oracle/`, `coverage/`, and `ocgpu-oracle` | `ocgpu-oracle check` passed all 8,508 entries/decisions with no deferred classification; runtime-resolvable coverage is 2,582/2,582 and ABI-relevant layout coverage is 508/508 | Complete |
| R-009 | Diagnostics CLI | `ocgpu-cli` doctor, symbol inventory, coverage, and module-inspection commands with the compressed report embedded at build time | All 32 CLI unit and 7 CLI integration tests passed; JSON symbol consolidation tests enforce one runtime row per name and exact CUDA 573/573 and HIP 535/535 completeness; safe PTX and bounded AMDGPU module inspection passed without a GPU workload | Complete |
| R-010 | CI, release, security, and operations docs | SHA-pinned `.github/` workflows/scripts plus developer, coverage, operations, security, release, provenance, CI, and assumptions documents | PowerShell helper syntax checks and full local `xtask ci` passed; tag publication is gated on locked Rust 1.85 feature-surface checks, RustSec, fresh source-package graph reconstruction, x64 package/import/export checks, and an aarch64 release job; cross-platform and hardware executions remain runner-defined evidence rather than local claims | Complete |
| R-011 | No panic across C ABI and structured errors | `ocgpu-capi` catch boundary, generated management-panic result, branch-only generated flat leaves, and backend/root error mapping | CAPI synthetic-panic containment maps to `OCGPU_ERROR_INTERNAL`; `backend_version_evidence_maps_to_too_old_result` preserves exact `BackendTooOld` symbol evidence; the template-level `flat_leaf_renderer_rejects_direct_panic_primitives` test rejects the enumerated direct panic/assert/unwrap/indexing constructs without claiming a formal transitive proof | Complete |
| R-012 | Hardware-safe runtime smoke coverage | `crates/ocgpu/tests/hardware_smoke.rs`, watchdog-backed `xtask hardware-smoke`, `.github/workflows/hardware-smoke.yml`, and profile-aware case-exact core-symbol assertion | Four opt-in modes cover isolated CUDA, isolated HIP, simultaneous bounded execution, and context-free simultaneous coexistence, with CPU-only bounded-container parser tests; six OS/backend-labelled jobs plus dispatch authorization are defined. CPU-only validation deliberately claims no GPU execution; runner results remain external operational evidence | Complete |
| R-013 | Full formatting/lint/test/release/doc checks | Entire workspace through `xtask ci`, strict oracle/xtask gates, and hosted target matrices | Final local `cargo run -p xtask -- ci` passed formatting, strict Clippy, workspace tests (including 840 generated layouts), release builds, flat-only build, and isolated warning-denying rustdoc; `xtask check` passed 17 artifacts and 8,508/8,508 oracle decisions; hosted cross-target results are not claimed locally | Complete |
| R-014 | One SDK-free application binary supports installed HIP 5, HIP 6, or HIP 7 capability levels | Secure loader candidate profiles; runtime-major validation; per-profile HIP compatibility ledger and raw-slot gating; common-table profile flags; typed `HipRuntimeProfile`; CLI profile and `profile_unavailable` diagnostics; common-vs-raw deployment documentation | Seven pinned HIP/CLR source replays produced exact per-release/target declarations; 27 codegen, 16 oracle, 10 loader, and 20 HIP profile tests passed; codegen reported 17/17 artifacts current and oracle 8,508/8,508 decisions with 3 profiles, 26 common operations, and 32 attributes; `xtask ci`, strict Clang C99 flag/layout assertions, Rust 1.97 minimal shipping checks, a locked offline Rust 1.85 hardware-harness check, and a six-crate extracted offline all-feature consumer graph passed. Static PE inspection found all 27 bootstrap/common exports in the exact observed HIP 5 DLL without loading it; local bounded HIP 5 execution is recorded separately from HIPRTC availability | Complete |
| R-015 | SDK-free, runtime-loaded NVRTC/HIPRTC compilation integrated with CUDA/HIP execution | Independent RTC loader state; common 11-call typed API; exact NVRTC 12.4 and HIPRTC 5.7.1 raw tables; bounded diagnostics/code retrieval; compile-only staging; watchdog-backed fixed-source launch harness | Local NVRTC compile-only passed in 0.10 seconds and its 64-byte/one-thread CUDA launch passed in 2.16 seconds. Hash-pinned `gfx90c` HIP driver execution passed in 1.19 seconds, and simultaneous bounded CUDA+HIP execution passed in 2.11 seconds. This machine has no HIPRTC DLL: all five reviewed candidates failed immediately before driver/context creation, so no HIPRTC execution is claimed. All six opt-in Actions RTC paths now require compile-only success before one bounded execution | Complete |
