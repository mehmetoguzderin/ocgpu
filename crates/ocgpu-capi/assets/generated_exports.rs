// SPDX-License-Identifier: CC0-1.0
// Generated by ocgpu-codegen from api/ocgpu-api.toml. Do not edit.

#[cfg(feature = "flat-c-exports")]
use core::ffi::{c_char, c_void};
#[cfg(feature = "flat-c-exports")]
use ocgpu_abi::{
    OCGPU_BACKEND_CUDA, OCGPU_BACKEND_HIP, OCGPU_ERROR_INVALID_ARGUMENT,
    OCGPU_ERROR_SYMBOL_UNAVAILABLE, ocgpuBackend, ocgpuCU_DEV_SM_RESOURCE_GROUP_PARAMS,
    ocgpuCUDA_ARRAY_DESCRIPTOR, ocgpuCUDA_ARRAY_MEMORY_REQUIREMENTS,
    ocgpuCUDA_ARRAY_SPARSE_PROPERTIES, ocgpuCUDA_ARRAY3D_DESCRIPTOR,
    ocgpuCUDA_BATCH_MEM_OP_NODE_PARAMS, ocgpuCUDA_EXT_SEM_SIGNAL_NODE_PARAMS,
    ocgpuCUDA_EXT_SEM_WAIT_NODE_PARAMS, ocgpuCUDA_EXTERNAL_MEMORY_BUFFER_DESC,
    ocgpuCUDA_EXTERNAL_MEMORY_HANDLE_DESC, ocgpuCUDA_EXTERNAL_MEMORY_MIPMAPPED_ARRAY_DESC,
    ocgpuCUDA_EXTERNAL_SEMAPHORE_HANDLE_DESC, ocgpuCUDA_EXTERNAL_SEMAPHORE_SIGNAL_PARAMS,
    ocgpuCUDA_EXTERNAL_SEMAPHORE_WAIT_PARAMS, ocgpuCUDA_GRAPH_INSTANTIATE_PARAMS,
    ocgpuCUDA_HOST_NODE_PARAMS, ocgpuCUDA_KERNEL_NODE_PARAMS, ocgpuCUDA_LAUNCH_PARAMS,
    ocgpuCUDA_MEM_ALLOC_NODE_PARAMS, ocgpuCUDA_MEMCPY2D, ocgpuCUDA_MEMCPY3D,
    ocgpuCUDA_MEMCPY3D_BATCH_OP, ocgpuCUDA_MEMCPY3D_PEER, ocgpuCUDA_MEMSET_NODE_PARAMS,
    ocgpuCUDA_RESOURCE_DESC, ocgpuCUDA_RESOURCE_VIEW_DESC, ocgpuCUDA_TEXTURE_DESC,
    ocgpuCUaddress_mode, ocgpuCUarray, ocgpuCUarray_format, ocgpuCUarrayMapInfo,
    ocgpuCUasyncCallback, ocgpuCUasyncCallbackHandle, ocgpuCUatomicOperation,
    ocgpuCUcheckpointCheckpointArgs, ocgpuCUcheckpointLockArgs, ocgpuCUcheckpointRestoreArgs,
    ocgpuCUcheckpointUnlockArgs, ocgpuCUcontext, ocgpuCUcoredumpCallbackHandle,
    ocgpuCUcoredumpSettings, ocgpuCUcoredumpStatusCallback, ocgpuCUctxCreateParams,
    ocgpuCUdevResource, ocgpuCUdevResourceDesc, ocgpuCUdevResourceType, ocgpuCUdevice,
    ocgpuCUdevice_P2PAttribute, ocgpuCUdevice_attribute, ocgpuCUdeviceptr, ocgpuCUdevprop,
    ocgpuCUdriverProcAddressQueryResult, ocgpuCUevent, ocgpuCUexecAffinityParam,
    ocgpuCUexecAffinityType, ocgpuCUexternalMemory, ocgpuCUexternalSemaphore, ocgpuCUfilter_mode,
    ocgpuCUflushGPUDirectRDMAWritesScope, ocgpuCUflushGPUDirectRDMAWritesTarget, ocgpuCUfunc_cache,
    ocgpuCUfunction, ocgpuCUfunction_attribute, ocgpuCUfunctionLoadingState, ocgpuCUgraph,
    ocgpuCUgraphConditionalHandle, ocgpuCUgraphEdgeData, ocgpuCUgraphExec,
    ocgpuCUgraphExecUpdateResult, ocgpuCUgraphExecUpdateResultInfo, ocgpuCUgraphMem_attribute,
    ocgpuCUgraphNode, ocgpuCUgraphNodeParams, ocgpuCUgraphNodeType, ocgpuCUgraphRecaptureCallback,
    ocgpuCUgraphicsResource, ocgpuCUgreenCtx, ocgpuCUhostFn, ocgpuCUipcEventHandle,
    ocgpuCUipcMemHandle, ocgpuCUjit_option, ocgpuCUjitInputType, ocgpuCUkernel,
    ocgpuCUkernelNodeAttrID, ocgpuCUkernelNodeAttrValue, ocgpuCUlaunchConfig, ocgpuCUlibrary,
    ocgpuCUlibraryOption, ocgpuCUlimit, ocgpuCUlinkState, ocgpuCUlogIterator,
    ocgpuCUlogicalEndpointId, ocgpuCUlogicalEndpointIpcHandleType, ocgpuCUlogicalEndpointProp,
    ocgpuCUlogsCallback, ocgpuCUlogsCallbackHandle, ocgpuCUmem_advise, ocgpuCUmem_range_attribute,
    ocgpuCUmemAccess_flags, ocgpuCUmemAccessDesc, ocgpuCUmemAllocationGranularity_flags,
    ocgpuCUmemAllocationHandleType, ocgpuCUmemAllocationProp, ocgpuCUmemAllocationType,
    ocgpuCUmemDecompressParams, ocgpuCUmemGenericAllocationHandle, ocgpuCUmemLocation,
    ocgpuCUmemPool_attribute, ocgpuCUmemPoolProps, ocgpuCUmemPoolPtrExportData,
    ocgpuCUmemRangeHandleType, ocgpuCUmemcpyAttributes, ocgpuCUmemoryPool, ocgpuCUmipmappedArray,
    ocgpuCUmodule, ocgpuCUmoduleLoadingMode, ocgpuCUmulticastGranularity_flags,
    ocgpuCUmulticastObjectProp, ocgpuCUoccupancyB2DSize, ocgpuCUoutput_mode,
    ocgpuCUpointer_attribute, ocgpuCUprocessState, ocgpuCUresult, ocgpuCUsharedconfig,
    ocgpuCUstream, ocgpuCUstreamAttrID, ocgpuCUstreamAttrValue, ocgpuCUstreamBatchMemOpParams,
    ocgpuCUstreamCallback, ocgpuCUstreamCaptureMode, ocgpuCUstreamCaptureStatus,
    ocgpuCUstreamCigCaptureParams, ocgpuCUsurfObject, ocgpuCUsurfref, ocgpuCUtensorMap,
    ocgpuCUtensorMapDataType, ocgpuCUtensorMapFloatOOBfill, ocgpuCUtensorMapIm2ColWideMode,
    ocgpuCUtensorMapInterleave, ocgpuCUtensorMapL2promotion, ocgpuCUtensorMapSwizzle,
    ocgpuCUtexObject, ocgpuCUtexref, ocgpuCUuserObject, ocgpuCUuuid, ocgpuContext, ocgpuDevice,
    ocgpuDeviceAttribute, ocgpuDeviceptr, ocgpuEvent, ocgpuFunction, ocgpuHIP_ARRAY_DESCRIPTOR,
    ocgpuHIP_ARRAY3D_DESCRIPTOR, ocgpuHIP_LAUNCH_CONFIG, ocgpuHIP_MEMCPY3D, ocgpuHIP_RESOURCE_DESC,
    ocgpuHIP_RESOURCE_VIEW_DESC, ocgpuHIP_TEXTURE_DESC, ocgpuHip_Memcpy2D, ocgpuHipArray_Format,
    ocgpuHipArray_const_t, ocgpuHipArray_t, ocgpuHipArrayMapInfo, ocgpuHipArrayMemoryRequirements,
    ocgpuHipBatchMemOpNodeParams, ocgpuHipChannelFormatDesc, ocgpuHipChannelFormatKind,
    ocgpuHipCtx_t, ocgpuHipDevResource, ocgpuHipDevResourceDesc_t, ocgpuHipDevResourceType,
    ocgpuHipDevSmResourceGroupParams, ocgpuHipDevice_t, ocgpuHipDeviceAttribute_t,
    ocgpuHipDeviceP2PAttr, ocgpuHipDeviceProp_tR0600, ocgpuHipDeviceptr_t,
    ocgpuHipDriverEntryPointQueryResult, ocgpuHipDriverProcAddressQueryResult, ocgpuHipError_t,
    ocgpuHipEvent_t, ocgpuHipExecutionCtx_t, ocgpuHipExtent, ocgpuHipExternalMemory_t,
    ocgpuHipExternalMemoryBufferDesc, ocgpuHipExternalMemoryHandleDesc,
    ocgpuHipExternalMemoryMipmappedArrayDesc, ocgpuHipExternalSemaphore_t,
    ocgpuHipExternalSemaphoreHandleDesc, ocgpuHipExternalSemaphoreSignalNodeParams,
    ocgpuHipExternalSemaphoreSignalParams, ocgpuHipExternalSemaphoreWaitNodeParams,
    ocgpuHipExternalSemaphoreWaitParams, ocgpuHipFuncAttribute, ocgpuHipFuncAttributes,
    ocgpuHipFuncCache_t, ocgpuHipFunction_attribute, ocgpuHipFunction_t,
    ocgpuHipFunctionLaunchParams, ocgpuHipGraph_t, ocgpuHipGraphEdgeData, ocgpuHipGraphExec_t,
    ocgpuHipGraphExecUpdateResult, ocgpuHipGraphInstantiateParams, ocgpuHipGraphMemAttributeType,
    ocgpuHipGraphNode_t, ocgpuHipGraphNodeParams, ocgpuHipGraphNodeType,
    ocgpuHipGraphicsResource_t, ocgpuHipHostFn_t, ocgpuHipHostNodeParams, ocgpuHipIpcEventHandle_t,
    ocgpuHipIpcMemHandle_t, ocgpuHipJitInputType, ocgpuHipJitOption, ocgpuHipKernel_t,
    ocgpuHipKernelNodeParams, ocgpuHipLaunchAttributeID, ocgpuHipLaunchAttributeValue,
    ocgpuHipLaunchConfig_t, ocgpuHipLaunchParams, ocgpuHipLibrary_t, ocgpuHipLibraryOption,
    ocgpuHipLimit_t, ocgpuHipLinkState_t, ocgpuHipMemAccessDesc, ocgpuHipMemAccessFlags,
    ocgpuHipMemAllocNodeParams, ocgpuHipMemAllocationGranularity_flags,
    ocgpuHipMemAllocationHandleType, ocgpuHipMemAllocationProp, ocgpuHipMemAllocationType,
    ocgpuHipMemGenericAllocationHandle_t, ocgpuHipMemLocation, ocgpuHipMemPool_t,
    ocgpuHipMemPoolAttr, ocgpuHipMemPoolProps, ocgpuHipMemPoolPtrExportData,
    ocgpuHipMemRangeAttribute, ocgpuHipMemRangeHandleType, ocgpuHipMemcpy3DBatchOp,
    ocgpuHipMemcpy3DParms, ocgpuHipMemcpy3DPeerParms, ocgpuHipMemcpyAttributes, ocgpuHipMemcpyKind,
    ocgpuHipMemoryAdvise, ocgpuHipMemsetParams, ocgpuHipMipmappedArray,
    ocgpuHipMipmappedArray_const_t, ocgpuHipMipmappedArray_t, ocgpuHipModule_t, ocgpuHipPitchedPtr,
    ocgpuHipPointer_attribute, ocgpuHipPointerAttribute_t, ocgpuHipResourceDesc,
    ocgpuHipResourceViewDesc, ocgpuHipSharedMemConfig, ocgpuHipStream_t,
    ocgpuHipStreamBatchMemOpParams, ocgpuHipStreamCallback_t, ocgpuHipStreamCaptureMode,
    ocgpuHipStreamCaptureStatus, ocgpuHipSurfaceObject_t, ocgpuHipTextureAddressMode,
    ocgpuHipTextureDesc, ocgpuHipTextureFilterMode, ocgpuHipTextureObject_t, ocgpuHipUUID,
    ocgpuHipUserObject_t, ocgpuModule, ocgpuResult, ocgpuStream, ocgpucuuint32_t, ocgpucuuint64_t,
    ocgpudim3, ocgputextureReference,
};

/// Negotiate a backend-bound unified ABI table.
///
/// # Safety
/// `output` must designate `output_size` writable bytes when non-null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuGetApi(
    backend: ocgpu_abi::ocgpuBackend,
    requested_abi: u32,
    output_size: usize,
    output: *mut ocgpu_abi::ocgpuApi_v1,
) -> ocgpu_abi::ocgpuResult {
    // SAFETY: This export forwards its documented output-buffer contract unchanged.
    unsafe { crate::implementation::get_api(backend, requested_abi, output_size, output) }
}

/// Negotiate a CUDA raw ABI table.
///
/// # Safety
/// `output` must designate `output_size` writable bytes when non-null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGetApi(
    requested_abi: u32,
    output_size: usize,
    output: *mut ocgpu_abi::ocgpuCuApi_v1,
) -> ocgpu_abi::ocgpuResult {
    // SAFETY: This export forwards its documented output-buffer contract unchanged.
    unsafe { crate::implementation::get_cuda_api(requested_abi, output_size, output) }
}

/// Negotiate a HIP raw ABI table.
///
/// # Safety
/// `output` must designate `output_size` writable bytes when non-null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetApi(
    requested_abi: u32,
    output_size: usize,
    output: *mut ocgpu_abi::ocgpuHipApi_v1,
) -> ocgpu_abi::ocgpuResult {
    // SAFETY: This export forwards its documented output-buffer contract unchanged.
    unsafe { crate::implementation::get_hip_api(requested_abi, output_size, output) }
}

/// Negotiate a backend-bound common RTC ABI table.
///
/// # Safety
/// `output` must designate `output_size` writable bytes when non-null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuRtcGetApi(
    backend: ocgpu_abi::ocgpuBackend,
    requested_abi: u32,
    output_size: usize,
    output: *mut ocgpu_abi::ocgpuRtcApi_v1,
) -> ocgpu_abi::ocgpuResult {
    // SAFETY: This export forwards its documented output-buffer contract unchanged.
    unsafe { crate::implementation::get_rtc_api(backend, requested_abi, output_size, output) }
}

/// Negotiate a raw NVRTC ABI table.
///
/// # Safety
/// `output` must designate `output_size` writable bytes when non-null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuNvrtcGetApi(
    requested_abi: u32,
    output_size: usize,
    output: *mut ocgpu_abi::ocgpuNvrtcApi_v1,
) -> ocgpu_abi::ocgpuResult {
    // SAFETY: This export forwards its documented output-buffer contract unchanged.
    unsafe { crate::implementation::get_nvrtc_api(requested_abi, output_size, output) }
}

/// Negotiate a raw HIPRTC ABI table.
///
/// # Safety
/// `output` must designate `output_size` writable bytes when non-null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHiprtcGetApi(
    requested_abi: u32,
    output_size: usize,
    output: *mut ocgpu_abi::ocgpuHiprtcApi_v1,
) -> ocgpu_abi::ocgpuResult {
    // SAFETY: This export forwards its documented output-buffer contract unchanged.
    unsafe { crate::implementation::get_hiprtc_api(requested_abi, output_size, output) }
}

/// Manifest-selected result for a panic caught at a management getter boundary.
pub(crate) const OCGPU_MANAGEMENT_PANIC_RESULT: ocgpu_abi::ocgpuResult =
    ocgpu_abi::OCGPU_ERROR_INTERNAL;

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuInit(backend: ocgpuBackend, flags: u32) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuInit else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(flags) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuDriverGetVersion(
    backend: ocgpuBackend,
    driver_version: *mut i32,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuDriverGetVersion else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(driver_version) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuDeviceGetCount(
    backend: ocgpuBackend,
    count: *mut i32,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuDeviceGetCount else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(count) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuDeviceGet(
    backend: ocgpuBackend,
    device: *mut ocgpuDevice,
    ordinal: i32,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuDeviceGet else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(device, ordinal) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuDeviceGetName(
    backend: ocgpuBackend,
    name: *mut c_char,
    length: i32,
    device: ocgpuDevice,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuDeviceGetName else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(name, length, device) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuDeviceGetAttribute(
    backend: ocgpuBackend,
    value: *mut i32,
    attribute: ocgpuDeviceAttribute,
    device: ocgpuDevice,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuDeviceGetAttribute else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(value, attribute, device) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCtxCreate(
    backend: ocgpuBackend,
    context: *mut ocgpuContext,
    flags: u32,
    device: ocgpuDevice,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuCtxCreate else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(context, flags, device) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCtxDestroy(
    backend: ocgpuBackend,
    context: ocgpuContext,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuCtxDestroy else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(context) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCtxSetCurrent(
    backend: ocgpuBackend,
    context: ocgpuContext,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuCtxSetCurrent else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(context) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCtxGetCurrent(
    backend: ocgpuBackend,
    context: *mut ocgpuContext,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuCtxGetCurrent else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(context) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCtxSynchronize(backend: ocgpuBackend) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuCtxSynchronize else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch() }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuMemAlloc(
    backend: ocgpuBackend,
    device_ptr: *mut ocgpuDeviceptr,
    bytes: usize,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuMemAlloc else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(device_ptr, bytes) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuMemFree(
    backend: ocgpuBackend,
    device_ptr: ocgpuDeviceptr,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuMemFree else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(device_ptr) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuMemcpyHtoD(
    backend: ocgpuBackend,
    destination: ocgpuDeviceptr,
    source: *const c_void,
    bytes: usize,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuMemcpyHtoD else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(destination, source, bytes) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuMemcpyDtoH(
    backend: ocgpuBackend,
    destination: *mut c_void,
    source: ocgpuDeviceptr,
    bytes: usize,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuMemcpyDtoH else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(destination, source, bytes) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuStreamCreate(
    backend: ocgpuBackend,
    stream: *mut ocgpuStream,
    flags: u32,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuStreamCreate else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(stream, flags) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuStreamDestroy(
    backend: ocgpuBackend,
    stream: ocgpuStream,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuStreamDestroy else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(stream) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuStreamSynchronize(
    backend: ocgpuBackend,
    stream: ocgpuStream,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuStreamSynchronize else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(stream) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuEventCreate(
    backend: ocgpuBackend,
    event: *mut ocgpuEvent,
    flags: u32,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuEventCreate else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(event, flags) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuEventDestroy(
    backend: ocgpuBackend,
    event: ocgpuEvent,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuEventDestroy else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(event) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuEventRecord(
    backend: ocgpuBackend,
    event: ocgpuEvent,
    stream: ocgpuStream,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuEventRecord else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(event, stream) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuEventSynchronize(
    backend: ocgpuBackend,
    event: ocgpuEvent,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuEventSynchronize else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(event) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuModuleLoadData(
    backend: ocgpuBackend,
    module: *mut ocgpuModule,
    image: *const c_void,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuModuleLoadData else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(module, image) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuModuleUnload(
    backend: ocgpuBackend,
    module: ocgpuModule,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuModuleUnload else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(module) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuModuleGetFunction(
    backend: ocgpuBackend,
    function: *mut ocgpuFunction,
    module: ocgpuModule,
    name: *const c_char,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuModuleGetFunction else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe { __ocgpu_dispatch(function, module, name) }
}

/// Convenience unified leaf export. The first argument selects the backend for this call.
///
/// # Safety
/// All pointer arguments must satisfy the operation's canonical ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuLaunchKernel(
    backend: ocgpuBackend,
    function: ocgpuFunction,
    grid_x: u32,
    grid_y: u32,
    grid_z: u32,
    block_x: u32,
    block_y: u32,
    block_z: u32,
    shared_mem_bytes: u32,
    stream: ocgpuStream,
    kernel_params: *mut *mut c_void,
    extra: *mut *mut c_void,
) -> ocgpuResult {
    let __ocgpu_backend = match backend {
        OCGPU_BACKEND_CUDA => ocgpu_core::BackendKind::Cuda,
        OCGPU_BACKEND_HIP => ocgpu_core::BackendKind::Hip,
        _ => return OCGPU_ERROR_INVALID_ARGUMENT,
    };
    let __ocgpu_table = match ocgpu_core::negotiated_common_table(__ocgpu_backend) {
        Ok(table) => table,
        Err(error) => return error.result(),
    };
    let Some(__ocgpu_dispatch) = __ocgpu_table.ocgpuLaunchKernel else {
        return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
    };
    // SAFETY: The caller upholds this leaf export's documented ABI contract.
    unsafe {
        __ocgpu_dispatch(
            function,
            grid_x,
            grid_y,
            grid_z,
            block_x,
            block_y,
            block_z,
            shared_mem_bytes,
            stream,
            kernel_params,
            extra,
        )
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuInit(flags: u32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuInit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDriverGetVersion(driver_version: *mut i32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDriverGetVersion else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(driver_version) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (driver_version,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetCount(count: *mut i32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetCount else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (count,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGet(
    device: *mut ocgpuCUdevice,
    ordinal: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGet else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, ordinal) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, ordinal);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetName(
    name: *mut c_char,
    length: i32,
    device: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(name, length, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (name, length, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetAttribute(
    value: *mut i32,
    attribute: ocgpuCUdevice_attribute,
    device: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attribute, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (value, attribute, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxCreate_v2(
    context: *mut ocgpuCUcontext,
    flags: u32,
    device: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxCreate_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context, flags, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (context, flags, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxDestroy_v2(context: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxDestroy_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (context,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSetCurrent(context: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSetCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (context,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetCurrent(context: *mut ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (context,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSynchronize() -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "cuda"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAlloc_v2(
    device_ptr: *mut ocgpuCUdeviceptr,
    bytes: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAlloc_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device_ptr, bytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device_ptr, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemFree_v2(device_ptr: ocgpuCUdeviceptr) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemFree_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device_ptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device_ptr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoD_v2(
    destination: ocgpuCUdeviceptr,
    source: *const c_void,
    bytes: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoD_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(destination, source, bytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (destination, source, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoH_v2(
    destination: *mut c_void,
    source: ocgpuCUdeviceptr,
    bytes: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoH_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(destination, source, bytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (destination, source, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamCreate(
    stream: *mut ocgpuCUstream,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamDestroy_v2(stream: ocgpuCUstream) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamDestroy_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamSynchronize(stream: ocgpuCUstream) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventCreate(event: *mut ocgpuCUevent, flags: u32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (event, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventDestroy_v2(event: ocgpuCUevent) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventDestroy_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (event,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventRecord(
    event: ocgpuCUevent,
    stream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventRecord else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, stream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (event, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventSynchronize(event: ocgpuCUevent) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (event,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleLoadData(
    module: *mut ocgpuCUmodule,
    image: *const c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleLoadData else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, image) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (module, image);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleUnload(module: ocgpuCUmodule) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleUnload else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (module,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetFunction(
    function: *mut ocgpuCUfunction,
    module: ocgpuCUmodule,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetFunction else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(function, module, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (function, module, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchKernel(
    function: ocgpuCUfunction,
    grid_x: u32,
    grid_y: u32,
    grid_z: u32,
    block_x: u32,
    block_y: u32,
    block_z: u32,
    shared_mem_bytes: u32,
    stream: ocgpuCUstream,
    kernel_params: *mut *mut c_void,
    extra: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchKernel else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                function,
                grid_x,
                grid_y,
                grid_z,
                block_x,
                block_y,
                block_z,
                shared_mem_bytes,
                stream,
                kernel_params,
                extra,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            function,
            grid_x,
            grid_y,
            grid_z,
            block_x,
            block_y,
            block_z,
            shared_mem_bytes,
            stream,
            kernel_params,
            extra,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxAttach(pctx: *mut ocgpuCUcontext, flags: u32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxAttach else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxDetach(ctx: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxDetach else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxDisablePeerAccess(peerContext: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxDisablePeerAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(peerContext) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (peerContext,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxEnablePeerAccess(
    peerContext: ocgpuCUcontext,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxEnablePeerAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(peerContext, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (peerContext, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetApiVersion(
    ctx: ocgpuCUcontext,
    version: *mut u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetApiVersion else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, version) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx, version);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetDevice(device: *mut ocgpuCUdevice) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetDevice_v2(
    device: *mut ocgpuCUdevice,
    ctx: ocgpuCUcontext,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetDevice_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, ctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetFlags(flags: *mut u32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetId(ctx: ocgpuCUcontext, ctxId: *mut u64) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, ctxId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx, ctxId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetStreamPriorityRange(
    leastPriority: *mut i32,
    greatestPriority: *mut i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetStreamPriorityRange
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leastPriority, greatestPriority) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leastPriority, greatestPriority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxPopCurrent_v2(pctx: *mut ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxPopCurrent_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxPushCurrent_v2(ctx: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxPushCurrent_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxRecordEvent(
    hCtx: ocgpuCUcontext,
    hEvent: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxRecordEvent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx, hEvent) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx, hEvent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxResetPersistingL2Cache() -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxResetPersistingL2Cache
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "cuda"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSetFlags(flags: u32) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSynchronize_v2(ctx: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSynchronize_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxWaitEvent(
    hCtx: ocgpuCUcontext,
    hEvent: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxWaitEvent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx, hEvent) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx, hEvent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceCanAccessPeer(
    canAccessPeer: *mut i32,
    dev: ocgpuCUdevice,
    peerDev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceCanAccessPeer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(canAccessPeer, dev, peerDev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (canAccessPeer, dev, peerDev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceComputeCapability(
    major: *mut i32,
    minor: *mut i32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceComputeCapability
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(major, minor, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (major, minor, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetByPCIBusId(
    dev: *mut ocgpuCUdevice,
    pciBusId: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetByPCIBusId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, pciBusId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev, pciBusId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetLuid(
    luid: *mut c_char,
    deviceNodeMask: *mut u32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetLuid else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(luid, deviceNodeMask, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (luid, deviceNodeMask, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetPCIBusId(
    pciBusId: *mut c_char,
    len: i32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetPCIBusId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pciBusId, len, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pciBusId, len, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGraphMemTrim(device: ocgpuCUdevice) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGraphMemTrim else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxGetState(
    dev: ocgpuCUdevice,
    flags: *mut u32,
    active: *mut i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevicePrimaryCtxGetState
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, flags, active) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev, flags, active);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxRelease_v2(dev: ocgpuCUdevice) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDevicePrimaryCtxRelease_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxReset_v2(dev: ocgpuCUdevice) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevicePrimaryCtxReset_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxRetain(
    pctx: *mut ocgpuCUcontext,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevicePrimaryCtxRetain
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxSetFlags_v2(
    dev: ocgpuCUdevice,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDevicePrimaryCtxSetFlags_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceTotalMem_v2(
    bytes: *mut usize,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceTotalMem_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(bytes, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (bytes, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventElapsedTime(
    pMilliseconds: *mut f32,
    hStart: ocgpuCUevent,
    hEnd: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventElapsedTime else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pMilliseconds, hStart, hEnd) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pMilliseconds, hStart, hEnd);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventElapsedTime_v2(
    pMilliseconds: *mut f32,
    hStart: ocgpuCUevent,
    hEnd: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventElapsedTime_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pMilliseconds, hStart, hEnd) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pMilliseconds, hStart, hEnd);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventQuery(hEvent: ocgpuCUevent) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventQuery else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hEvent) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hEvent,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventRecordWithFlags(
    hEvent: ocgpuCUevent,
    hStream: ocgpuCUstream,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventRecordWithFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hEvent, hStream, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hEvent, hStream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncGetModule(
    hmod: *mut ocgpuCUmodule,
    hfunc: ocgpuCUfunction,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncGetModule else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hmod, hfunc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hmod, hfunc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncGetName(
    name: *mut *const c_char,
    hfunc: ocgpuCUfunction,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncGetName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(name, hfunc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (name, hfunc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncGetParamCount(
    func: ocgpuCUfunction,
    paramCount: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncGetParamCount else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(func, paramCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (func, paramCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncGetParamInfo(
    func: ocgpuCUfunction,
    paramIndex: usize,
    paramOffset: *mut usize,
    paramSize: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncGetParamInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(func, paramIndex, paramOffset, paramSize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (func, paramIndex, paramOffset, paramSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncLoad(function: ocgpuCUfunction) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncLoad else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(function) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (function,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncSetBlockShape(
    hfunc: ocgpuCUfunction,
    x: i32,
    y: i32,
    z: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncSetBlockShape else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, x, y, z) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, x, y, z);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncSetSharedSize(
    hfunc: ocgpuCUfunction,
    bytes: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncSetSharedSize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, bytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGetErrorName(
    error: ocgpuCUresult,
    pStr: *mut *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGetErrorName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(error, pStr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (error, pStr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGetErrorString(
    error: ocgpuCUresult,
    pStr: *mut *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGetErrorString else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(error, pStr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (error, pStr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuIpcCloseMemHandle(dptr: ocgpuCUdeviceptr) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuIpcCloseMemHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunch(f: ocgpuCUfunction) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunch else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(f) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (f,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchCooperativeKernel(
    f: ocgpuCUfunction,
    gridDimX: u32,
    gridDimY: u32,
    gridDimZ: u32,
    blockDimX: u32,
    blockDimY: u32,
    blockDimZ: u32,
    sharedMemBytes: u32,
    hStream: ocgpuCUstream,
    kernelParams: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchCooperativeKernel
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                f,
                gridDimX,
                gridDimY,
                gridDimZ,
                blockDimX,
                blockDimY,
                blockDimZ,
                sharedMemBytes,
                hStream,
                kernelParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            f,
            gridDimX,
            gridDimY,
            gridDimZ,
            blockDimX,
            blockDimY,
            blockDimZ,
            sharedMemBytes,
            hStream,
            kernelParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchGrid(
    f: ocgpuCUfunction,
    grid_width: i32,
    grid_height: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchGrid else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(f, grid_width, grid_height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (f, grid_width, grid_height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchGridAsync(
    f: ocgpuCUfunction,
    grid_width: i32,
    grid_height: i32,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchGridAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(f, grid_width, grid_height, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (f, grid_width, grid_height, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAddressFree(
    ptr: ocgpuCUdeviceptr,
    size: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAddressFree else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ptr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAddressReserve(
    ptr: *mut ocgpuCUdeviceptr,
    size: usize,
    alignment: usize,
    addr: ocgpuCUdeviceptr,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAddressReserve else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, alignment, addr, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ptr, size, alignment, addr, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocAsync(
    dptr: *mut ocgpuCUdeviceptr,
    bytesize: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytesize, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytesize, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocHost_v2(
    pp: *mut *mut c_void,
    bytesize: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocHost_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pp, bytesize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pp, bytesize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocManaged(
    dptr: *mut ocgpuCUdeviceptr,
    bytesize: usize,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocManaged else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytesize, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytesize, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocPitch_v2(
    dptr: *mut ocgpuCUdeviceptr,
    pPitch: *mut usize,
    WidthInBytes: usize,
    Height: usize,
    ElementSizeBytes: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocPitch_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, pPitch, WidthInBytes, Height, ElementSizeBytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, pPitch, WidthInBytes, Height, ElementSizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemDiscardBatchAsync(
    dptrs: *mut ocgpuCUdeviceptr,
    sizes: *mut usize,
    count: usize,
    flags: u64,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemDiscardBatchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptrs, sizes, count, flags, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptrs, sizes, count, flags, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemFreeAsync(
    dptr: ocgpuCUdeviceptr,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemFreeAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemFreeHost(p: *mut c_void) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemFreeHost else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (p,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetAddressRange_v2(
    pbase: *mut ocgpuCUdeviceptr,
    psize: *mut usize,
    dptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetAddressRange_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pbase, psize, dptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pbase, psize, dptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetInfo_v2(
    free: *mut usize,
    total: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetInfo_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(free, total) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (free, total);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostAlloc(
    pp: *mut *mut c_void,
    bytesize: usize,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemHostAlloc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pp, bytesize, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pp, bytesize, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostGetDevicePointer_v2(
    pdptr: *mut ocgpuCUdeviceptr,
    p: *mut c_void,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemHostGetDevicePointer_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pdptr, p, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pdptr, p, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostGetFlags(pFlags: *mut u32, p: *mut c_void) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemHostGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFlags, p) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pFlags, p);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostRegister_v2(
    p: *mut c_void,
    bytesize: usize,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemHostRegister_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p, bytesize, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (p, bytesize, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostUnregister(p: *mut c_void) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemHostUnregister else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (p,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPrefetchAsync(
    devPtr: ocgpuCUdeviceptr,
    count: usize,
    dstDevice: ocgpuCUdevice,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPrefetchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, count, dstDevice, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (devPtr, count, dstDevice, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemUnmap(ptr: ocgpuCUdeviceptr, size: usize) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemUnmap else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ptr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy(
    dst: ocgpuCUdeviceptr,
    src: ocgpuCUdeviceptr,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dst, src, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAsync(
    dst: ocgpuCUdeviceptr,
    src: ocgpuCUdeviceptr,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dst, src, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoDAsync_v2(
    dstDevice: ocgpuCUdeviceptr,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoDAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcDevice, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcDevice, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoD_v2(
    dstDevice: ocgpuCUdeviceptr,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoD_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcDevice, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcDevice, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoHAsync_v2(
    dstHost: *mut c_void,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoHAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcDevice, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcDevice, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoDAsync_v2(
    dstDevice: ocgpuCUdeviceptr,
    srcHost: *const c_void,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoDAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcHost, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcHost, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyPeer(
    dstDevice: ocgpuCUdeviceptr,
    dstContext: ocgpuCUcontext,
    srcDevice: ocgpuCUdeviceptr,
    srcContext: ocgpuCUcontext,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyPeer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstContext, srcDevice, srcContext, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstContext, srcDevice, srcContext, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyPeerAsync(
    dstDevice: ocgpuCUdeviceptr,
    dstContext: ocgpuCUcontext,
    srcDevice: ocgpuCUdeviceptr,
    srcContext: ocgpuCUcontext,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyPeerAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dstDevice, dstContext, srcDevice, srcContext, ByteCount, hStream,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            dstDevice, dstContext, srcDevice, srcContext, ByteCount, hStream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD16Async(
    dstDevice: ocgpuCUdeviceptr,
    us: u16,
    N: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD16Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, us, N, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, us, N, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD16_v2(
    dstDevice: ocgpuCUdeviceptr,
    us: u16,
    N: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD16_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, us, N) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, us, N);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D16Async(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    us: u16,
    Width: usize,
    Height: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D16Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, us, Width, Height, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, us, Width, Height, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D16_v2(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    us: u16,
    Width: usize,
    Height: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D16_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, us, Width, Height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, us, Width, Height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D32Async(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    ui: u32,
    Width: usize,
    Height: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D32Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, ui, Width, Height, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, ui, Width, Height, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D32_v2(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    ui: u32,
    Width: usize,
    Height: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D32_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, ui, Width, Height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, ui, Width, Height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D8Async(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    uc: u8,
    Width: usize,
    Height: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D8Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, uc, Width, Height, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, uc, Width, Height, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D8_v2(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    uc: u8,
    Width: usize,
    Height: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D8_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, uc, Width, Height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, uc, Width, Height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD32Async(
    dstDevice: ocgpuCUdeviceptr,
    ui: u32,
    N: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD32Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, ui, N, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, ui, N, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD32_v2(
    dstDevice: ocgpuCUdeviceptr,
    ui: u32,
    N: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD32_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, ui, N) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, ui, N);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD8Async(
    dstDevice: ocgpuCUdeviceptr,
    uc: u8,
    N: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD8Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, uc, N, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, uc, N, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD8_v2(
    dstDevice: ocgpuCUdeviceptr,
    uc: u8,
    N: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD8_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, uc, N) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, uc, N);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleEnumerateFunctions(
    functions: *mut ocgpuCUfunction,
    numFunctions: u32,
    mod_: ocgpuCUmodule,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleEnumerateFunctions
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(functions, numFunctions, mod_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (functions, numFunctions, mod_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetFunctionCount(
    count: *mut u32,
    mod_: ocgpuCUmodule,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetFunctionCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, mod_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (count, mod_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetGlobal_v2(
    dptr: *mut ocgpuCUdeviceptr,
    bytes: *mut usize,
    hmod: ocgpuCUmodule,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetGlobal_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, hmod, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytes, hmod, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleLoad(
    module: *mut ocgpuCUmodule,
    fname: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleLoad else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, fname) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (module, fname);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleLoadFatBinary(
    module: *mut ocgpuCUmodule,
    fatCubin: *const c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleLoadFatBinary else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, fatCubin) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (module, fatCubin);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyAvailableDynamicSMemPerBlock(
    dynamicSmemSize: *mut usize,
    func: ocgpuCUfunction,
    numBlocks: i32,
    blockSize: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyAvailableDynamicSMemPerBlock
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dynamicSmemSize, func, numBlocks, blockSize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dynamicSmemSize, func, numBlocks, blockSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyMaxActiveBlocksPerMultiprocessor(
    numBlocks: *mut i32,
    func: ocgpuCUfunction,
    blockSize: i32,
    dynamicSMemSize: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyMaxActiveBlocksPerMultiprocessor
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numBlocks, func, blockSize, dynamicSMemSize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numBlocks, func, blockSize, dynamicSMemSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyMaxActiveBlocksPerMultiprocessorWithFlags(
    numBlocks: *mut i32,
    func: ocgpuCUfunction,
    blockSize: i32,
    dynamicSMemSize: usize,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyMaxActiveBlocksPerMultiprocessorWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numBlocks, func, blockSize, dynamicSMemSize, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numBlocks, func, blockSize, dynamicSMemSize, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuParamSetSize(
    hfunc: ocgpuCUfunction,
    numbytes: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuParamSetSize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, numbytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, numbytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuParamSetf(
    hfunc: ocgpuCUfunction,
    offset: i32,
    value: f32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuParamSetf else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, offset, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, offset, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuParamSeti(
    hfunc: ocgpuCUfunction,
    offset: i32,
    value: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuParamSeti else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, offset, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, offset, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuParamSetv(
    hfunc: ocgpuCUfunction,
    offset: i32,
    ptr: *mut c_void,
    numbytes: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuParamSetv else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, offset, ptr, numbytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, offset, ptr, numbytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuProfilerStart() -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuProfilerStart else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "cuda"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuProfilerStop() -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuProfilerStop else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "cuda"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamAttachMemAsync(
    hStream: ocgpuCUstream,
    dptr: ocgpuCUdeviceptr,
    length: usize,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamAttachMemAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, dptr, length, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, dptr, length, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamCopyAttributes(
    dst: ocgpuCUstream,
    src: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamCopyAttributes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dst, src);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamCreateWithPriority(
    phStream: *mut ocgpuCUstream,
    flags: u32,
    priority: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamCreateWithPriority
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phStream, flags, priority) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phStream, flags, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamEndCaptureToCig(hStream: ocgpuCUstream) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamEndCaptureToCig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetCtx(
    hStream: ocgpuCUstream,
    pctx: *mut ocgpuCUcontext,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetCtx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, pctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, pctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetDevice(
    hStream: ocgpuCUstream,
    device: *mut ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetFlags(
    hStream: ocgpuCUstream,
    flags: *mut u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetId(
    hStream: ocgpuCUstream,
    streamId: *mut u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, streamId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, streamId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetPriority(
    hStream: ocgpuCUstream,
    priority: *mut i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetPriority else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, priority) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamQuery(hStream: ocgpuCUstream) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamQuery else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWaitEvent(
    hStream: ocgpuCUstream,
    hEvent: ocgpuCUevent,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWaitEvent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, hEvent, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, hEvent, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArray3DCreate_v2(
    pHandle: *mut ocgpuCUarray,
    pAllocateArray: *const ocgpuCUDA_ARRAY3D_DESCRIPTOR,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArray3DCreate_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pAllocateArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, pAllocateArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArray3DGetDescriptor_v2(
    pArrayDescriptor: *mut ocgpuCUDA_ARRAY3D_DESCRIPTOR,
    hArray: ocgpuCUarray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArray3DGetDescriptor_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArrayDescriptor, hArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pArrayDescriptor, hArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayCreate_v2(
    pHandle: *mut ocgpuCUarray,
    pAllocateArray: *const ocgpuCUDA_ARRAY_DESCRIPTOR,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayCreate_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pAllocateArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, pAllocateArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayDestroy(hArray: ocgpuCUarray) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hArray,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayGetDescriptor_v2(
    pArrayDescriptor: *mut ocgpuCUDA_ARRAY_DESCRIPTOR,
    hArray: ocgpuCUarray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayGetDescriptor_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArrayDescriptor, hArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pArrayDescriptor, hArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayGetMemoryRequirements(
    memoryRequirements: *mut ocgpuCUDA_ARRAY_MEMORY_REQUIREMENTS,
    array: ocgpuCUarray,
    device: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuArrayGetMemoryRequirements
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(memoryRequirements, array, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (memoryRequirements, array, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayGetPlane(
    pPlaneArray: *mut ocgpuCUarray,
    hArray: ocgpuCUarray,
    planeIdx: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayGetPlane else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pPlaneArray, hArray, planeIdx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pPlaneArray, hArray, planeIdx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayGetSparseProperties(
    sparseProperties: *mut ocgpuCUDA_ARRAY_SPARSE_PROPERTIES,
    array: ocgpuCUarray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayGetSparseProperties
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(sparseProperties, array) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (sparseProperties, array);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpDeregisterCompleteCallback(
    callback: ocgpuCUcoredumpCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCoredumpDeregisterCompleteCallback
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(callback) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (callback,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpDeregisterStartCallback(
    callback: ocgpuCUcoredumpCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCoredumpDeregisterStartCallback
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(callback) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (callback,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpGetAttribute(
    attrib: ocgpuCUcoredumpSettings,
    value: *mut c_void,
    size: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCoredumpGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attrib, value, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (attrib, value, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpGetAttributeGlobal(
    attrib: ocgpuCUcoredumpSettings,
    value: *mut c_void,
    size: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCoredumpGetAttributeGlobal
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attrib, value, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (attrib, value, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpRegisterCompleteCallback(
    callback: ocgpuCUcoredumpStatusCallback,
    userData: *mut c_void,
    callbackOut: *mut ocgpuCUcoredumpCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCoredumpRegisterCompleteCallback
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(callback, userData, callbackOut) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (callback, userData, callbackOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpRegisterStartCallback(
    callback: ocgpuCUcoredumpStatusCallback,
    userData: *mut c_void,
    callbackOut: *mut ocgpuCUcoredumpCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCoredumpRegisterStartCallback
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(callback, userData, callbackOut) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (callback, userData, callbackOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpSetAttribute(
    attrib: ocgpuCUcoredumpSettings,
    value: *mut c_void,
    size: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCoredumpSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attrib, value, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (attrib, value, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCoredumpSetAttributeGlobal(
    attrib: ocgpuCUcoredumpSettings,
    value: *mut c_void,
    size: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCoredumpSetAttributeGlobal
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attrib, value, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (attrib, value, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxCreate_v3(
    pctx: *mut ocgpuCUcontext,
    paramsArray: *mut ocgpuCUexecAffinityParam,
    numParams: i32,
    flags: u32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxCreate_v3 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx, paramsArray, numParams, flags, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx, paramsArray, numParams, flags, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxCreate_v4(
    pctx: *mut ocgpuCUcontext,
    ctxCreateParams: *mut ocgpuCUctxCreateParams,
    flags: u32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxCreate_v4 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx, ctxCreateParams, flags, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx, ctxCreateParams, flags, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxFromGreenCtx(
    pContext: *mut ocgpuCUcontext,
    hCtx: ocgpuCUgreenCtx,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxFromGreenCtx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pContext, hCtx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pContext, hCtx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetCacheConfig(
    pconfig: *mut ocgpuCUfunc_cache,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pconfig) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pconfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetDevResource(
    hCtx: ocgpuCUcontext,
    resource: *mut ocgpuCUdevResource,
    type_: ocgpuCUdevResourceType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetDevResource else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx, resource, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetExecAffinity(
    pExecAffinity: *mut ocgpuCUexecAffinityParam,
    type_: ocgpuCUexecAffinityType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetExecAffinity else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pExecAffinity, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pExecAffinity, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetLimit(
    pvalue: *mut usize,
    limit: ocgpuCUlimit,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetLimit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pvalue, limit) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pvalue, limit);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxGetSharedMemConfig(
    pConfig: *mut ocgpuCUsharedconfig,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxGetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pConfig) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSetCacheConfig(config: ocgpuCUfunc_cache) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (config,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSetLimit(limit: ocgpuCUlimit, value: usize) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSetLimit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(limit, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (limit, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxSetSharedMemConfig(
    config: ocgpuCUsharedconfig,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxSetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (config,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDestroyExternalMemory(
    extMem: ocgpuCUexternalMemory,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDestroyExternalMemory
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extMem) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (extMem,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDestroyExternalSemaphore(
    extSem: ocgpuCUexternalSemaphore,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDestroyExternalSemaphore
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSem) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (extSem,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevResourceGenerateDesc(
    phDesc: *mut ocgpuCUdevResourceDesc,
    resources: *mut ocgpuCUdevResource,
    nbResources: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevResourceGenerateDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phDesc, resources, nbResources) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phDesc, resources, nbResources);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevSmResourceSplit(
    result: *mut ocgpuCUdevResource,
    nbGroups: u32,
    input: *const ocgpuCUdevResource,
    remainder: *mut ocgpuCUdevResource,
    flags: u32,
    groupParams: *mut ocgpuCU_DEV_SM_RESOURCE_GROUP_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevSmResourceSplit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(result, nbGroups, input, remainder, flags, groupParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (result, nbGroups, input, remainder, flags, groupParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevSmResourceSplitByCount(
    result: *mut ocgpuCUdevResource,
    nbGroups: *mut u32,
    input: *const ocgpuCUdevResource,
    remainder: *mut ocgpuCUdevResource,
    flags: u32,
    minCount: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevSmResourceSplitByCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(result, nbGroups, input, remainder, flags, minCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (result, nbGroups, input, remainder, flags, minCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetDefaultMemPool(
    pool_out: *mut ocgpuCUmemoryPool,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetDefaultMemPool
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool_out, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool_out, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetDevResource(
    device: ocgpuCUdevice,
    resource: *mut ocgpuCUdevResource,
    type_: ocgpuCUdevResourceType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetDevResource else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, resource, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetExecAffinitySupport(
    pi: *mut i32,
    type_: ocgpuCUexecAffinityType,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceGetExecAffinitySupport
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pi, type_, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pi, type_, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetGraphMemAttribute(
    device: ocgpuCUdevice,
    attr: ocgpuCUgraphMem_attribute,
    value: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceGetGraphMemAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, attr, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetHostAtomicCapabilities(
    capabilities: *mut u32,
    operations: *const ocgpuCUatomicOperation,
    count: u32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceGetHostAtomicCapabilities
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(capabilities, operations, count, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (capabilities, operations, count, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetMemPool(
    pool: *mut ocgpuCUmemoryPool,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetP2PAtomicCapabilities(
    capabilities: *mut u32,
    operations: *const ocgpuCUatomicOperation,
    count: u32,
    srcDevice: ocgpuCUdevice,
    dstDevice: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceGetP2PAtomicCapabilities
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(capabilities, operations, count, srcDevice, dstDevice) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (capabilities, operations, count, srcDevice, dstDevice);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetP2PAttribute(
    value: *mut i32,
    attrib: ocgpuCUdevice_P2PAttribute,
    srcDevice: ocgpuCUdevice,
    dstDevice: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetP2PAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attrib, srcDevice, dstDevice) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (value, attrib, srcDevice, dstDevice);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetProperties(
    prop: *mut ocgpuCUdevprop,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetProperties else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(prop, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (prop, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetTexture1DLinearMaxWidth(
    maxWidthInElements: *mut usize,
    format: ocgpuCUarray_format,
    numChannels: u32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceGetTexture1DLinearMaxWidth
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(maxWidthInElements, format, numChannels, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (maxWidthInElements, format, numChannels, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetUuid(
    uuid: *mut ocgpuCUuuid,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetUuid else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(uuid, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (uuid, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetUuid_v2(
    uuid: *mut ocgpuCUuuid,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceGetUuid_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(uuid, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (uuid, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceRegisterAsyncNotification(
    device: ocgpuCUdevice,
    callbackFunc: ocgpuCUasyncCallback,
    userData: *mut c_void,
    callback: *mut ocgpuCUasyncCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceRegisterAsyncNotification
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, callbackFunc, userData, callback) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, callbackFunc, userData, callback);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceSetGraphMemAttribute(
    device: ocgpuCUdevice,
    attr: ocgpuCUgraphMem_attribute,
    value: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceSetGraphMemAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, attr, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceSetMemPool(
    dev: ocgpuCUdevice,
    pool: ocgpuCUmemoryPool,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceSetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, pool) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev, pool);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceUnregisterAsyncNotification(
    device: ocgpuCUdevice,
    callback: ocgpuCUasyncCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceUnregisterAsyncNotification
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, callback) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (device, callback);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuExternalMemoryGetMappedBuffer(
    devPtr: *mut ocgpuCUdeviceptr,
    extMem: ocgpuCUexternalMemory,
    bufferDesc: *const ocgpuCUDA_EXTERNAL_MEMORY_BUFFER_DESC,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuExternalMemoryGetMappedBuffer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, extMem, bufferDesc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (devPtr, extMem, bufferDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuExternalMemoryGetMappedMipmappedArray(
    mipmap: *mut ocgpuCUmipmappedArray,
    extMem: ocgpuCUexternalMemory,
    mipmapDesc: *const ocgpuCUDA_EXTERNAL_MEMORY_MIPMAPPED_ARRAY_DESC,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuExternalMemoryGetMappedMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mipmap, extMem, mipmapDesc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mipmap, extMem, mipmapDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFlushGPUDirectRDMAWrites(
    target: ocgpuCUflushGPUDirectRDMAWritesTarget,
    scope: ocgpuCUflushGPUDirectRDMAWritesScope,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFlushGPUDirectRDMAWrites
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(target, scope) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (target, scope);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncGetAttribute(
    pi: *mut i32,
    attrib: ocgpuCUfunction_attribute,
    hfunc: ocgpuCUfunction,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pi, attrib, hfunc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pi, attrib, hfunc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncIsLoaded(
    state: *mut ocgpuCUfunctionLoadingState,
    function: ocgpuCUfunction,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncIsLoaded else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state, function) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (state, function);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncSetAttribute(
    hfunc: ocgpuCUfunction,
    attrib: ocgpuCUfunction_attribute,
    value: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, attrib, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, attrib, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncSetCacheConfig(
    hfunc: ocgpuCUfunction,
    config: ocgpuCUfunc_cache,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncSetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, config) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuFuncSetSharedMemConfig(
    hfunc: ocgpuCUfunction,
    config: ocgpuCUsharedconfig,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuFuncSetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, config) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGetExportTable(
    ppExportTable: *mut *const c_void,
    pExportTableId: *const ocgpuCUuuid,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGetExportTable else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ppExportTable, pExportTableId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ppExportTable, pExportTableId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGetProcAddress(
    symbol: *const c_char,
    pfn: *mut *mut c_void,
    cudaVersion: i32,
    flags: ocgpucuuint64_t,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGetProcAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, pfn, cudaVersion, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (symbol, pfn, cudaVersion, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGetProcAddress_v2(
    symbol: *const c_char,
    pfn: *mut *mut c_void,
    cudaVersion: i32,
    flags: ocgpucuuint64_t,
    symbolStatus: *mut ocgpuCUdriverProcAddressQueryResult,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGetProcAddress_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, pfn, cudaVersion, flags, symbolStatus) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (symbol, pfn, cudaVersion, flags, symbolStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddBatchMemOpNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *const ocgpuCUDA_BATCH_MEM_OP_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddBatchMemOpNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddChildGraphNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    childGraph: ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddChildGraphNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                childGraph,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            childGraph,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddDependencies(
    hGraph: ocgpuCUgraph,
    from: *const ocgpuCUgraphNode,
    to: *const ocgpuCUgraphNode,
    numDependencies: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddDependencies else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, from, to, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, from, to, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddDependencies_v2(
    hGraph: ocgpuCUgraph,
    from: *const ocgpuCUgraphNode,
    to: *const ocgpuCUgraphNode,
    edgeData: *const ocgpuCUgraphEdgeData,
    numDependencies: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddDependencies_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, from, to, edgeData, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, from, to, edgeData, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddEmptyNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddEmptyNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphNode, hGraph, dependencies, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphNode, hGraph, dependencies, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddEventRecordNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddEventRecordNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphNode, hGraph, dependencies, numDependencies, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphNode, hGraph, dependencies, numDependencies, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddEventWaitNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddEventWaitNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphNode, hGraph, dependencies, numDependencies, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphNode, hGraph, dependencies, numDependencies, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddExternalSemaphoresSignalNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *const ocgpuCUDA_EXT_SEM_SIGNAL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphAddExternalSemaphoresSignalNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddExternalSemaphoresWaitNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *const ocgpuCUDA_EXT_SEM_WAIT_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphAddExternalSemaphoresWaitNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddHostNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *const ocgpuCUDA_HOST_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddHostNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddKernelNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *const ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddKernelNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddKernelNode_v2(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *const ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddKernelNode_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddMemAllocNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *mut ocgpuCUDA_MEM_ALLOC_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddMemAllocNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddMemFreeNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    dptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddMemFreeNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphNode, hGraph, dependencies, numDependencies, dptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphNode, hGraph, dependencies, numDependencies, dptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddMemcpyNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    copyParams: *const ocgpuCUDA_MEMCPY3D,
    ctx: ocgpuCUcontext,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddMemcpyNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                copyParams,
                ctx,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            copyParams,
            ctx,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddMemsetNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    memsetParams: *const ocgpuCUDA_MEMSET_NODE_PARAMS,
    ctx: ocgpuCUcontext,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddMemsetNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                memsetParams,
                ctx,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            memsetParams,
            ctx,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddNode(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    numDependencies: usize,
    nodeParams: *mut ocgpuCUgraphNodeParams,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphAddNode_v2(
    phGraphNode: *mut ocgpuCUgraphNode,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    dependencyData: *const ocgpuCUgraphEdgeData,
    numDependencies: usize,
    nodeParams: *mut ocgpuCUgraphNodeParams,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphAddNode_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                dependencyData,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            dependencyData,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphBatchMemOpNodeGetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams_out: *mut ocgpuCUDA_BATCH_MEM_OP_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphBatchMemOpNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphBatchMemOpNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_BATCH_MEM_OP_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphBatchMemOpNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphChildGraphNodeGetGraph(
    hNode: ocgpuCUgraphNode,
    phGraph: *mut ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphChildGraphNodeGetGraph
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, phGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, phGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphClone(
    phGraphClone: *mut ocgpuCUgraph,
    originalGraph: ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphClone else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphClone, originalGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphClone, originalGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphConditionalHandleCreate(
    pHandle_out: *mut ocgpuCUgraphConditionalHandle,
    hGraph: ocgpuCUgraph,
    ctx: ocgpuCUcontext,
    defaultLaunchValue: u32,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphConditionalHandleCreate
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle_out, hGraph, ctx, defaultLaunchValue, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle_out, hGraph, ctx, defaultLaunchValue, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphCreate(
    phGraph: *mut ocgpuCUgraph,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraph, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraph, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphDebugDotPrint(
    hGraph: ocgpuCUgraph,
    path: *const c_char,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphDebugDotPrint else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, path, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, path, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphDestroy(hGraph: ocgpuCUgraph) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphDestroyNode(hNode: ocgpuCUgraphNode) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphDestroyNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphEventRecordNodeGetEvent(
    hNode: ocgpuCUgraphNode,
    event_out: *mut ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphEventRecordNodeGetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, event_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, event_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphEventRecordNodeSetEvent(
    hNode: ocgpuCUgraphNode,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphEventRecordNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphEventWaitNodeGetEvent(
    hNode: ocgpuCUgraphNode,
    event_out: *mut ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphEventWaitNodeGetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, event_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, event_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphEventWaitNodeSetEvent(
    hNode: ocgpuCUgraphNode,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphEventWaitNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecBatchMemOpNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_BATCH_MEM_OP_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecBatchMemOpNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecChildGraphNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    childGraph: ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecChildGraphNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, childGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, childGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecDestroy(hGraphExec: ocgpuCUgraphExec) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphExecDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecEventRecordNodeSetEvent(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecEventRecordNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecEventWaitNodeSetEvent(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecEventWaitNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecExternalSemaphoresSignalNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_EXT_SEM_SIGNAL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecExternalSemaphoresSignalNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecExternalSemaphoresWaitNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_EXT_SEM_WAIT_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecExternalSemaphoresWaitNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecGetFlags(
    hGraphExec: ocgpuCUgraphExec,
    flags: *mut ocgpucuuint64_t,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphExecGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecGetId(
    hGraphExec: ocgpuCUgraphExec,
    graphId: *mut u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphExecGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, graphId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, graphId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecHostNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_HOST_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecHostNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecKernelNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecKernelNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecKernelNodeSetParams_v2(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecKernelNodeSetParams_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecMemcpyNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    copyParams: *const ocgpuCUDA_MEMCPY3D,
    ctx: ocgpuCUcontext,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecMemcpyNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, copyParams, ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, copyParams, ctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecMemsetNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    memsetParams: *const ocgpuCUDA_MEMSET_NODE_PARAMS,
    ctx: ocgpuCUcontext,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExecMemsetNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, memsetParams, ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, memsetParams, ctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecNodeSetParams(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUgraphNodeParams,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphExecNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecUpdate(
    hGraphExec: ocgpuCUgraphExec,
    hGraph: ocgpuCUgraph,
    hErrorNode_out: *mut ocgpuCUgraphNode,
    updateResult_out: *mut ocgpuCUgraphExecUpdateResult,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphExecUpdate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hGraph, hErrorNode_out, updateResult_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hGraph, hErrorNode_out, updateResult_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExecUpdate_v2(
    hGraphExec: ocgpuCUgraphExec,
    hGraph: ocgpuCUgraph,
    resultInfo: *mut ocgpuCUgraphExecUpdateResultInfo,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphExecUpdate_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hGraph, resultInfo) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hGraph, resultInfo);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExternalSemaphoresSignalNodeGetParams(
    hNode: ocgpuCUgraphNode,
    params_out: *mut ocgpuCUDA_EXT_SEM_SIGNAL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExternalSemaphoresSignalNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, params_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, params_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExternalSemaphoresSignalNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_EXT_SEM_SIGNAL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExternalSemaphoresSignalNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExternalSemaphoresWaitNodeGetParams(
    hNode: ocgpuCUgraphNode,
    params_out: *mut ocgpuCUDA_EXT_SEM_WAIT_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExternalSemaphoresWaitNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, params_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, params_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphExternalSemaphoresWaitNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_EXT_SEM_WAIT_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphExternalSemaphoresWaitNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphGetEdges(
    hGraph: ocgpuCUgraph,
    from: *mut ocgpuCUgraphNode,
    to: *mut ocgpuCUgraphNode,
    numEdges: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphGetEdges else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, from, to, numEdges) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, from, to, numEdges);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphGetEdges_v2(
    hGraph: ocgpuCUgraph,
    from: *mut ocgpuCUgraphNode,
    to: *mut ocgpuCUgraphNode,
    edgeData: *mut ocgpuCUgraphEdgeData,
    numEdges: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphGetEdges_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, from, to, edgeData, numEdges) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, from, to, edgeData, numEdges);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphGetId(
    hGraph: ocgpuCUgraph,
    graphId: *mut u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, graphId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, graphId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphGetNodes(
    hGraph: ocgpuCUgraph,
    nodes: *mut ocgpuCUgraphNode,
    numNodes: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphGetNodes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, nodes, numNodes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, nodes, numNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphGetRootNodes(
    hGraph: ocgpuCUgraph,
    rootNodes: *mut ocgpuCUgraphNode,
    numRootNodes: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphGetRootNodes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, rootNodes, numRootNodes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, rootNodes, numRootNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphHostNodeGetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUDA_HOST_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphHostNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphHostNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_HOST_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphHostNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphInstantiateWithFlags(
    phGraphExec: *mut ocgpuCUgraphExec,
    hGraph: ocgpuCUgraph,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphInstantiateWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphExec, hGraph, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphExec, hGraph, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphInstantiateWithParams(
    phGraphExec: *mut ocgpuCUgraphExec,
    hGraph: ocgpuCUgraph,
    instantiateParams: *mut ocgpuCUDA_GRAPH_INSTANTIATE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphInstantiateWithParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphExec, hGraph, instantiateParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphExec, hGraph, instantiateParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphInstantiate_v2(
    phGraphExec: *mut ocgpuCUgraphExec,
    hGraph: ocgpuCUgraph,
    phErrorNode: *mut ocgpuCUgraphNode,
    logBuffer: *mut c_char,
    bufferSize: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphInstantiate_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphExec, hGraph, phErrorNode, logBuffer, bufferSize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphExec, hGraph, phErrorNode, logBuffer, bufferSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeCopyAttributes(
    dst: ocgpuCUgraphNode,
    src: ocgpuCUgraphNode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphKernelNodeCopyAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dst, src);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeGetAttribute(
    hNode: ocgpuCUgraphNode,
    attr: ocgpuCUkernelNodeAttrID,
    value_out: *mut ocgpuCUkernelNodeAttrValue,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphKernelNodeGetAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, attr, value_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, attr, value_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeGetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphKernelNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeGetParams_v2(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphKernelNodeGetParams_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeSetAttribute(
    hNode: ocgpuCUgraphNode,
    attr: ocgpuCUkernelNodeAttrID,
    value: *const ocgpuCUkernelNodeAttrValue,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphKernelNodeSetAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, attr, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphKernelNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphKernelNodeSetParams_v2(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_KERNEL_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphKernelNodeSetParams_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphLaunch(
    hGraphExec: ocgpuCUgraphExec,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphLaunch else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphMemAllocNodeGetParams(
    hNode: ocgpuCUgraphNode,
    params_out: *mut ocgpuCUDA_MEM_ALLOC_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphMemAllocNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, params_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, params_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphMemFreeNodeGetParams(
    hNode: ocgpuCUgraphNode,
    dptr_out: *mut ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphMemFreeNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, dptr_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, dptr_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphMemcpyNodeGetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUDA_MEMCPY3D,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphMemcpyNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphMemcpyNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_MEMCPY3D,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphMemcpyNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphMemsetNodeGetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUDA_MEMSET_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphMemsetNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphMemsetNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *const ocgpuCUDA_MEMSET_NODE_PARAMS,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphMemsetNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeFindInClone(
    phNode: *mut ocgpuCUgraphNode,
    hOriginalNode: ocgpuCUgraphNode,
    hClonedGraph: ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeFindInClone else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phNode, hOriginalNode, hClonedGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phNode, hOriginalNode, hClonedGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetContainingGraph(
    hNode: ocgpuCUgraphNode,
    phGraph: *mut ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphNodeGetContainingGraph
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, phGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, phGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetDependencies(
    hNode: ocgpuCUgraphNode,
    dependencies: *mut ocgpuCUgraphNode,
    numDependencies: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeGetDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, dependencies, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, dependencies, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetDependencies_v2(
    hNode: ocgpuCUgraphNode,
    dependencies: *mut ocgpuCUgraphNode,
    edgeData: *mut ocgpuCUgraphEdgeData,
    numDependencies: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphNodeGetDependencies_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, dependencies, edgeData, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, dependencies, edgeData, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetDependentNodes(
    hNode: ocgpuCUgraphNode,
    dependentNodes: *mut ocgpuCUgraphNode,
    numDependentNodes: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphNodeGetDependentNodes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, dependentNodes, numDependentNodes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, dependentNodes, numDependentNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetDependentNodes_v2(
    hNode: ocgpuCUgraphNode,
    dependentNodes: *mut ocgpuCUgraphNode,
    edgeData: *mut ocgpuCUgraphEdgeData,
    numDependentNodes: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphNodeGetDependentNodes_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, dependentNodes, edgeData, numDependentNodes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, dependentNodes, edgeData, numDependentNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetEnabled(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    isEnabled: *mut u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeGetEnabled else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, isEnabled) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, isEnabled);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetLocalId(
    hNode: ocgpuCUgraphNode,
    nodeId: *mut u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeGetLocalId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUgraphNodeParams,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeGetParams else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetToolsId(
    hNode: ocgpuCUgraphNode,
    toolsNodeId: *mut u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeGetToolsId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, toolsNodeId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, toolsNodeId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeGetType(
    hNode: ocgpuCUgraphNode,
    type_: *mut ocgpuCUgraphNodeType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeGetType else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeSetEnabled(
    hGraphExec: ocgpuCUgraphExec,
    hNode: ocgpuCUgraphNode,
    isEnabled: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeSetEnabled else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, isEnabled) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hNode, isEnabled);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphNodeSetParams(
    hNode: ocgpuCUgraphNode,
    nodeParams: *mut ocgpuCUgraphNodeParams,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphNodeSetParams else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphReleaseUserObject(
    graph: ocgpuCUgraph,
    object: ocgpuCUuserObject,
    count: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphReleaseUserObject
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, object, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (graph, object, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphRemoveDependencies(
    hGraph: ocgpuCUgraph,
    from: *const ocgpuCUgraphNode,
    to: *const ocgpuCUgraphNode,
    numDependencies: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphRemoveDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, from, to, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, from, to, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphRemoveDependencies_v2(
    hGraph: ocgpuCUgraph,
    from: *const ocgpuCUgraphNode,
    to: *const ocgpuCUgraphNode,
    edgeData: *const ocgpuCUgraphEdgeData,
    numDependencies: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphRemoveDependencies_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraph, from, to, edgeData, numDependencies) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraph, from, to, edgeData, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphRetainUserObject(
    graph: ocgpuCUgraph,
    object: ocgpuCUuserObject,
    count: u32,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphRetainUserObject
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, object, count, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (graph, object, count, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphUpload(
    hGraphExec: ocgpuCUgraphExec,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphUpload else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hGraphExec, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsMapResources(
    count: u32,
    resources: *mut ocgpuCUgraphicsResource,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphicsMapResources else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, resources, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (count, resources, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsResourceGetMappedMipmappedArray(
    pMipmappedArray: *mut ocgpuCUmipmappedArray,
    resource: ocgpuCUgraphicsResource,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsResourceGetMappedMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pMipmappedArray, resource) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pMipmappedArray, resource);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsResourceGetMappedPointer_v2(
    pDevPtr: *mut ocgpuCUdeviceptr,
    pSize: *mut usize,
    resource: ocgpuCUgraphicsResource,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsResourceGetMappedPointer_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pDevPtr, pSize, resource) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pDevPtr, pSize, resource);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsResourceSetMapFlags_v2(
    resource: ocgpuCUgraphicsResource,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsResourceSetMapFlags_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(resource, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (resource, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsSubResourceGetMappedArray(
    pArray: *mut ocgpuCUarray,
    resource: ocgpuCUgraphicsResource,
    arrayIndex: u32,
    mipLevel: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsSubResourceGetMappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArray, resource, arrayIndex, mipLevel) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pArray, resource, arrayIndex, mipLevel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsUnmapResources(
    count: u32,
    resources: *mut ocgpuCUgraphicsResource,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphicsUnmapResources
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, resources, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (count, resources, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsUnregisterResource(
    resource: ocgpuCUgraphicsResource,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsUnregisterResource
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(resource) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (resource,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxCreate(
    phCtx: *mut ocgpuCUgreenCtx,
    desc: ocgpuCUdevResourceDesc,
    dev: ocgpuCUdevice,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phCtx, desc, dev, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phCtx, desc, dev, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxDestroy(hCtx: ocgpuCUgreenCtx) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxGetDevResource(
    hCtx: ocgpuCUgreenCtx,
    resource: *mut ocgpuCUdevResource,
    type_: ocgpuCUdevResourceType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxGetDevResource
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx, resource, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxGetId(
    greenCtx: ocgpuCUgreenCtx,
    greenCtxId: *mut u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(greenCtx, greenCtxId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (greenCtx, greenCtxId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxRecordEvent(
    hCtx: ocgpuCUgreenCtx,
    hEvent: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxRecordEvent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx, hEvent) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx, hEvent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxStreamCreate(
    phStream: *mut ocgpuCUstream,
    greenCtx: ocgpuCUgreenCtx,
    flags: u32,
    priority: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxStreamCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phStream, greenCtx, flags, priority) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phStream, greenCtx, flags, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGreenCtxWaitEvent(
    hCtx: ocgpuCUgreenCtx,
    hEvent: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGreenCtxWaitEvent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hCtx, hEvent) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hCtx, hEvent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuImportExternalMemory(
    extMem_out: *mut ocgpuCUexternalMemory,
    memHandleDesc: *const ocgpuCUDA_EXTERNAL_MEMORY_HANDLE_DESC,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuImportExternalMemory else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extMem_out, memHandleDesc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (extMem_out, memHandleDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuImportExternalSemaphore(
    extSem_out: *mut ocgpuCUexternalSemaphore,
    semHandleDesc: *const ocgpuCUDA_EXTERNAL_SEMAPHORE_HANDLE_DESC,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuImportExternalSemaphore
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSem_out, semHandleDesc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (extSem_out, semHandleDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuIpcGetEventHandle(
    pHandle: *mut ocgpuCUipcEventHandle,
    event: ocgpuCUevent,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuIpcGetEventHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, event) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuIpcGetMemHandle(
    pHandle: *mut ocgpuCUipcMemHandle,
    dptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuIpcGetMemHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, dptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, dptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelGetAttribute(
    pi: *mut i32,
    attrib: ocgpuCUfunction_attribute,
    kernel: ocgpuCUkernel,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pi, attrib, kernel, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pi, attrib, kernel, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelGetFunction(
    pFunc: *mut ocgpuCUfunction,
    kernel: ocgpuCUkernel,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelGetFunction else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFunc, kernel) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pFunc, kernel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelGetLibrary(
    pLib: *mut ocgpuCUlibrary,
    kernel: ocgpuCUkernel,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelGetLibrary else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pLib, kernel) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pLib, kernel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelGetName(
    name: *mut *const c_char,
    hfunc: ocgpuCUkernel,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelGetName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(name, hfunc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (name, hfunc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelGetParamCount(
    kernel: ocgpuCUkernel,
    paramCount: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelGetParamCount else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(kernel, paramCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (kernel, paramCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelGetParamInfo(
    kernel: ocgpuCUkernel,
    paramIndex: usize,
    paramOffset: *mut usize,
    paramSize: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelGetParamInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(kernel, paramIndex, paramOffset, paramSize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (kernel, paramIndex, paramOffset, paramSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelSetAttribute(
    attrib: ocgpuCUfunction_attribute,
    val: i32,
    kernel: ocgpuCUkernel,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attrib, val, kernel, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (attrib, val, kernel, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuKernelSetCacheConfig(
    kernel: ocgpuCUkernel,
    config: ocgpuCUfunc_cache,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuKernelSetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(kernel, config, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (kernel, config, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchCooperativeKernelMultiDevice(
    launchParamsList: *mut ocgpuCUDA_LAUNCH_PARAMS,
    numDevices: u32,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuLaunchCooperativeKernelMultiDevice
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(launchParamsList, numDevices, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (launchParamsList, numDevices, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchHostFunc(
    hStream: ocgpuCUstream,
    fn_: ocgpuCUhostFn,
    userData: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchHostFunc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, fn_, userData) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, fn_, userData);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchHostFunc_v2(
    hStream: ocgpuCUstream,
    fn_: ocgpuCUhostFn,
    userData: *mut c_void,
    syncMode: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchHostFunc_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, fn_, userData, syncMode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, fn_, userData, syncMode);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLaunchKernelEx(
    config: *const ocgpuCUlaunchConfig,
    f: ocgpuCUfunction,
    kernelParams: *mut *mut c_void,
    extra: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLaunchKernelEx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config, f, kernelParams, extra) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (config, f, kernelParams, extra);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryEnumerateKernels(
    kernels: *mut ocgpuCUkernel,
    numKernels: u32,
    lib: ocgpuCUlibrary,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryEnumerateKernels
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(kernels, numKernels, lib) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (kernels, numKernels, lib);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryGetGlobal(
    dptr: *mut ocgpuCUdeviceptr,
    bytes: *mut usize,
    library: ocgpuCUlibrary,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryGetGlobal else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, library, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytes, library, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryGetKernel(
    pKernel: *mut ocgpuCUkernel,
    library: ocgpuCUlibrary,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryGetKernel else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pKernel, library, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pKernel, library, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryGetKernelCount(
    count: *mut u32,
    lib: ocgpuCUlibrary,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryGetKernelCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, lib) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (count, lib);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryGetManaged(
    dptr: *mut ocgpuCUdeviceptr,
    bytes: *mut usize,
    library: ocgpuCUlibrary,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryGetManaged else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, library, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytes, library, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryGetModule(
    pMod: *mut ocgpuCUmodule,
    library: ocgpuCUlibrary,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryGetModule else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pMod, library) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pMod, library);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryGetUnifiedFunction(
    fptr: *mut *mut c_void,
    library: ocgpuCUlibrary,
    symbol: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryGetUnifiedFunction
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(fptr, library, symbol) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (fptr, library, symbol);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryLoadData(
    library: *mut ocgpuCUlibrary,
    code: *const c_void,
    jitOptions: *mut ocgpuCUjit_option,
    jitOptionsValues: *mut *mut c_void,
    numJitOptions: u32,
    libraryOptions: *mut ocgpuCUlibraryOption,
    libraryOptionValues: *mut *mut c_void,
    numLibraryOptions: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryLoadData else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                library,
                code,
                jitOptions,
                jitOptionsValues,
                numJitOptions,
                libraryOptions,
                libraryOptionValues,
                numLibraryOptions,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            library,
            code,
            jitOptions,
            jitOptionsValues,
            numJitOptions,
            libraryOptions,
            libraryOptionValues,
            numLibraryOptions,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryLoadFromFile(
    library: *mut ocgpuCUlibrary,
    fileName: *const c_char,
    jitOptions: *mut ocgpuCUjit_option,
    jitOptionsValues: *mut *mut c_void,
    numJitOptions: u32,
    libraryOptions: *mut ocgpuCUlibraryOption,
    libraryOptionValues: *mut *mut c_void,
    numLibraryOptions: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryLoadFromFile else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                library,
                fileName,
                jitOptions,
                jitOptionsValues,
                numJitOptions,
                libraryOptions,
                libraryOptionValues,
                numLibraryOptions,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            library,
            fileName,
            jitOptions,
            jitOptionsValues,
            numJitOptions,
            libraryOptions,
            libraryOptionValues,
            numLibraryOptions,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLibraryUnload(library: ocgpuCUlibrary) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLibraryUnload else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(library) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (library,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkAddData_v2(
    state: ocgpuCUlinkState,
    type_: ocgpuCUjitInputType,
    data: *mut c_void,
    size: usize,
    name: *const c_char,
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkAddData_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                state,
                type_,
                data,
                size,
                name,
                numOptions,
                options,
                optionValues,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            state,
            type_,
            data,
            size,
            name,
            numOptions,
            options,
            optionValues,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkAddFile_v2(
    state: ocgpuCUlinkState,
    type_: ocgpuCUjitInputType,
    path: *const c_char,
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkAddFile_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state, type_, path, numOptions, options, optionValues) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (state, type_, path, numOptions, options, optionValues);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkComplete(
    state: ocgpuCUlinkState,
    cubinOut: *mut *mut c_void,
    sizeOut: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkComplete else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state, cubinOut, sizeOut) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (state, cubinOut, sizeOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkCreate_v2(
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
    stateOut: *mut ocgpuCUlinkState,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkCreate_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numOptions, options, optionValues, stateOut) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numOptions, options, optionValues, stateOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkDestroy(state: ocgpuCUlinkState) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (state,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointAddDevice(
    leId: ocgpuCUlogicalEndpointId,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointAddDevice
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointBindAddr(
    leId: ocgpuCUlogicalEndpointId,
    dev: ocgpuCUdevice,
    offset: ocgpucuuint64_t,
    ptr: *mut c_void,
    size: ocgpucuuint64_t,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointBindAddr
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, dev, offset, ptr, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, dev, offset, ptr, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointBindMem(
    leId: ocgpuCUlogicalEndpointId,
    dev: ocgpuCUdevice,
    offset: ocgpucuuint64_t,
    memHandle: ocgpuCUmemGenericAllocationHandle,
    memOffset: ocgpucuuint64_t,
    size: ocgpucuuint64_t,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointBindMem
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, dev, offset, memHandle, memOffset, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, dev, offset, memHandle, memOffset, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointCreate(
    leId: ocgpuCUlogicalEndpointId,
    prop: *const ocgpuCUlogicalEndpointProp,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointCreate
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, prop) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, prop);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointDestroy(
    leId: ocgpuCUlogicalEndpointId,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointDestroy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointExport(
    handle: *mut c_void,
    leId: ocgpuCUlogicalEndpointId,
    handleType: ocgpuCUlogicalEndpointIpcHandleType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointExport
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, leId, handleType) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle, leId, handleType);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointGetLimits(
    bindAlignment: *mut ocgpucuuint64_t,
    maxSize: *mut ocgpucuuint64_t,
    prop: *const ocgpuCUlogicalEndpointProp,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointGetLimits
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(bindAlignment, maxSize, prop) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (bindAlignment, maxSize, prop);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointIdRelease(
    baseLeId: ocgpuCUlogicalEndpointId,
    count: ocgpucuuint32_t,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointIdRelease
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(baseLeId, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (baseLeId, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointIdReserve(
    baseLeId: *mut ocgpuCUlogicalEndpointId,
    count: ocgpucuuint32_t,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointIdReserve
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(baseLeId, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (baseLeId, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointImport(
    leId: ocgpuCUlogicalEndpointId,
    handle: *const c_void,
    handleType: ocgpuCUlogicalEndpointIpcHandleType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointImport
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, handle, handleType) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, handle, handleType);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointQuery(
    leId: ocgpuCUlogicalEndpointId,
    count: ocgpucuuint32_t,
    queryStatus: *mut i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointQuery else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, count, queryStatus) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, count, queryStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogicalEndpointUnbind(
    leId: ocgpuCUlogicalEndpointId,
    dev: ocgpuCUdevice,
    offset: ocgpucuuint64_t,
    size: ocgpucuuint64_t,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogicalEndpointUnbind
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leId, dev, offset, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (leId, dev, offset, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogsCurrent(
    iterator_out: *mut ocgpuCUlogIterator,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogsCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(iterator_out, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (iterator_out, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogsDumpToFile(
    iterator: *mut ocgpuCUlogIterator,
    pathToFile: *const c_char,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogsDumpToFile else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(iterator, pathToFile, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (iterator, pathToFile, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogsDumpToMemory(
    iterator: *mut ocgpuCUlogIterator,
    buffer: *mut c_char,
    size: *mut usize,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogsDumpToMemory else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(iterator, buffer, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (iterator, buffer, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogsRegisterCallback(
    callbackFunc: ocgpuCUlogsCallback,
    userData: *mut c_void,
    callback_out: *mut ocgpuCUlogsCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogsRegisterCallback else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(callbackFunc, userData, callback_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (callbackFunc, userData, callback_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLogsUnregisterCallback(
    callback: ocgpuCUlogsCallbackHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLogsUnregisterCallback
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(callback) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (callback,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAdvise(
    devPtr: ocgpuCUdeviceptr,
    count: usize,
    advice: ocgpuCUmem_advise,
    device: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAdvise else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, count, advice, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (devPtr, count, advice, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocFromPoolAsync(
    dptr: *mut ocgpuCUdeviceptr,
    bytesize: usize,
    pool: ocgpuCUmemoryPool,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocFromPoolAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytesize, pool, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytesize, pool, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemBatchDecompressAsync(
    paramsArray: *mut ocgpuCUmemDecompressParams,
    count: usize,
    flags: u32,
    errorIndex: *mut usize,
    stream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemBatchDecompressAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(paramsArray, count, flags, errorIndex, stream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (paramsArray, count, flags, errorIndex, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemCreate(
    handle: *mut ocgpuCUmemGenericAllocationHandle,
    size: usize,
    prop: *const ocgpuCUmemAllocationProp,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, size, prop, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle, size, prop, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemDiscardAndPrefetchBatchAsync(
    dptrs: *mut ocgpuCUdeviceptr,
    sizes: *mut usize,
    count: usize,
    prefetchLocs: *mut ocgpuCUmemLocation,
    prefetchLocIdxs: *mut usize,
    numPrefetchLocs: usize,
    flags: u64,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemDiscardAndPrefetchBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dptrs,
                sizes,
                count,
                prefetchLocs,
                prefetchLocIdxs,
                numPrefetchLocs,
                flags,
                hStream,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            dptrs,
            sizes,
            count,
            prefetchLocs,
            prefetchLocIdxs,
            numPrefetchLocs,
            flags,
            hStream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetAccess(
    flags: *mut u64,
    location: *const ocgpuCUmemLocation,
    ptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags, location, ptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (flags, location, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetAllocationGranularity(
    granularity: *mut usize,
    prop: *const ocgpuCUmemAllocationProp,
    option: ocgpuCUmemAllocationGranularity_flags,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemGetAllocationGranularity
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(granularity, prop, option) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (granularity, prop, option);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetAllocationPropertiesFromHandle(
    prop: *mut ocgpuCUmemAllocationProp,
    handle: ocgpuCUmemGenericAllocationHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemGetAllocationPropertiesFromHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(prop, handle) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (prop, handle);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetDefaultMemPool(
    pool_out: *mut ocgpuCUmemoryPool,
    location: *mut ocgpuCUmemLocation,
    type_: ocgpuCUmemAllocationType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetDefaultMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool_out, location, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool_out, location, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetHandleForAddressRange(
    handle: *mut c_void,
    dptr: ocgpuCUdeviceptr,
    size: usize,
    handleType: ocgpuCUmemRangeHandleType,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemGetHandleForAddressRange
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, dptr, size, handleType, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle, dptr, size, handleType, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetMemPool(
    pool: *mut ocgpuCUmemoryPool,
    location: *mut ocgpuCUmemLocation,
    type_: ocgpuCUmemAllocationType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, location, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, location, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemMap(
    ptr: ocgpuCUdeviceptr,
    size: usize,
    offset: usize,
    handle: ocgpuCUmemGenericAllocationHandle,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemMap else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, offset, handle, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ptr, size, offset, handle, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemMapArrayAsync(
    mapInfoList: *mut ocgpuCUarrayMapInfo,
    count: u32,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemMapArrayAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mapInfoList, count, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mapInfoList, count, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolCreate(
    pool: *mut ocgpuCUmemoryPool,
    poolProps: *const ocgpuCUmemPoolProps,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, poolProps) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, poolProps);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolDestroy(pool: ocgpuCUmemoryPool) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolExportPointer(
    shareData_out: *mut ocgpuCUmemPoolPtrExportData,
    ptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolExportPointer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(shareData_out, ptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (shareData_out, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolGetAccess(
    flags: *mut ocgpuCUmemAccess_flags,
    memPool: ocgpuCUmemoryPool,
    location: *mut ocgpuCUmemLocation,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolGetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags, memPool, location) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (flags, memPool, location);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolGetAttribute(
    pool: ocgpuCUmemoryPool,
    attr: ocgpuCUmemPool_attribute,
    value: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, attr, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolImportPointer(
    ptr_out: *mut ocgpuCUdeviceptr,
    pool: ocgpuCUmemoryPool,
    shareData: *mut ocgpuCUmemPoolPtrExportData,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolImportPointer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr_out, pool, shareData) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ptr_out, pool, shareData);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolSetAccess(
    pool: ocgpuCUmemoryPool,
    map: *const ocgpuCUmemAccessDesc,
    count: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolSetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, map, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, map, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolSetAttribute(
    pool: ocgpuCUmemoryPool,
    attr: ocgpuCUmemPool_attribute,
    value: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, attr, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolTrimTo(
    pool: ocgpuCUmemoryPool,
    minBytesToKeep: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPoolTrimTo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, minBytesToKeep) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool, minBytesToKeep);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPrefetchBatchAsync(
    dptrs: *mut ocgpuCUdeviceptr,
    sizes: *mut usize,
    count: usize,
    prefetchLocs: *mut ocgpuCUmemLocation,
    prefetchLocIdxs: *mut usize,
    numPrefetchLocs: usize,
    flags: u64,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPrefetchBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dptrs,
                sizes,
                count,
                prefetchLocs,
                prefetchLocIdxs,
                numPrefetchLocs,
                flags,
                hStream,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            dptrs,
            sizes,
            count,
            prefetchLocs,
            prefetchLocIdxs,
            numPrefetchLocs,
            flags,
            hStream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemRangeGetAttribute(
    data: *mut c_void,
    dataSize: usize,
    attribute: ocgpuCUmem_range_attribute,
    devPtr: ocgpuCUdeviceptr,
    count: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemRangeGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(data, dataSize, attribute, devPtr, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (data, dataSize, attribute, devPtr, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemRangeGetAttributes(
    data: *mut *mut c_void,
    dataSizes: *mut usize,
    attributes: *mut ocgpuCUmem_range_attribute,
    numAttributes: usize,
    devPtr: ocgpuCUdeviceptr,
    count: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemRangeGetAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(data, dataSizes, attributes, numAttributes, devPtr, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (data, dataSizes, attributes, numAttributes, devPtr, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemRelease(
    handle: ocgpuCUmemGenericAllocationHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemRelease else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemRetainAllocationHandle(
    handle: *mut ocgpuCUmemGenericAllocationHandle,
    addr: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemRetainAllocationHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, addr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle, addr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemSetAccess(
    ptr: ocgpuCUdeviceptr,
    size: usize,
    desc: *const ocgpuCUmemAccessDesc,
    count: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemSetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, desc, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ptr, size, desc, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemSetMemPool(
    location: *mut ocgpuCUmemLocation,
    type_: ocgpuCUmemAllocationType,
    pool: ocgpuCUmemoryPool,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemSetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(location, type_, pool) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (location, type_, pool);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy2DAsync_v2(
    pCopy: *const ocgpuCUDA_MEMCPY2D,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy2DAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy2DUnaligned_v2(
    pCopy: *const ocgpuCUDA_MEMCPY2D,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy2DUnaligned_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy2D_v2(pCopy: *const ocgpuCUDA_MEMCPY2D) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy2D_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DAsync_v2(
    pCopy: *const ocgpuCUDA_MEMCPY3D,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3DAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DBatchAsync(
    numOps: usize,
    opList: *mut ocgpuCUDA_MEMCPY3D_BATCH_OP,
    failIdx: *mut usize,
    flags: u64,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3DBatchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numOps, opList, failIdx, flags, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numOps, opList, failIdx, flags, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DBatchAsync_v2(
    numOps: usize,
    opList: *mut ocgpuCUDA_MEMCPY3D_BATCH_OP,
    flags: u64,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3DBatchAsync_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numOps, opList, flags, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numOps, opList, flags, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DPeer(
    pCopy: *const ocgpuCUDA_MEMCPY3D_PEER,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3DPeer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DPeerAsync(
    pCopy: *const ocgpuCUDA_MEMCPY3D_PEER,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3DPeerAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DWithAttributesAsync(
    op: *mut ocgpuCUDA_MEMCPY3D_BATCH_OP,
    flags: u64,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemcpy3DWithAttributesAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(op, flags, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (op, flags, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3D_v2(pCopy: *const ocgpuCUDA_MEMCPY3D) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3D_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoA_v2(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoA_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoD_v2(
    dstDevice: ocgpuCUdeviceptr,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoD_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoHAsync_v2(
    dstHost: *mut c_void,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoHAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcArray, srcOffset, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcArray, srcOffset, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoH_v2(
    dstHost: *mut c_void,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoH_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyBatchAsync(
    dsts: *mut ocgpuCUdeviceptr,
    srcs: *mut ocgpuCUdeviceptr,
    sizes: *mut usize,
    count: usize,
    attrs: *mut ocgpuCUmemcpyAttributes,
    attrsIdxs: *mut usize,
    numAttrs: usize,
    failIdx: *mut usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyBatchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dsts, srcs, sizes, count, attrs, attrsIdxs, numAttrs, failIdx, hStream,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            dsts, srcs, sizes, count, attrs, attrsIdxs, numAttrs, failIdx, hStream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyBatchAsync_v2(
    dsts: *mut ocgpuCUdeviceptr,
    srcs: *mut ocgpuCUdeviceptr,
    sizes: *mut usize,
    count: usize,
    attrs: *mut ocgpuCUmemcpyAttributes,
    attrsIdxs: *mut usize,
    numAttrs: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyBatchAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dsts, srcs, sizes, count, attrs, attrsIdxs, numAttrs, hStream,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            dsts, srcs, sizes, count, attrs, attrsIdxs, numAttrs, hStream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoA_v2(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoA_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcDevice, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcDevice, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoAAsync_v2(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcHost: *const c_void,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoAAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcHost, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcHost, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoA_v2(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcHost: *const c_void,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoA_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcHost, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcHost, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyWithAttributesAsync(
    dst: ocgpuCUdeviceptr,
    src: ocgpuCUdeviceptr,
    size: usize,
    attr: *mut ocgpuCUmemcpyAttributes,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyWithAttributesAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, size, attr, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dst, src, size, attr, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMipmappedArrayCreate(
    pHandle: *mut ocgpuCUmipmappedArray,
    pMipmappedArrayDesc: *const ocgpuCUDA_ARRAY3D_DESCRIPTOR,
    numMipmapLevels: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMipmappedArrayCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pMipmappedArrayDesc, numMipmapLevels) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, pMipmappedArrayDesc, numMipmapLevels);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMipmappedArrayDestroy(
    hMipmappedArray: ocgpuCUmipmappedArray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMipmappedArrayDestroy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hMipmappedArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hMipmappedArray,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMipmappedArrayGetLevel(
    pLevelArray: *mut ocgpuCUarray,
    hMipmappedArray: ocgpuCUmipmappedArray,
    level: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMipmappedArrayGetLevel
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pLevelArray, hMipmappedArray, level) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pLevelArray, hMipmappedArray, level);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMipmappedArrayGetMemoryRequirements(
    memoryRequirements: *mut ocgpuCUDA_ARRAY_MEMORY_REQUIREMENTS,
    mipmap: ocgpuCUmipmappedArray,
    device: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMipmappedArrayGetMemoryRequirements
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(memoryRequirements, mipmap, device) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (memoryRequirements, mipmap, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMipmappedArrayGetSparseProperties(
    sparseProperties: *mut ocgpuCUDA_ARRAY_SPARSE_PROPERTIES,
    mipmap: ocgpuCUmipmappedArray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMipmappedArrayGetSparseProperties
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(sparseProperties, mipmap) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (sparseProperties, mipmap);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetLoadingMode(
    mode: *mut ocgpuCUmoduleLoadingMode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetLoadingMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mode,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetSurfRef(
    pSurfRef: *mut ocgpuCUsurfref,
    hmod: ocgpuCUmodule,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetSurfRef else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pSurfRef, hmod, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pSurfRef, hmod, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetTexRef(
    pTexRef: *mut ocgpuCUtexref,
    hmod: ocgpuCUmodule,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetTexRef else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexRef, hmod, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pTexRef, hmod, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleLoadDataEx(
    module: *mut ocgpuCUmodule,
    image: *const c_void,
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleLoadDataEx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, image, numOptions, options, optionValues) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (module, image, numOptions, options, optionValues);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastAddDevice(
    mcHandle: ocgpuCUmemGenericAllocationHandle,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastAddDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastBindAddr(
    mcHandle: ocgpuCUmemGenericAllocationHandle,
    mcOffset: usize,
    memptr: ocgpuCUdeviceptr,
    size: usize,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastBindAddr else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, mcOffset, memptr, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, mcOffset, memptr, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastBindAddr_v2(
    mcHandle: ocgpuCUmemGenericAllocationHandle,
    dev: ocgpuCUdevice,
    mcOffset: usize,
    memptr: ocgpuCUdeviceptr,
    size: usize,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastBindAddr_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, dev, mcOffset, memptr, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, dev, mcOffset, memptr, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastBindMem(
    mcHandle: ocgpuCUmemGenericAllocationHandle,
    mcOffset: usize,
    memHandle: ocgpuCUmemGenericAllocationHandle,
    memOffset: usize,
    size: usize,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastBindMem else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, mcOffset, memHandle, memOffset, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, mcOffset, memHandle, memOffset, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastBindMem_v2(
    mcHandle: ocgpuCUmemGenericAllocationHandle,
    dev: ocgpuCUdevice,
    mcOffset: usize,
    memHandle: ocgpuCUmemGenericAllocationHandle,
    memOffset: usize,
    size: usize,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastBindMem_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, dev, mcOffset, memHandle, memOffset, size, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, dev, mcOffset, memHandle, memOffset, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastCreate(
    mcHandle: *mut ocgpuCUmemGenericAllocationHandle,
    prop: *const ocgpuCUmulticastObjectProp,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, prop) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, prop);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastGetGranularity(
    granularity: *mut usize,
    prop: *const ocgpuCUmulticastObjectProp,
    option: ocgpuCUmulticastGranularity_flags,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastGetGranularity
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(granularity, prop, option) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (granularity, prop, option);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMulticastUnbind(
    mcHandle: ocgpuCUmemGenericAllocationHandle,
    dev: ocgpuCUdevice,
    mcOffset: usize,
    size: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMulticastUnbind else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mcHandle, dev, mcOffset, size) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mcHandle, dev, mcOffset, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyMaxActiveClusters(
    numClusters: *mut i32,
    func: ocgpuCUfunction,
    config: *const ocgpuCUlaunchConfig,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyMaxActiveClusters
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numClusters, func, config) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numClusters, func, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyMaxPotentialBlockSize(
    minGridSize: *mut i32,
    blockSize: *mut i32,
    func: ocgpuCUfunction,
    blockSizeToDynamicSMemSize: ocgpuCUoccupancyB2DSize,
    dynamicSMemSize: usize,
    blockSizeLimit: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyMaxPotentialBlockSize
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                minGridSize,
                blockSize,
                func,
                blockSizeToDynamicSMemSize,
                dynamicSMemSize,
                blockSizeLimit,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            minGridSize,
            blockSize,
            func,
            blockSizeToDynamicSMemSize,
            dynamicSMemSize,
            blockSizeLimit,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyMaxPotentialBlockSizeWithFlags(
    minGridSize: *mut i32,
    blockSize: *mut i32,
    func: ocgpuCUfunction,
    blockSizeToDynamicSMemSize: ocgpuCUoccupancyB2DSize,
    dynamicSMemSize: usize,
    blockSizeLimit: i32,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyMaxPotentialBlockSizeWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                minGridSize,
                blockSize,
                func,
                blockSizeToDynamicSMemSize,
                dynamicSMemSize,
                blockSizeLimit,
                flags,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            minGridSize,
            blockSize,
            func,
            blockSizeToDynamicSMemSize,
            dynamicSMemSize,
            blockSizeLimit,
            flags,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuOccupancyMaxPotentialClusterSize(
    clusterSize: *mut i32,
    func: ocgpuCUfunction,
    config: *const ocgpuCUlaunchConfig,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuOccupancyMaxPotentialClusterSize
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(clusterSize, func, config) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (clusterSize, func, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuParamSetTexRef(
    hfunc: ocgpuCUfunction,
    texunit: i32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuParamSetTexRef else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hfunc, texunit, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hfunc, texunit, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuPointerGetAttribute(
    data: *mut c_void,
    attribute: ocgpuCUpointer_attribute,
    ptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuPointerGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(data, attribute, ptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (data, attribute, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuPointerGetAttributes(
    numAttributes: u32,
    attributes: *mut ocgpuCUpointer_attribute,
    data: *mut *mut c_void,
    ptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuPointerGetAttributes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numAttributes, attributes, data, ptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numAttributes, attributes, data, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuPointerSetAttribute(
    value: *const c_void,
    attribute: ocgpuCUpointer_attribute,
    ptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuPointerSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attribute, ptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (value, attribute, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuProfilerInitialize(
    configFile: *const c_char,
    outputFile: *const c_char,
    outputMode: ocgpuCUoutput_mode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuProfilerInitialize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(configFile, outputFile, outputMode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (configFile, outputFile, outputMode);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuSignalExternalSemaphoresAsync(
    extSemArray: *const ocgpuCUexternalSemaphore,
    paramsArray: *const ocgpuCUDA_EXTERNAL_SEMAPHORE_SIGNAL_PARAMS,
    numExtSems: u32,
    stream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuSignalExternalSemaphoresAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSemArray, paramsArray, numExtSems, stream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (extSemArray, paramsArray, numExtSems, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamAddCallback(
    hStream: ocgpuCUstream,
    callback: ocgpuCUstreamCallback,
    userData: *mut c_void,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamAddCallback else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, callback, userData, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, callback, userData, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBatchMemOp(
    stream: ocgpuCUstream,
    count: u32,
    paramArray: *mut ocgpuCUstreamBatchMemOpParams,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamBatchMemOp else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, count, paramArray, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, count, paramArray, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBatchMemOp_v2(
    stream: ocgpuCUstream,
    count: u32,
    paramArray: *mut ocgpuCUstreamBatchMemOpParams,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamBatchMemOp_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, count, paramArray, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, count, paramArray, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBeginCaptureToCig(
    hStream: ocgpuCUstream,
    streamCigCaptureParams: *mut ocgpuCUstreamCigCaptureParams,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamBeginCaptureToCig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, streamCigCaptureParams) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, streamCigCaptureParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBeginCaptureToGraph(
    hStream: ocgpuCUstream,
    hGraph: ocgpuCUgraph,
    dependencies: *const ocgpuCUgraphNode,
    dependencyData: *const ocgpuCUgraphEdgeData,
    numDependencies: usize,
    mode: ocgpuCUstreamCaptureMode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamBeginCaptureToGraph
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                hStream,
                hGraph,
                dependencies,
                dependencyData,
                numDependencies,
                mode,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            hStream,
            hGraph,
            dependencies,
            dependencyData,
            numDependencies,
            mode,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBeginCapture_v2(
    hStream: ocgpuCUstream,
    mode: ocgpuCUstreamCaptureMode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamBeginCapture_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, mode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, mode);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBeginRecaptureToGraph(
    hStream: ocgpuCUstream,
    mode: ocgpuCUstreamCaptureMode,
    hGraph: ocgpuCUgraph,
    callbackFunc: ocgpuCUgraphRecaptureCallback,
    userData: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuStreamBeginRecaptureToGraph
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, mode, hGraph, callbackFunc, userData) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, mode, hGraph, callbackFunc, userData);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamEndCapture(
    hStream: ocgpuCUstream,
    phGraph: *mut ocgpuCUgraph,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamEndCapture else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, phGraph) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, phGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetAttribute(
    hStream: ocgpuCUstream,
    attr: ocgpuCUstreamAttrID,
    value_out: *mut ocgpuCUstreamAttrValue,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, attr, value_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, attr, value_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetCaptureInfo(
    hStream: ocgpuCUstream,
    captureStatus_out: *mut ocgpuCUstreamCaptureStatus,
    id_out: *mut ocgpucuuint64_t,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetCaptureInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, captureStatus_out, id_out) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, captureStatus_out, id_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetCaptureInfo_v2(
    hStream: ocgpuCUstream,
    captureStatus_out: *mut ocgpuCUstreamCaptureStatus,
    id_out: *mut ocgpucuuint64_t,
    graph_out: *mut ocgpuCUgraph,
    dependencies_out: *mut *const ocgpuCUgraphNode,
    numDependencies_out: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetCaptureInfo_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                hStream,
                captureStatus_out,
                id_out,
                graph_out,
                dependencies_out,
                numDependencies_out,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            hStream,
            captureStatus_out,
            id_out,
            graph_out,
            dependencies_out,
            numDependencies_out,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetCaptureInfo_v3(
    hStream: ocgpuCUstream,
    captureStatus_out: *mut ocgpuCUstreamCaptureStatus,
    id_out: *mut ocgpucuuint64_t,
    graph_out: *mut ocgpuCUgraph,
    dependencies_out: *mut *const ocgpuCUgraphNode,
    edgeData_out: *mut *const ocgpuCUgraphEdgeData,
    numDependencies_out: *mut usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetCaptureInfo_v3
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                hStream,
                captureStatus_out,
                id_out,
                graph_out,
                dependencies_out,
                edgeData_out,
                numDependencies_out,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            hStream,
            captureStatus_out,
            id_out,
            graph_out,
            dependencies_out,
            edgeData_out,
            numDependencies_out,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetCtx_v2(
    hStream: ocgpuCUstream,
    pCtx: *mut ocgpuCUcontext,
    pGreenCtx: *mut ocgpuCUgreenCtx,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetCtx_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, pCtx, pGreenCtx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, pCtx, pGreenCtx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetDevResource(
    hStream: ocgpuCUstream,
    resource: *mut ocgpuCUdevResource,
    type_: ocgpuCUdevResourceType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetDevResource else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, resource, type_) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamGetGreenCtx(
    hStream: ocgpuCUstream,
    phCtx: *mut ocgpuCUgreenCtx,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamGetGreenCtx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, phCtx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, phCtx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamIsCapturing(
    hStream: ocgpuCUstream,
    captureStatus: *mut ocgpuCUstreamCaptureStatus,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamIsCapturing else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, captureStatus) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, captureStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamSetAttribute(
    hStream: ocgpuCUstream,
    attr: ocgpuCUstreamAttrID,
    value: *const ocgpuCUstreamAttrValue,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, attr, value) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamUpdateCaptureDependencies(
    hStream: ocgpuCUstream,
    dependencies: *mut ocgpuCUgraphNode,
    numDependencies: usize,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuStreamUpdateCaptureDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, dependencies, numDependencies, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, dependencies, numDependencies, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamUpdateCaptureDependencies_v2(
    hStream: ocgpuCUstream,
    dependencies: *mut ocgpuCUgraphNode,
    dependencyData: *const ocgpuCUgraphEdgeData,
    numDependencies: usize,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuStreamUpdateCaptureDependencies_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                hStream,
                dependencies,
                dependencyData,
                numDependencies,
                flags,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            hStream,
            dependencies,
            dependencyData,
            numDependencies,
            flags,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWaitValue32(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint32_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWaitValue32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWaitValue32_v2(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint32_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWaitValue32_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWaitValue64(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint64_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWaitValue64 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWaitValue64_v2(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint64_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWaitValue64_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWriteValue32(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint32_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWriteValue32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWriteValue32_v2(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint32_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWriteValue32_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWriteValue64(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint64_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWriteValue64 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamWriteValue64_v2(
    stream: ocgpuCUstream,
    addr: ocgpuCUdeviceptr,
    value: ocgpucuuint64_t,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamWriteValue64_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, addr, value, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (stream, addr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuSurfObjectCreate(
    pSurfObject: *mut ocgpuCUsurfObject,
    pResDesc: *const ocgpuCUDA_RESOURCE_DESC,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuSurfObjectCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pSurfObject, pResDesc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pSurfObject, pResDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuSurfObjectDestroy(surfObject: ocgpuCUsurfObject) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuSurfObjectDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(surfObject) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (surfObject,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuSurfObjectGetResourceDesc(
    pResDesc: *mut ocgpuCUDA_RESOURCE_DESC,
    surfObject: ocgpuCUsurfObject,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuSurfObjectGetResourceDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResDesc, surfObject) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pResDesc, surfObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuSurfRefGetArray(
    phArray: *mut ocgpuCUarray,
    hSurfRef: ocgpuCUsurfref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuSurfRefGetArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phArray, hSurfRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phArray, hSurfRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuSurfRefSetArray(
    hSurfRef: ocgpuCUsurfref,
    hArray: ocgpuCUarray,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuSurfRefSetArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hSurfRef, hArray, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hSurfRef, hArray, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTensorMapEncodeIm2col(
    tensorMap: *mut ocgpuCUtensorMap,
    tensorDataType: ocgpuCUtensorMapDataType,
    tensorRank: ocgpucuuint32_t,
    globalAddress: *mut c_void,
    globalDim: *const ocgpucuuint64_t,
    globalStrides: *const ocgpucuuint64_t,
    pixelBoxLowerCorner: *const i32,
    pixelBoxUpperCorner: *const i32,
    channelsPerPixel: ocgpucuuint32_t,
    pixelsPerColumn: ocgpucuuint32_t,
    elementStrides: *const ocgpucuuint32_t,
    interleave: ocgpuCUtensorMapInterleave,
    swizzle: ocgpuCUtensorMapSwizzle,
    l2Promotion: ocgpuCUtensorMapL2promotion,
    oobFill: ocgpuCUtensorMapFloatOOBfill,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTensorMapEncodeIm2col
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                tensorMap,
                tensorDataType,
                tensorRank,
                globalAddress,
                globalDim,
                globalStrides,
                pixelBoxLowerCorner,
                pixelBoxUpperCorner,
                channelsPerPixel,
                pixelsPerColumn,
                elementStrides,
                interleave,
                swizzle,
                l2Promotion,
                oobFill,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            tensorMap,
            tensorDataType,
            tensorRank,
            globalAddress,
            globalDim,
            globalStrides,
            pixelBoxLowerCorner,
            pixelBoxUpperCorner,
            channelsPerPixel,
            pixelsPerColumn,
            elementStrides,
            interleave,
            swizzle,
            l2Promotion,
            oobFill,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTensorMapEncodeIm2colWide(
    tensorMap: *mut ocgpuCUtensorMap,
    tensorDataType: ocgpuCUtensorMapDataType,
    tensorRank: ocgpucuuint32_t,
    globalAddress: *mut c_void,
    globalDim: *const ocgpucuuint64_t,
    globalStrides: *const ocgpucuuint64_t,
    pixelBoxLowerCornerWidth: i32,
    pixelBoxUpperCornerWidth: i32,
    channelsPerPixel: ocgpucuuint32_t,
    pixelsPerColumn: ocgpucuuint32_t,
    elementStrides: *const ocgpucuuint32_t,
    interleave: ocgpuCUtensorMapInterleave,
    mode: ocgpuCUtensorMapIm2ColWideMode,
    swizzle: ocgpuCUtensorMapSwizzle,
    l2Promotion: ocgpuCUtensorMapL2promotion,
    oobFill: ocgpuCUtensorMapFloatOOBfill,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTensorMapEncodeIm2colWide
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                tensorMap,
                tensorDataType,
                tensorRank,
                globalAddress,
                globalDim,
                globalStrides,
                pixelBoxLowerCornerWidth,
                pixelBoxUpperCornerWidth,
                channelsPerPixel,
                pixelsPerColumn,
                elementStrides,
                interleave,
                mode,
                swizzle,
                l2Promotion,
                oobFill,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            tensorMap,
            tensorDataType,
            tensorRank,
            globalAddress,
            globalDim,
            globalStrides,
            pixelBoxLowerCornerWidth,
            pixelBoxUpperCornerWidth,
            channelsPerPixel,
            pixelsPerColumn,
            elementStrides,
            interleave,
            mode,
            swizzle,
            l2Promotion,
            oobFill,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTensorMapEncodeTiled(
    tensorMap: *mut ocgpuCUtensorMap,
    tensorDataType: ocgpuCUtensorMapDataType,
    tensorRank: ocgpucuuint32_t,
    globalAddress: *mut c_void,
    globalDim: *const ocgpucuuint64_t,
    globalStrides: *const ocgpucuuint64_t,
    boxDim: *const ocgpucuuint32_t,
    elementStrides: *const ocgpucuuint32_t,
    interleave: ocgpuCUtensorMapInterleave,
    swizzle: ocgpuCUtensorMapSwizzle,
    l2Promotion: ocgpuCUtensorMapL2promotion,
    oobFill: ocgpuCUtensorMapFloatOOBfill,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTensorMapEncodeTiled else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                tensorMap,
                tensorDataType,
                tensorRank,
                globalAddress,
                globalDim,
                globalStrides,
                boxDim,
                elementStrides,
                interleave,
                swizzle,
                l2Promotion,
                oobFill,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            tensorMap,
            tensorDataType,
            tensorRank,
            globalAddress,
            globalDim,
            globalStrides,
            boxDim,
            elementStrides,
            interleave,
            swizzle,
            l2Promotion,
            oobFill,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTensorMapReplaceAddress(
    tensorMap: *mut ocgpuCUtensorMap,
    globalAddress: *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTensorMapReplaceAddress
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(tensorMap, globalAddress) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (tensorMap, globalAddress);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexObjectCreate(
    pTexObject: *mut ocgpuCUtexObject,
    pResDesc: *const ocgpuCUDA_RESOURCE_DESC,
    pTexDesc: *const ocgpuCUDA_TEXTURE_DESC,
    pResViewDesc: *const ocgpuCUDA_RESOURCE_VIEW_DESC,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexObjectCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexObject, pResDesc, pTexDesc, pResViewDesc) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pTexObject, pResDesc, pTexDesc, pResViewDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexObjectDestroy(texObject: ocgpuCUtexObject) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexObjectDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texObject) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (texObject,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexObjectGetResourceDesc(
    pResDesc: *mut ocgpuCUDA_RESOURCE_DESC,
    texObject: ocgpuCUtexObject,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexObjectGetResourceDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResDesc, texObject) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pResDesc, texObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexObjectGetResourceViewDesc(
    pResViewDesc: *mut ocgpuCUDA_RESOURCE_VIEW_DESC,
    texObject: ocgpuCUtexObject,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuTexObjectGetResourceViewDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResViewDesc, texObject) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pResViewDesc, texObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexObjectGetTextureDesc(
    pTexDesc: *mut ocgpuCUDA_TEXTURE_DESC,
    texObject: ocgpuCUtexObject,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexObjectGetTextureDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexDesc, texObject) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pTexDesc, texObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefCreate(pTexRef: *mut ocgpuCUtexref) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pTexRef,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefDestroy(hTexRef: ocgpuCUtexref) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetAddressMode(
    pam: *mut ocgpuCUaddress_mode,
    hTexRef: ocgpuCUtexref,
    dim: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetAddressMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pam, hTexRef, dim) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pam, hTexRef, dim);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetAddress_v2(
    pdptr: *mut ocgpuCUdeviceptr,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetAddress_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pdptr, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pdptr, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetArray(
    phArray: *mut ocgpuCUarray,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phArray, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phArray, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetBorderColor(
    pBorderColor: *mut f32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetBorderColor else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pBorderColor, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pBorderColor, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetFilterMode(
    pfm: *mut ocgpuCUfilter_mode,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetFilterMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pfm, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pfm, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetFlags(
    pFlags: *mut u32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFlags, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pFlags, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetFormat(
    pFormat: *mut ocgpuCUarray_format,
    pNumChannels: *mut i32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetFormat else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFormat, pNumChannels, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pFormat, pNumChannels, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetMaxAnisotropy(
    pmaxAniso: *mut i32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetMaxAnisotropy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pmaxAniso, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pmaxAniso, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetMipmapFilterMode(
    pfm: *mut ocgpuCUfilter_mode,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetMipmapFilterMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pfm, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pfm, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetMipmapLevelBias(
    pbias: *mut f32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetMipmapLevelBias
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pbias, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pbias, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetMipmapLevelClamp(
    pminMipmapLevelClamp: *mut f32,
    pmaxMipmapLevelClamp: *mut f32,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetMipmapLevelClamp
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pminMipmapLevelClamp, pmaxMipmapLevelClamp, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pminMipmapLevelClamp, pmaxMipmapLevelClamp, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetMipmappedArray(
    phMipmappedArray: *mut ocgpuCUmipmappedArray,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phMipmappedArray, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phMipmappedArray, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetAddress2D_v3(
    hTexRef: ocgpuCUtexref,
    desc: *const ocgpuCUDA_ARRAY_DESCRIPTOR,
    dptr: ocgpuCUdeviceptr,
    Pitch: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetAddress2D_v3
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, desc, dptr, Pitch) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, desc, dptr, Pitch);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetAddressMode(
    hTexRef: ocgpuCUtexref,
    dim: i32,
    am: ocgpuCUaddress_mode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetAddressMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, dim, am) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, dim, am);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetAddress_v2(
    ByteOffset: *mut usize,
    hTexRef: ocgpuCUtexref,
    dptr: ocgpuCUdeviceptr,
    bytes: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetAddress_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ByteOffset, hTexRef, dptr, bytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ByteOffset, hTexRef, dptr, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetArray(
    hTexRef: ocgpuCUtexref,
    hArray: ocgpuCUarray,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, hArray, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, hArray, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetBorderColor(
    hTexRef: ocgpuCUtexref,
    pBorderColor: *mut f32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetBorderColor else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, pBorderColor) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, pBorderColor);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetFilterMode(
    hTexRef: ocgpuCUtexref,
    fm: ocgpuCUfilter_mode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetFilterMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, fm) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, fm);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetFlags(
    hTexRef: ocgpuCUtexref,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetFormat(
    hTexRef: ocgpuCUtexref,
    fmt: ocgpuCUarray_format,
    NumPackedComponents: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetFormat else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, fmt, NumPackedComponents) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, fmt, NumPackedComponents);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetMaxAnisotropy(
    hTexRef: ocgpuCUtexref,
    maxAniso: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetMaxAnisotropy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, maxAniso) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, maxAniso);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetMipmapFilterMode(
    hTexRef: ocgpuCUtexref,
    fm: ocgpuCUfilter_mode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetMipmapFilterMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, fm) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, fm);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetMipmapLevelBias(
    hTexRef: ocgpuCUtexref,
    bias: f32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetMipmapLevelBias
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, bias) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, bias);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetMipmapLevelClamp(
    hTexRef: ocgpuCUtexref,
    minMipmapLevelClamp: f32,
    maxMipmapLevelClamp: f32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetMipmapLevelClamp
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, minMipmapLevelClamp, maxMipmapLevelClamp) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, minMipmapLevelClamp, maxMipmapLevelClamp);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetMipmappedArray(
    hTexRef: ocgpuCUtexref,
    hMipmappedArray: ocgpuCUmipmappedArray,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, hMipmappedArray, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, hMipmappedArray, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuThreadExchangeStreamCaptureMode(
    mode: *mut ocgpuCUstreamCaptureMode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuThreadExchangeStreamCaptureMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (mode,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuUserObjectCreate(
    object_out: *mut ocgpuCUuserObject,
    ptr: *mut c_void,
    destroy: ocgpuCUhostFn,
    initialRefcount: u32,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuUserObjectCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(object_out, ptr, destroy, initialRefcount, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (object_out, ptr, destroy, initialRefcount, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuUserObjectRelease(
    object: ocgpuCUuserObject,
    count: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuUserObjectRelease else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(object, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (object, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuUserObjectRetain(
    object: ocgpuCUuserObject,
    count: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuUserObjectRetain else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(object, count) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (object, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuWaitExternalSemaphoresAsync(
    extSemArray: *const ocgpuCUexternalSemaphore,
    paramsArray: *const ocgpuCUDA_EXTERNAL_SEMAPHORE_WAIT_PARAMS,
    numExtSems: u32,
    stream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuWaitExternalSemaphoresAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSemArray, paramsArray, numExtSems, stream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (extSemArray, paramsArray, numExtSems, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuIpcOpenEventHandle(
    phEvent: *mut ocgpuCUevent,
    handle: ocgpuCUipcEventHandle,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuIpcOpenEventHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phEvent, handle) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phEvent, handle);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuIpcOpenMemHandle_v2(
    pdptr: *mut ocgpuCUdeviceptr,
    handle: ocgpuCUipcMemHandle,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuIpcOpenMemHandle_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pdptr, handle, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pdptr, handle, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAdvise_v2(
    devPtr: ocgpuCUdeviceptr,
    count: usize,
    advice: ocgpuCUmem_advise,
    location: ocgpuCUmemLocation,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAdvise_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, count, advice, location) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (devPtr, count, advice, location);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemExportToShareableHandle(
    shareableHandle: *mut c_void,
    handle: ocgpuCUmemGenericAllocationHandle,
    handleType: ocgpuCUmemAllocationHandleType,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemExportToShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(shareableHandle, handle, handleType, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (shareableHandle, handle, handleType, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemImportFromShareableHandle(
    handle: *mut ocgpuCUmemGenericAllocationHandle,
    osHandle: *mut c_void,
    shHandleType: ocgpuCUmemAllocationHandleType,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemImportFromShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, osHandle, shHandleType) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle, osHandle, shHandleType);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolExportToShareableHandle(
    handle_out: *mut c_void,
    pool: ocgpuCUmemoryPool,
    handleType: ocgpuCUmemAllocationHandleType,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemPoolExportToShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle_out, pool, handleType, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (handle_out, pool, handleType, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPoolImportFromShareableHandle(
    pool_out: *mut ocgpuCUmemoryPool,
    handle: *mut c_void,
    handleType: ocgpuCUmemAllocationHandleType,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuMemPoolImportFromShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool_out, handle, handleType, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pool_out, handle, handleType, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemPrefetchAsync_v2(
    devPtr: ocgpuCUdeviceptr,
    count: usize,
    location: ocgpuCUmemLocation,
    flags: u32,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemPrefetchAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, count, location, flags, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (devPtr, count, location, flags, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCheckpointProcessCheckpoint(
    pid: i32,
    args: *mut ocgpuCUcheckpointCheckpointArgs,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCheckpointProcessCheckpoint
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pid, args) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pid, args);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCheckpointProcessGetRestoreThreadId(
    pid: i32,
    tid: *mut i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuCheckpointProcessGetRestoreThreadId
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pid, tid) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pid, tid);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCheckpointProcessGetState(
    pid: i32,
    state: *mut ocgpuCUprocessState,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCheckpointProcessGetState
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pid, state) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pid, state);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCheckpointProcessLock(
    pid: i32,
    args: *mut ocgpuCUcheckpointLockArgs,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCheckpointProcessLock
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pid, args) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pid, args);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCheckpointProcessRestore(
    pid: i32,
    args: *mut ocgpuCUcheckpointRestoreArgs,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCheckpointProcessRestore
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pid, args) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pid, args);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCheckpointProcessUnlock(
    pid: i32,
    args: *mut ocgpuCUcheckpointUnlockArgs,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCheckpointProcessUnlock
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pid, args) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pid, args);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceGetNvSciSyncAttributes(
    nvSciSyncAttrList: *mut c_void,
    dev: ocgpuCUdevice,
    flags: i32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuDeviceGetNvSciSyncAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(nvSciSyncAttrList, dev, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (nvSciSyncAttrList, dev, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArray3DCreate(
    pHandle: *mut ocgpuCUarray,
    pAllocateArray: *const ocgpuCUDA_ARRAY3D_DESCRIPTOR,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArray3DCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pAllocateArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, pAllocateArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArray3DGetDescriptor(
    pArrayDescriptor: *mut ocgpuCUDA_ARRAY3D_DESCRIPTOR,
    hArray: ocgpuCUarray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArray3DGetDescriptor else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArrayDescriptor, hArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pArrayDescriptor, hArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayCreate(
    pHandle: *mut ocgpuCUarray,
    pAllocateArray: *const ocgpuCUDA_ARRAY_DESCRIPTOR,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pAllocateArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pHandle, pAllocateArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuArrayGetDescriptor(
    pArrayDescriptor: *mut ocgpuCUDA_ARRAY_DESCRIPTOR,
    hArray: ocgpuCUarray,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuArrayGetDescriptor else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArrayDescriptor, hArray) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pArrayDescriptor, hArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxCreate(
    pctx: *mut ocgpuCUcontext,
    ctxCreateParams: *mut ocgpuCUctxCreateParams,
    flags: u32,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx, ctxCreateParams, flags, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx, ctxCreateParams, flags, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxDestroy(ctx: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxPopCurrent(pctx: *mut ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxPopCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuCtxPushCurrent(ctx: ocgpuCUcontext) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuCtxPushCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxRelease(dev: ocgpuCUdevice) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevicePrimaryCtxRelease
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxReset(dev: ocgpuCUdevice) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevicePrimaryCtxReset
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDevicePrimaryCtxSetFlags(
    dev: ocgpuCUdevice,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDevicePrimaryCtxSetFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dev, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuDeviceTotalMem(
    bytes: *mut usize,
    dev: ocgpuCUdevice,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuDeviceTotalMem else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(bytes, dev) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (bytes, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuEventDestroy(hEvent: ocgpuCUevent) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuEventDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hEvent) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hEvent,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphInstantiate(
    phGraphExec: *mut ocgpuCUgraphExec,
    hGraph: ocgpuCUgraph,
    flags: u64,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuGraphInstantiate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphExec, hGraph, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (phGraphExec, hGraph, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsResourceGetMappedPointer(
    pDevPtr: *mut ocgpuCUdeviceptr,
    pSize: *mut usize,
    resource: ocgpuCUgraphicsResource,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsResourceGetMappedPointer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pDevPtr, pSize, resource) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pDevPtr, pSize, resource);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuGraphicsResourceSetMapFlags(
    resource: ocgpuCUgraphicsResource,
    flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuCuGraphicsResourceSetMapFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(resource, flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (resource, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuIpcOpenMemHandle(
    pdptr: *mut ocgpuCUdeviceptr,
    handle: ocgpuCUipcMemHandle,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuIpcOpenMemHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pdptr, handle, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pdptr, handle, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkAddData(
    state: ocgpuCUlinkState,
    type_: ocgpuCUjitInputType,
    data: *mut c_void,
    size: usize,
    name: *const c_char,
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkAddData else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                state,
                type_,
                data,
                size,
                name,
                numOptions,
                options,
                optionValues,
            )
        }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (
            state,
            type_,
            data,
            size,
            name,
            numOptions,
            options,
            optionValues,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkAddFile(
    state: ocgpuCUlinkState,
    type_: ocgpuCUjitInputType,
    path: *const c_char,
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkAddFile else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state, type_, path, numOptions, options, optionValues) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (state, type_, path, numOptions, options, optionValues);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuLinkCreate(
    numOptions: u32,
    options: *mut ocgpuCUjit_option,
    optionValues: *mut *mut c_void,
    stateOut: *mut ocgpuCUlinkState,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuLinkCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numOptions, options, optionValues, stateOut) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (numOptions, options, optionValues, stateOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAlloc(
    dptr: *mut ocgpuCUdeviceptr,
    bytesize: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAlloc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytesize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytesize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocHost(
    pp: *mut *mut c_void,
    bytesize: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocHost else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pp, bytesize) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pp, bytesize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemAllocPitch(
    dptr: *mut ocgpuCUdeviceptr,
    pPitch: *mut usize,
    WidthInBytes: usize,
    Height: usize,
    ElementSizeBytes: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemAllocPitch else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, pPitch, WidthInBytes, Height, ElementSizeBytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, pPitch, WidthInBytes, Height, ElementSizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemFree(dptr: ocgpuCUdeviceptr) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemFree else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetAddressRange(
    pbase: *mut ocgpuCUdeviceptr,
    psize: *mut usize,
    dptr: ocgpuCUdeviceptr,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetAddressRange else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pbase, psize, dptr) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pbase, psize, dptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemGetInfo(free: *mut usize, total: *mut usize) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemGetInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(free, total) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (free, total);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostGetDevicePointer(
    pdptr: *mut ocgpuCUdeviceptr,
    p: *mut c_void,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemHostGetDevicePointer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pdptr, p, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pdptr, p, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemHostRegister(
    p: *mut c_void,
    bytesize: usize,
    Flags: u32,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemHostRegister else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p, bytesize, Flags) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (p, bytesize, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy2D(pCopy: *const ocgpuCUDA_MEMCPY2D) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy2DAsync(
    pCopy: *const ocgpuCUDA_MEMCPY2D,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy2DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy2DUnaligned(
    pCopy: *const ocgpuCUDA_MEMCPY2D,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy2DUnaligned else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3D(pCopy: *const ocgpuCUDA_MEMCPY3D) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpy3DAsync(
    pCopy: *const ocgpuCUDA_MEMCPY3D,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpy3DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pCopy, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoA(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoA else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoD(
    dstDevice: ocgpuCUdeviceptr,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoD else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoH(
    dstHost: *mut c_void,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoH else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyAtoHAsync(
    dstHost: *mut c_void,
    srcArray: ocgpuCUarray,
    srcOffset: usize,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyAtoHAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcArray, srcOffset, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcArray, srcOffset, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoA(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoA else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcDevice, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcDevice, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoD(
    dstDevice: ocgpuCUdeviceptr,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoD else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcDevice, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcDevice, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoDAsync(
    dstDevice: ocgpuCUdeviceptr,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoDAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcDevice, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcDevice, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoH(
    dstHost: *mut c_void,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoH else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcDevice, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcDevice, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyDtoHAsync(
    dstHost: *mut c_void,
    srcDevice: ocgpuCUdeviceptr,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyDtoHAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcDevice, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstHost, srcDevice, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoA(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcHost: *const c_void,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoA else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcHost, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcHost, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoAAsync(
    dstArray: ocgpuCUarray,
    dstOffset: usize,
    srcHost: *const c_void,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoAAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcHost, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstArray, dstOffset, srcHost, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoD(
    dstDevice: ocgpuCUdeviceptr,
    srcHost: *const c_void,
    ByteCount: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoD else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcHost, ByteCount) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcHost, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemcpyHtoDAsync(
    dstDevice: ocgpuCUdeviceptr,
    srcHost: *const c_void,
    ByteCount: usize,
    hStream: ocgpuCUstream,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemcpyHtoDAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcHost, ByteCount, hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, srcHost, ByteCount, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD16(
    dstDevice: ocgpuCUdeviceptr,
    us: u16,
    N: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD16 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, us, N) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, us, N);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D16(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    us: u16,
    Width: usize,
    Height: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D16 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, us, Width, Height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, us, Width, Height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D32(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    ui: u32,
    Width: usize,
    Height: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, ui, Width, Height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, ui, Width, Height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD2D8(
    dstDevice: ocgpuCUdeviceptr,
    dstPitch: usize,
    uc: u8,
    Width: usize,
    Height: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD2D8 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, dstPitch, uc, Width, Height) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, dstPitch, uc, Width, Height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD32(
    dstDevice: ocgpuCUdeviceptr,
    ui: u32,
    N: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, ui, N) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, ui, N);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuMemsetD8(
    dstDevice: ocgpuCUdeviceptr,
    uc: u8,
    N: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuMemsetD8 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, uc, N) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dstDevice, uc, N);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuModuleGetGlobal(
    dptr: *mut ocgpuCUdeviceptr,
    bytes: *mut usize,
    hmod: ocgpuCUmodule,
    name: *const c_char,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuModuleGetGlobal else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, hmod, name) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (dptr, bytes, hmod, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamBeginCapture(
    hStream: ocgpuCUstream,
    mode: ocgpuCUstreamCaptureMode,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamBeginCapture else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, mode) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream, mode);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuStreamDestroy(hStream: ocgpuCUstream) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuStreamDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hStream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefGetAddress(
    pdptr: *mut ocgpuCUdeviceptr,
    hTexRef: ocgpuCUtexref,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefGetAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pdptr, hTexRef) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (pdptr, hTexRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetAddress(
    ByteOffset: *mut usize,
    hTexRef: ocgpuCUtexref,
    dptr: ocgpuCUdeviceptr,
    bytes: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ByteOffset, hTexRef, dptr, bytes) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (ByteOffset, hTexRef, dptr, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuCuTexRefSetAddress2D(
    hTexRef: ocgpuCUtexref,
    desc: *const ocgpuCUDA_ARRAY_DESCRIPTOR,
    dptr: ocgpuCUdeviceptr,
    Pitch: usize,
) -> ocgpuCUresult {
    #[cfg(feature = "cuda")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::cuda::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuCuTexRefSetAddress2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hTexRef, desc, dptr, Pitch) }
    }
    #[cfg(not(feature = "cuda"))]
    {
        let _ = (hTexRef, desc, dptr, Pitch);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipInit(flags: u32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipInit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDriverGetVersion(driver_version: *mut i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDriverGetVersion else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(driver_version) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (driver_version,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDeviceCount(count: *mut i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDeviceCount else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (count,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGet(
    device: *mut ocgpuHipDevice_t,
    ordinal: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGet else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, ordinal) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, ordinal);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetName(
    name: *mut c_char,
    length: i32,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(name, length, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (name, length, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetAttribute(
    value: *mut i32,
    attribute: ocgpuHipDeviceAttribute_t,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attribute, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (value, attribute, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxCreate(
    context: *mut ocgpuHipCtx_t,
    flags: u32,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context, flags, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (context, flags, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxDestroy(context: ocgpuHipCtx_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (context,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxSetCurrent(context: ocgpuHipCtx_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxSetCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (context,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxGetCurrent(context: *mut ocgpuHipCtx_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxGetCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(context) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (context,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxSynchronize() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMalloc(
    device_ptr: *mut *mut c_void,
    bytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMalloc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device_ptr, bytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device_ptr, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFree(device_ptr: *mut c_void) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFree else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device_ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device_ptr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyHtoD(
    destination: ocgpuHipDeviceptr_t,
    source: *const c_void,
    bytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyHtoD else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(destination, source, bytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (destination, source, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyDtoH(
    destination: *mut c_void,
    source: ocgpuHipDeviceptr_t,
    bytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyDtoH else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(destination, source, bytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (destination, source, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamCreateWithFlags(
    stream: *mut ocgpuHipStream_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamCreateWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamDestroy(stream: ocgpuHipStream_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamSynchronize(stream: ocgpuHipStream_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventCreateWithFlags(
    event: *mut ocgpuHipEvent_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventCreateWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventDestroy(event: ocgpuHipEvent_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventRecord(
    event: ocgpuHipEvent_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventRecord else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventSynchronize(event: ocgpuHipEvent_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLoadData(
    module: *mut ocgpuHipModule_t,
    image: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleLoadData else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, image) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (module, image);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleUnload(module: ocgpuHipModule_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleUnload else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (module,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleGetFunction(
    function: *mut ocgpuHipFunction_t,
    module: ocgpuHipModule_t,
    name: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleGetFunction else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(function, module, name) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (function, module, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLaunchKernel(
    function: ocgpuHipFunction_t,
    grid_x: u32,
    grid_y: u32,
    grid_z: u32,
    block_x: u32,
    block_y: u32,
    block_z: u32,
    shared_mem_bytes: u32,
    stream: ocgpuHipStream_t,
    kernel_params: *mut *mut c_void,
    extra: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleLaunchKernel else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                function,
                grid_x,
                grid_y,
                grid_z,
                block_x,
                block_y,
                block_z,
                shared_mem_bytes,
                stream,
                kernel_params,
                extra,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            function,
            grid_x,
            grid_y,
            grid_z,
            block_x,
            block_y,
            block_z,
            shared_mem_bytes,
            stream,
            kernel_params,
            extra,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipApiName(id: u32) -> *const c_char {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return core::ptr::null();
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipApiName else {
            return core::ptr::null();
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(id) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (id,);
        core::ptr::null()
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxDisablePeerAccess(peerCtx: ocgpuHipCtx_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxDisablePeerAccess
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(peerCtx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (peerCtx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxEnablePeerAccess(
    peerCtx: ocgpuHipCtx_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxEnablePeerAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(peerCtx, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (peerCtx, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxGetApiVersion(
    ctx: ocgpuHipCtx_t,
    apiVersion: *mut u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxGetApiVersion else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, apiVersion) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, apiVersion);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxGetDevice(device: *mut ocgpuHipDevice_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxGetDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxGetFlags(flags: *mut u32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxPopCurrent(ctx: *mut ocgpuHipCtx_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxPopCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxPushCurrent(ctx: ocgpuHipCtx_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxPushCurrent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceCanAccessPeer(
    canAccessPeer: *mut i32,
    deviceId: i32,
    peerDeviceId: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceCanAccessPeer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(canAccessPeer, deviceId, peerDeviceId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (canAccessPeer, deviceId, peerDeviceId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceComputeCapability(
    major: *mut i32,
    minor: *mut i32,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceComputeCapability
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(major, minor, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (major, minor, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceDisablePeerAccess(peerDeviceId: i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceDisablePeerAccess
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(peerDeviceId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (peerDeviceId,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceEnablePeerAccess(
    peerDeviceId: i32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceEnablePeerAccess
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(peerDeviceId, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (peerDeviceId, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetByPCIBusId(
    device: *mut i32,
    pciBusId: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetByPCIBusId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, pciBusId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, pciBusId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetPCIBusId(
    pciBusId: *mut c_char,
    len: i32,
    device: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetPCIBusId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pciBusId, len, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pciBusId, len, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetStreamPriorityRange(
    leastPriority: *mut i32,
    greatestPriority: *mut i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDeviceGetStreamPriorityRange
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(leastPriority, greatestPriority) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (leastPriority, greatestPriority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGraphMemTrim(device: i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGraphMemTrim else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevicePrimaryCtxGetState(
    dev: ocgpuHipDevice_t,
    flags: *mut u32,
    active: *mut i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevicePrimaryCtxGetState
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, flags, active) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev, flags, active);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevicePrimaryCtxRelease(dev: ocgpuHipDevice_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevicePrimaryCtxRelease
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevicePrimaryCtxReset(dev: ocgpuHipDevice_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevicePrimaryCtxReset
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevicePrimaryCtxRetain(
    pctx: *mut ocgpuHipCtx_t,
    dev: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevicePrimaryCtxRetain
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pctx, dev) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pctx, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevicePrimaryCtxSetFlags(
    dev: ocgpuHipDevice_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevicePrimaryCtxSetFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceReset() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceReset else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceSynchronize() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceSynchronize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceTotalMem(
    bytes: *mut usize,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceTotalMem else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(bytes, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (bytes, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGetErrorName(
    hipError: ocgpuHipError_t,
    errorString: *mut *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvGetErrorName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hipError, errorString) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hipError, errorString);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGetErrorString(
    hipError: ocgpuHipError_t,
    errorString: *mut *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvGetErrorString else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hipError, errorString) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hipError, errorString);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventCreate(event: *mut ocgpuHipEvent_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventElapsedTime(
    ms: *mut f32,
    start: ocgpuHipEvent_t,
    stop: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventElapsedTime else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ms, start, stop) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ms, start, stop);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventQuery(event: ocgpuHipEvent_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventQuery else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventRecordWithFlags(
    event: ocgpuHipEvent_t,
    stream: ocgpuHipStream_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventRecordWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, stream, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event, stream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipEventRecord_spt(
    event: ocgpuHipEvent_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipEventRecord_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtGetLastError() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtGetLastError else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtGetLinkTypeAndHopCount(
    device1: i32,
    device2: i32,
    linktype: *mut u32,
    hopcount: *mut u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipExtGetLinkTypeAndHopCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device1, device2, linktype, hopcount) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device1, device2, linktype, hopcount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtMallocWithFlags(
    ptr: *mut *mut c_void,
    sizeBytes: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtMallocWithFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, sizeBytes, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, sizeBytes, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtStreamCreateWithCUMask(
    stream: *mut ocgpuHipStream_t,
    cuMaskSize: u32,
    cuMask: *const u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipExtStreamCreateWithCUMask
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, cuMaskSize, cuMask) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, cuMaskSize, cuMask);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtStreamGetCUMask(
    stream: ocgpuHipStream_t,
    cuMaskSize: u32,
    cuMask: *mut u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtStreamGetCUMask else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, cuMaskSize, cuMask) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, cuMaskSize, cuMask);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFreeAsync(
    dev_ptr: *mut c_void,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFreeAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFreeHost(ptr: *mut c_void) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFreeHost else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDevice(deviceId: *mut i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(deviceId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (deviceId,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDeviceFlags(flags: *mut u32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDeviceFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetErrorName(hip_error: ocgpuHipError_t) -> *const c_char {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return core::ptr::null();
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetErrorName else {
            return core::ptr::null();
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hip_error) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hip_error,);
        core::ptr::null()
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetErrorString(hipError: ocgpuHipError_t) -> *const c_char {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return core::ptr::null();
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetErrorString else {
            return core::ptr::null();
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hipError) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hipError,);
        core::ptr::null()
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetFuncBySymbol(
    functionPtr: *mut ocgpuHipFunction_t,
    symbolPtr: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetFuncBySymbol else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(functionPtr, symbolPtr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (functionPtr, symbolPtr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetLastError() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetLastError else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetStreamDeviceId(stream: ocgpuHipStream_t) -> i32 {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return 0;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetStreamDeviceId else {
            return 0;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        0
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetSymbolAddress(
    devPtr: *mut *mut c_void,
    symbol: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetSymbolAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, symbol) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr, symbol);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetSymbolSize(
    size: *mut usize,
    symbol: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetSymbolSize else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(size, symbol) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (size, symbol);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostAlloc(
    ptr: *mut *mut c_void,
    size: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostAlloc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostFree(ptr: *mut c_void) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostFree else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostGetDevicePointer(
    devPtr: *mut *mut c_void,
    hstPtr: *mut c_void,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostGetDevicePointer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, hstPtr, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr, hstPtr, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostGetFlags(
    flagsPtr: *mut u32,
    hostPtr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flagsPtr, hostPtr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flagsPtr, hostPtr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostMalloc(
    ptr: *mut *mut c_void,
    size: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostMalloc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostRegister(
    hostPtr: *mut c_void,
    sizeBytes: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostRegister else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hostPtr, sizeBytes, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hostPtr, sizeBytes, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipHostUnregister(hostPtr: *mut c_void) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipHostUnregister else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hostPtr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hostPtr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipIpcCloseMemHandle(devPtr: *mut c_void) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipIpcCloseMemHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelNameRef(f: ocgpuHipFunction_t) -> *const c_char {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return core::ptr::null();
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelNameRef else {
            return core::ptr::null();
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(f) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (f,);
        core::ptr::null()
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelNameRefByPtr(
    hostFunction: *const c_void,
    stream: ocgpuHipStream_t,
) -> *const c_char {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return core::ptr::null();
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelNameRefByPtr else {
            return core::ptr::null();
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hostFunction, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hostFunction, stream);
        core::ptr::null()
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchByPtr(func: *const c_void) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchByPtr else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(func) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (func,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocAsync(
    dev_ptr: *mut *mut c_void,
    size: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, size, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, size, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocHost(ptr: *mut *mut c_void, size: usize) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocHost else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocManaged(
    dev_ptr: *mut *mut c_void,
    size: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocManaged else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, size, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, size, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocPitch(
    ptr: *mut *mut c_void,
    pitch: *mut usize,
    width: usize,
    height: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocPitch else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, pitch, width, height) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, pitch, width, height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemAddressFree(
    devPtr: *mut c_void,
    size: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemAddressFree else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, size) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemAddressReserve(
    ptr: *mut *mut c_void,
    size: usize,
    alignment: usize,
    addr: *mut c_void,
    flags: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemAddressReserve else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, alignment, addr, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size, alignment, addr, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemAllocHost(
    ptr: *mut *mut c_void,
    size: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemAllocHost else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemAllocPitch(
    dptr: *mut ocgpuHipDeviceptr_t,
    pitch: *mut usize,
    widthInBytes: usize,
    height: usize,
    elementSizeBytes: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemAllocPitch else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, pitch, widthInBytes, height, elementSizeBytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dptr, pitch, widthInBytes, height, elementSizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetAddressRange(
    pbase: *mut ocgpuHipDeviceptr_t,
    psize: *mut usize,
    dptr: ocgpuHipDeviceptr_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemGetAddressRange else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pbase, psize, dptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pbase, psize, dptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetInfo(
    free: *mut usize,
    total: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemGetInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(free, total) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (free, total);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPrefetchAsync(
    dev_ptr: *const c_void,
    count: usize,
    device: i32,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPrefetchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, count, device, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, count, device, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPtrGetInfo(
    ptr: *mut c_void,
    size: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPtrGetInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemUnmap(ptr: *mut c_void, size: usize) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemUnmap else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyDtoD(
    dst: ocgpuHipDeviceptr_t,
    src: ocgpuHipDeviceptr_t,
    sizeBytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyDtoD else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyDtoDAsync(
    dst: ocgpuHipDeviceptr_t,
    src: ocgpuHipDeviceptr_t,
    sizeBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyDtoDAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyDtoHAsync(
    dst: *mut c_void,
    src: ocgpuHipDeviceptr_t,
    sizeBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyDtoHAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyHtoDAsync(
    dst: ocgpuHipDeviceptr_t,
    src: *const c_void,
    sizeBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyHtoDAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyPeer(
    dst: *mut c_void,
    dstDeviceId: i32,
    src: *const c_void,
    srcDeviceId: i32,
    sizeBytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyPeer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstDeviceId, src, srcDeviceId, sizeBytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstDeviceId, src, srcDeviceId, sizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyPeerAsync(
    dst: *mut c_void,
    dstDeviceId: i32,
    src: *const c_void,
    srcDevice: i32,
    sizeBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyPeerAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstDeviceId, src, srcDevice, sizeBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstDeviceId, src, srcDevice, sizeBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset(
    dst: *mut c_void,
    value: i32,
    sizeBytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, value, sizeBytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, value, sizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset2D(
    dst: *mut c_void,
    pitch: usize,
    value: i32,
    width: usize,
    height: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, pitch, value, width, height) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, pitch, value, width, height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset2DAsync(
    dst: *mut c_void,
    pitch: usize,
    value: i32,
    width: usize,
    height: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset2DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, pitch, value, width, height, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, pitch, value, width, height, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset2DAsync_spt(
    dst: *mut c_void,
    pitch: usize,
    value: i32,
    width: usize,
    height: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset2DAsync_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, pitch, value, width, height, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, pitch, value, width, height, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset2D_spt(
    dst: *mut c_void,
    pitch: usize,
    value: i32,
    width: usize,
    height: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset2D_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, pitch, value, width, height) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, pitch, value, width, height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetAsync(
    dst: *mut c_void,
    value: i32,
    sizeBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, value, sizeBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, value, sizeBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetAsync_spt(
    dst: *mut c_void,
    value: i32,
    sizeBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetAsync_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, value, sizeBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, value, sizeBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD16(
    dest: ocgpuHipDeviceptr_t,
    value: u16,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD16 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dest, value, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dest, value, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD16Async(
    dest: ocgpuHipDeviceptr_t,
    value: u16,
    count: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD16Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dest, value, count, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dest, value, count, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD2D16(
    dst: ocgpuHipDeviceptr_t,
    dstPitch: usize,
    value: u16,
    width: usize,
    height: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD2D16 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstPitch, value, width, height) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstPitch, value, width, height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD2D16Async(
    dst: ocgpuHipDeviceptr_t,
    dstPitch: usize,
    value: u16,
    width: usize,
    height: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD2D16Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstPitch, value, width, height, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstPitch, value, width, height, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD2D32(
    dst: ocgpuHipDeviceptr_t,
    dstPitch: usize,
    value: u32,
    width: usize,
    height: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD2D32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstPitch, value, width, height) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstPitch, value, width, height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD2D32Async(
    dst: ocgpuHipDeviceptr_t,
    dstPitch: usize,
    value: u32,
    width: usize,
    height: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD2D32Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstPitch, value, width, height, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstPitch, value, width, height, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD2D8(
    dst: ocgpuHipDeviceptr_t,
    dstPitch: usize,
    value: u8,
    width: usize,
    height: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD2D8 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstPitch, value, width, height) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstPitch, value, width, height);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD2D8Async(
    dst: ocgpuHipDeviceptr_t,
    dstPitch: usize,
    value: u8,
    width: usize,
    height: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD2D8Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dstPitch, value, width, height, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dstPitch, value, width, height, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD32(
    dest: ocgpuHipDeviceptr_t,
    value: i32,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dest, value, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dest, value, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD32Async(
    dst: ocgpuHipDeviceptr_t,
    value: i32,
    count: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD32Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, value, count, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, value, count, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD8(
    dest: ocgpuHipDeviceptr_t,
    value: u8,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD8 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dest, value, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dest, value, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemsetD8Async(
    dest: ocgpuHipDeviceptr_t,
    value: u8,
    count: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemsetD8Async else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dest, value, count, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dest, value, count, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset_spt(
    dst: *mut c_void,
    value: i32,
    sizeBytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, value, sizeBytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, value, sizeBytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleGetFunctionCount(
    count: *mut u32,
    mod_: ocgpuHipModule_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleGetFunctionCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, mod_) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (count, mod_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleGetGlobal(
    dptr: *mut ocgpuHipDeviceptr_t,
    bytes: *mut usize,
    hmod: ocgpuHipModule_t,
    name: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleGetGlobal else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, hmod, name) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dptr, bytes, hmod, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLaunchCooperativeKernel(
    f: ocgpuHipFunction_t,
    gridDimX: u32,
    gridDimY: u32,
    gridDimZ: u32,
    blockDimX: u32,
    blockDimY: u32,
    blockDimZ: u32,
    sharedMemBytes: u32,
    stream: ocgpuHipStream_t,
    kernelParams: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipModuleLaunchCooperativeKernel
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                f,
                gridDimX,
                gridDimY,
                gridDimZ,
                blockDimX,
                blockDimY,
                blockDimZ,
                sharedMemBytes,
                stream,
                kernelParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            f,
            gridDimX,
            gridDimY,
            gridDimZ,
            blockDimX,
            blockDimY,
            blockDimZ,
            sharedMemBytes,
            stream,
            kernelParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLoad(
    module: *mut ocgpuHipModule_t,
    fname: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleLoad else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, fname) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (module, fname);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLoadFatBinary(
    module: *mut ocgpuHipModule_t,
    fatbin: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleLoadFatBinary else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, fatbin) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (module, fatbin);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleOccupancyMaxActiveBlocksPerMultiprocessor(
    numBlocks: *mut i32,
    f: ocgpuHipFunction_t,
    blockSize: i32,
    dynSharedMemPerBlk: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipModuleOccupancyMaxActiveBlocksPerMultiprocessor
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numBlocks, f, blockSize, dynSharedMemPerBlk) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numBlocks, f, blockSize, dynSharedMemPerBlk);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleOccupancyMaxActiveBlocksPerMultiprocessorWithFlags(
    numBlocks: *mut i32,
    f: ocgpuHipFunction_t,
    blockSize: i32,
    dynSharedMemPerBlk: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipModuleOccupancyMaxActiveBlocksPerMultiprocessorWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numBlocks, f, blockSize, dynSharedMemPerBlk, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numBlocks, f, blockSize, dynSharedMemPerBlk, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleOccupancyMaxPotentialBlockSize(
    gridSize: *mut i32,
    blockSize: *mut i32,
    f: ocgpuHipFunction_t,
    dynSharedMemPerBlk: usize,
    blockSizeLimit: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipModuleOccupancyMaxPotentialBlockSize
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(gridSize, blockSize, f, dynSharedMemPerBlk, blockSizeLimit) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (gridSize, blockSize, f, dynSharedMemPerBlk, blockSizeLimit);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleOccupancyMaxPotentialBlockSizeWithFlags(
    gridSize: *mut i32,
    blockSize: *mut i32,
    f: ocgpuHipFunction_t,
    dynSharedMemPerBlk: usize,
    blockSizeLimit: i32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipModuleOccupancyMaxPotentialBlockSizeWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                gridSize,
                blockSize,
                f,
                dynSharedMemPerBlk,
                blockSizeLimit,
                flags,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            gridSize,
            blockSize,
            f,
            dynSharedMemPerBlk,
            blockSizeLimit,
            flags,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipOccupancyAvailableDynamicSMemPerBlock(
    dynamicSmemSize: *mut usize,
    f: *const c_void,
    numBlocks: i32,
    blockSize: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipOccupancyAvailableDynamicSMemPerBlock
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dynamicSmemSize, f, numBlocks, blockSize) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dynamicSmemSize, f, numBlocks, blockSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipOccupancyMaxActiveBlocksPerMultiprocessor(
    numBlocks: *mut i32,
    f: *const c_void,
    blockSize: i32,
    dynSharedMemPerBlk: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipOccupancyMaxActiveBlocksPerMultiprocessor
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numBlocks, f, blockSize, dynSharedMemPerBlk) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numBlocks, f, blockSize, dynSharedMemPerBlk);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipOccupancyMaxActiveBlocksPerMultiprocessorWithFlags(
    numBlocks: *mut i32,
    f: *const c_void,
    blockSize: i32,
    dynSharedMemPerBlk: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipOccupancyMaxActiveBlocksPerMultiprocessorWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numBlocks, f, blockSize, dynSharedMemPerBlk, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numBlocks, f, blockSize, dynSharedMemPerBlk, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipOccupancyMaxPotentialBlockSize(
    gridSize: *mut i32,
    blockSize: *mut i32,
    f: *const c_void,
    dynSharedMemPerBlk: usize,
    blockSizeLimit: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipOccupancyMaxPotentialBlockSize
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(gridSize, blockSize, f, dynSharedMemPerBlk, blockSizeLimit) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (gridSize, blockSize, f, dynSharedMemPerBlk, blockSizeLimit);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipPeekAtLastError() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipPeekAtLastError else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipProfilerStart() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipProfilerStart else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipProfilerStop() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipProfilerStop else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipRuntimeGetVersion(runtimeVersion: *mut i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipRuntimeGetVersion else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(runtimeVersion) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (runtimeVersion,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipSetDevice(deviceId: i32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipSetDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(deviceId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (deviceId,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipSetDeviceFlags(flags: u32) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipSetDeviceFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flags,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipSetValidDevices(
    device_arr: *mut i32,
    len: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipSetValidDevices else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device_arr, len) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device_arr, len);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipSetupArgument(
    arg: *const c_void,
    size: usize,
    offset: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipSetupArgument else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(arg, size, offset) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (arg, size, offset);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamAttachMemAsync(
    stream: ocgpuHipStream_t,
    dev_ptr: *mut c_void,
    length: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamAttachMemAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, dev_ptr, length, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, dev_ptr, length, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamCopyAttributes(
    dst: ocgpuHipStream_t,
    src: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamCopyAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamCreate(stream: *mut ocgpuHipStream_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamCreateWithPriority(
    stream: *mut ocgpuHipStream_t,
    flags: u32,
    priority: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamCreateWithPriority
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, flags, priority) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, flags, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetDevice(
    stream: ocgpuHipStream_t,
    device: *mut ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetFlags(
    stream: ocgpuHipStream_t,
    flags: *mut u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetFlags_spt(
    stream: ocgpuHipStream_t,
    flags: *mut u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetFlags_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetId(
    stream: ocgpuHipStream_t,
    streamId: *mut u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, streamId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, streamId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetPriority(
    stream: ocgpuHipStream_t,
    priority: *mut i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetPriority else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, priority) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetPriority_spt(
    stream: ocgpuHipStream_t,
    priority: *mut i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetPriority_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, priority) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamQuery(stream: ocgpuHipStream_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamQuery else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamQuery_spt(stream: ocgpuHipStream_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamQuery_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamSynchronize_spt(
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamSynchronize_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamWaitEvent(
    stream: ocgpuHipStream_t,
    event: ocgpuHipEvent_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamWaitEvent else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, event, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, event, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamWaitEvent_spt(
    stream: ocgpuHipStream_t,
    event: ocgpuHipEvent_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamWaitEvent_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, event, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, event, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamWaitValue32(
    stream: ocgpuHipStream_t,
    ptr: *mut c_void,
    value: u32,
    flags: u32,
    mask: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamWaitValue32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, ptr, value, flags, mask) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, ptr, value, flags, mask);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamWaitValue64(
    stream: ocgpuHipStream_t,
    ptr: *mut c_void,
    value: u64,
    flags: u32,
    mask: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamWaitValue64 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, ptr, value, flags, mask) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, ptr, value, flags, mask);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamWriteValue32(
    stream: ocgpuHipStream_t,
    ptr: *mut c_void,
    value: u32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamWriteValue32 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, ptr, value, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, ptr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamWriteValue64(
    stream: ocgpuHipStream_t,
    ptr: *mut c_void,
    value: u64,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamWriteValue64 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, ptr, value, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, ptr, value, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipArray3DCreate(
    array: *mut ocgpuHipArray_t,
    pAllocateArray: *const ocgpuHIP_ARRAY3D_DESCRIPTOR,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipArray3DCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(array, pAllocateArray) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (array, pAllocateArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipArray3DGetDescriptor(
    pArrayDescriptor: *mut ocgpuHIP_ARRAY3D_DESCRIPTOR,
    array: ocgpuHipArray_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipArray3DGetDescriptor
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArrayDescriptor, array) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pArrayDescriptor, array);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipArrayCreate(
    pHandle: *mut ocgpuHipArray_t,
    pAllocateArray: *const ocgpuHIP_ARRAY_DESCRIPTOR,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipArrayCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pAllocateArray) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pHandle, pAllocateArray);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipArrayDestroy(array: ocgpuHipArray_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipArrayDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(array) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (array,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipArrayGetDescriptor(
    pArrayDescriptor: *mut ocgpuHIP_ARRAY_DESCRIPTOR,
    array: ocgpuHipArray_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipArrayGetDescriptor else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArrayDescriptor, array) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pArrayDescriptor, array);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipArrayGetInfo(
    desc: *mut ocgpuHipChannelFormatDesc,
    extent: *mut ocgpuHipExtent,
    flags: *mut u32,
    array: ocgpuHipArray_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipArrayGetInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(desc, extent, flags, array) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (desc, extent, flags, array);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipBindTexture(
    offset: *mut usize,
    tex: *const ocgputextureReference,
    devPtr: *const c_void,
    desc: *const ocgpuHipChannelFormatDesc,
    size: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipBindTexture else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(offset, tex, devPtr, desc, size) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (offset, tex, devPtr, desc, size);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipBindTexture2D(
    offset: *mut usize,
    tex: *const ocgputextureReference,
    devPtr: *const c_void,
    desc: *const ocgpuHipChannelFormatDesc,
    width: usize,
    height: usize,
    pitch: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipBindTexture2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(offset, tex, devPtr, desc, width, height, pitch) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (offset, tex, devPtr, desc, width, height, pitch);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipBindTextureToArray(
    tex: *const ocgputextureReference,
    array: ocgpuHipArray_const_t,
    desc: *const ocgpuHipChannelFormatDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipBindTextureToArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(tex, array, desc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (tex, array, desc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipBindTextureToMipmappedArray(
    tex: *const ocgputextureReference,
    mipmappedArray: ocgpuHipMipmappedArray_const_t,
    desc: *const ocgpuHipChannelFormatDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipBindTextureToMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(tex, mipmappedArray, desc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (tex, mipmappedArray, desc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipChooseDeviceR0600(
    device: *mut i32,
    prop: *const ocgpuHipDeviceProp_tR0600,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipChooseDeviceR0600 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, prop) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, prop);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCreateSurfaceObject(
    pSurfObject: *mut ocgpuHipSurfaceObject_t,
    pResDesc: *const ocgpuHipResourceDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCreateSurfaceObject else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pSurfObject, pResDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pSurfObject, pResDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCreateTextureObject(
    pTexObject: *mut ocgpuHipTextureObject_t,
    pResDesc: *const ocgpuHipResourceDesc,
    pTexDesc: *const ocgpuHipTextureDesc,
    pResViewDesc: *const ocgpuHipResourceViewDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCreateTextureObject else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexObject, pResDesc, pTexDesc, pResViewDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pTexObject, pResDesc, pTexDesc, pResViewDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxGetCacheConfig(
    cacheConfig: *mut ocgpuHipFuncCache_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxGetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(cacheConfig) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (cacheConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxGetSharedMemConfig(
    pConfig: *mut ocgpuHipSharedMemConfig,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxGetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pConfig) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxSetCacheConfig(
    cacheConfig: ocgpuHipFuncCache_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxSetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(cacheConfig) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (cacheConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipCtxSetSharedMemConfig(
    config: ocgpuHipSharedMemConfig,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCtxSetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (config,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDestroyExternalMemory(
    extMem: ocgpuHipExternalMemory_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDestroyExternalMemory
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extMem) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (extMem,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDestroyExternalSemaphore(
    extSem: ocgpuHipExternalSemaphore_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDestroyExternalSemaphore
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSem) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (extSem,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDestroySurfaceObject(
    surfaceObject: ocgpuHipSurfaceObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDestroySurfaceObject
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(surfaceObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (surfaceObject,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDestroyTextureObject(
    textureObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDestroyTextureObject
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(textureObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (textureObject,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetCacheConfig(
    cacheConfig: *mut ocgpuHipFuncCache_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetCacheConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(cacheConfig) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (cacheConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetDefaultMemPool(
    mem_pool: *mut ocgpuHipMemPool_t,
    device: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetDefaultMemPool
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetGraphMemAttribute(
    device: i32,
    attr: ocgpuHipGraphMemAttributeType,
    value: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDeviceGetGraphMemAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetLimit(
    pValue: *mut usize,
    limit: ocgpuHipLimit_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetLimit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pValue, limit) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pValue, limit);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetMemPool(
    mem_pool: *mut ocgpuHipMemPool_t,
    device: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetP2PAttribute(
    value: *mut i32,
    attr: ocgpuHipDeviceP2PAttr,
    srcDevice: i32,
    dstDevice: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetP2PAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attr, srcDevice, dstDevice) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (value, attr, srcDevice, dstDevice);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetSharedMemConfig(
    pConfig: *mut ocgpuHipSharedMemConfig,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pConfig) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetTexture1DLinearMaxWidth(
    max_width: *mut usize,
    desc: *const ocgpuHipChannelFormatDesc,
    device: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDeviceGetTexture1DLinearMaxWidth
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(max_width, desc, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (max_width, desc, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetUuid(
    uuid: *mut ocgpuHipUUID,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetUuid else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(uuid, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (uuid, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceSetCacheConfig(
    cacheConfig: ocgpuHipFuncCache_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceSetCacheConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(cacheConfig) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (cacheConfig,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceSetGraphMemAttribute(
    device: i32,
    attr: ocgpuHipGraphMemAttributeType,
    value: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDeviceSetGraphMemAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceSetLimit(
    limit: ocgpuHipLimit_t,
    value: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceSetLimit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(limit, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (limit, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceSetMemPool(
    device: i32,
    mem_pool: ocgpuHipMemPool_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceSetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, mem_pool) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, mem_pool);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceSetSharedMemConfig(
    config: ocgpuHipSharedMemConfig,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceSetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (config,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphAddMemFreeNode(
    phGraphNode: *mut ocgpuHipGraphNode_t,
    hGraph: ocgpuHipGraph_t,
    dependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    dptr: ocgpuHipDeviceptr_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvGraphAddMemFreeNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phGraphNode, hGraph, dependencies, numDependencies, dptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (phGraphNode, hGraph, dependencies, numDependencies, dptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphAddMemcpyNode(
    phGraphNode: *mut ocgpuHipGraphNode_t,
    hGraph: ocgpuHipGraph_t,
    dependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    copyParams: *const ocgpuHIP_MEMCPY3D,
    ctx: ocgpuHipCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvGraphAddMemcpyNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                copyParams,
                ctx,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            copyParams,
            ctx,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphAddMemsetNode(
    phGraphNode: *mut ocgpuHipGraphNode_t,
    hGraph: ocgpuHipGraph_t,
    dependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    memsetParams: *const ocgpuHipMemsetParams,
    ctx: ocgpuHipCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvGraphAddMemsetNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                memsetParams,
                ctx,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            memsetParams,
            ctx,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphExecMemcpyNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    copyParams: *const ocgpuHIP_MEMCPY3D,
    ctx: ocgpuHipCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDrvGraphExecMemcpyNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, copyParams, ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, copyParams, ctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphExecMemsetNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    memsetParams: *const ocgpuHipMemsetParams,
    ctx: ocgpuHipCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDrvGraphExecMemsetNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, memsetParams, ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, memsetParams, ctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphMemcpyNodeGetParams(
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *mut ocgpuHIP_MEMCPY3D,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDrvGraphMemcpyNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvGraphMemcpyNodeSetParams(
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *const ocgpuHIP_MEMCPY3D,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDrvGraphMemcpyNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvLaunchKernelEx(
    config: *const ocgpuHIP_LAUNCH_CONFIG,
    f: ocgpuHipFunction_t,
    params: *mut *mut c_void,
    extra: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvLaunchKernelEx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config, f, params, extra) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (config, f, params, extra);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvMemcpy2DUnaligned(
    pCopy: *const ocgpuHip_Memcpy2D,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvMemcpy2DUnaligned
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvMemcpy3D(pCopy: *const ocgpuHIP_MEMCPY3D) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvMemcpy3D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvMemcpy3DAsync(
    pCopy: *const ocgpuHIP_MEMCPY3D,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvMemcpy3DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pCopy, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvPointerGetAttributes(
    numAttributes: u32,
    attributes: *mut ocgpuHipPointer_attribute,
    data: *mut *mut c_void,
    ptr: ocgpuHipDeviceptr_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvPointerGetAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numAttributes, attributes, data, ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numAttributes, attributes, data, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtLaunchMultiKernelMultiDevice(
    launchParamsList: *mut ocgpuHipLaunchParams,
    numDevices: i32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipExtLaunchMultiKernelMultiDevice
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(launchParamsList, numDevices, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (launchParamsList, numDevices, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExternalMemoryGetMappedBuffer(
    devPtr: *mut *mut c_void,
    extMem: ocgpuHipExternalMemory_t,
    bufferDesc: *const ocgpuHipExternalMemoryBufferDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipExternalMemoryGetMappedBuffer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, extMem, bufferDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr, extMem, bufferDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExternalMemoryGetMappedMipmappedArray(
    mipmap: *mut ocgpuHipMipmappedArray_t,
    extMem: ocgpuHipExternalMemory_t,
    mipmapDesc: *const ocgpuHipExternalMemoryMipmappedArrayDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipExternalMemoryGetMappedMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mipmap, extMem, mipmapDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mipmap, extMem, mipmapDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFreeArray(array: ocgpuHipArray_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFreeArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(array) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (array,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFreeMipmappedArray(
    mipmappedArray: ocgpuHipMipmappedArray_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFreeMipmappedArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mipmappedArray) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mipmappedArray,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFuncGetAttribute(
    value: *mut i32,
    attrib: ocgpuHipFunction_attribute,
    hfunc: ocgpuHipFunction_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFuncGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attrib, hfunc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (value, attrib, hfunc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFuncGetAttributes(
    attr: *mut ocgpuHipFuncAttributes,
    func: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFuncGetAttributes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attr, func) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (attr, func);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFuncSetAttribute(
    func: *const c_void,
    attr: ocgpuHipFuncAttribute,
    value: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFuncSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(func, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (func, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFuncSetCacheConfig(
    func: *const c_void,
    config: ocgpuHipFuncCache_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFuncSetCacheConfig else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(func, config) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (func, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipFuncSetSharedMemConfig(
    func: *const c_void,
    config: ocgpuHipSharedMemConfig,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipFuncSetSharedMemConfig
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(func, config) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (func, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetChannelDesc(
    desc: *mut ocgpuHipChannelFormatDesc,
    array: ocgpuHipArray_const_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetChannelDesc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(desc, array) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (desc, array);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDevicePropertiesR0600(
    prop: *mut ocgpuHipDeviceProp_tR0600,
    deviceId: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDevicePropertiesR0600
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(prop, deviceId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (prop, deviceId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDriverEntryPoint(
    symbol: *const c_char,
    funcPtr: *mut *mut c_void,
    flags: u64,
    driverStatus: *mut ocgpuHipDriverEntryPointQueryResult,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDriverEntryPoint else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, funcPtr, flags, driverStatus) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, funcPtr, flags, driverStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDriverEntryPoint_spt(
    symbol: *const c_char,
    funcPtr: *mut *mut c_void,
    flags: u64,
    status: *mut ocgpuHipDriverEntryPointQueryResult,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDriverEntryPoint_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, funcPtr, flags, status) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, funcPtr, flags, status);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetMipmappedArrayLevel(
    levelArray: *mut ocgpuHipArray_t,
    mipmappedArray: ocgpuHipMipmappedArray_const_t,
    level: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetMipmappedArrayLevel
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(levelArray, mipmappedArray, level) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (levelArray, mipmappedArray, level);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetProcAddress(
    symbol: *const c_char,
    pfn: *mut *mut c_void,
    hipVersion: i32,
    flags: u64,
    symbolStatus: *mut ocgpuHipDriverProcAddressQueryResult,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetProcAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, pfn, hipVersion, flags, symbolStatus) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, pfn, hipVersion, flags, symbolStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetTextureAlignmentOffset(
    offset: *mut usize,
    texref: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGetTextureAlignmentOffset
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(offset, texref) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (offset, texref);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetTextureObjectResourceDesc(
    pResDesc: *mut ocgpuHipResourceDesc,
    textureObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGetTextureObjectResourceDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResDesc, textureObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pResDesc, textureObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetTextureObjectResourceViewDesc(
    pResViewDesc: *mut ocgpuHipResourceViewDesc,
    textureObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGetTextureObjectResourceViewDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResViewDesc, textureObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pResViewDesc, textureObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetTextureObjectTextureDesc(
    pTexDesc: *mut ocgpuHipTextureDesc,
    textureObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGetTextureObjectTextureDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexDesc, textureObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pTexDesc, textureObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetTextureReference(
    texref: *mut *const ocgputextureReference,
    symbol: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetTextureReference else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texref, symbol) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texref, symbol);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddBatchMemOpNode(
    phGraphNode: *mut ocgpuHipGraphNode_t,
    hGraph: ocgpuHipGraph_t,
    dependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    nodeParams: *const ocgpuHipBatchMemOpNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddBatchMemOpNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                phGraphNode,
                hGraph,
                dependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            phGraphNode,
            hGraph,
            dependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddChildGraphNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    childGraph: ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddChildGraphNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                childGraph,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            childGraph,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddDependencies(
    graph: ocgpuHipGraph_t,
    from: *const ocgpuHipGraphNode_t,
    to: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, from, to, numDependencies) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, from, to, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddEmptyNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddEmptyNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphNode, graph, pDependencies, numDependencies) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphNode, graph, pDependencies, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddEventRecordNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddEventRecordNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphNode, graph, pDependencies, numDependencies, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphNode, graph, pDependencies, numDependencies, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddEventWaitNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddEventWaitNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphNode, graph, pDependencies, numDependencies, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphNode, graph, pDependencies, numDependencies, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddExternalSemaphoresSignalNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    nodeParams: *const ocgpuHipExternalSemaphoreSignalNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphAddExternalSemaphoresSignalNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddExternalSemaphoresWaitNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    nodeParams: *const ocgpuHipExternalSemaphoreWaitNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphAddExternalSemaphoresWaitNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddHostNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    pNodeParams: *const ocgpuHipHostNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddHostNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                pNodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            pNodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddKernelNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    pNodeParams: *const ocgpuHipKernelNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddKernelNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                pNodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            pNodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemAllocNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    pNodeParams: *mut ocgpuHipMemAllocNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddMemAllocNode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                pNodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            pNodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemFreeNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    dev_ptr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddMemFreeNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphNode, graph, pDependencies, numDependencies, dev_ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphNode, graph, pDependencies, numDependencies, dev_ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemcpyNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    pCopyParams: *const ocgpuHipMemcpy3DParms,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddMemcpyNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                pCopyParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            pCopyParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemcpyNode1D(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    dst: *mut c_void,
    src: *const c_void,
    count: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddMemcpyNode1D
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                dst,
                src,
                count,
                kind,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            dst,
            src,
            count,
            kind,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemcpyNodeFromSymbol(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    dst: *mut c_void,
    symbol: *const c_void,
    count: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphAddMemcpyNodeFromSymbol
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                dst,
                symbol,
                count,
                offset,
                kind,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            dst,
            symbol,
            count,
            offset,
            kind,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemcpyNodeToSymbol(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    symbol: *const c_void,
    src: *const c_void,
    count: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphAddMemcpyNodeToSymbol
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                symbol,
                src,
                count,
                offset,
                kind,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            symbol,
            src,
            count,
            offset,
            kind,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddMemsetNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    pMemsetParams: *const ocgpuHipMemsetParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddMemsetNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                pMemsetParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            pMemsetParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphAddNode(
    pGraphNode: *mut ocgpuHipGraphNode_t,
    graph: ocgpuHipGraph_t,
    pDependencies: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
    nodeParams: *mut ocgpuHipGraphNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphAddNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                pGraphNode,
                graph,
                pDependencies,
                numDependencies,
                nodeParams,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            pGraphNode,
            graph,
            pDependencies,
            numDependencies,
            nodeParams,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphBatchMemOpNodeGetParams(
    hNode: ocgpuHipGraphNode_t,
    nodeParams_out: *mut ocgpuHipBatchMemOpNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphBatchMemOpNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, nodeParams_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphBatchMemOpNodeSetParams(
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *mut ocgpuHipBatchMemOpNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphBatchMemOpNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphChildGraphNodeGetGraph(
    node: ocgpuHipGraphNode_t,
    pGraph: *mut ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphChildGraphNodeGetGraph
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pGraph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphClone(
    pGraphClone: *mut ocgpuHipGraph_t,
    originalGraph: ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphClone else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphClone, originalGraph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphClone, originalGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphCreate(
    pGraph: *mut ocgpuHipGraph_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraph, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraph, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphDebugDotPrint(
    graph: ocgpuHipGraph_t,
    path: *const c_char,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphDebugDotPrint else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, path, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, path, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphDestroy(graph: ocgpuHipGraph_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphDestroyNode(node: ocgpuHipGraphNode_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphDestroyNode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphEventRecordNodeGetEvent(
    node: ocgpuHipGraphNode_t,
    event_out: *mut ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphEventRecordNodeGetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, event_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, event_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphEventRecordNodeSetEvent(
    node: ocgpuHipGraphNode_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphEventRecordNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphEventWaitNodeGetEvent(
    node: ocgpuHipGraphNode_t,
    event_out: *mut ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphEventWaitNodeGetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, event_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, event_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphEventWaitNodeSetEvent(
    node: ocgpuHipGraphNode_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphEventWaitNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecBatchMemOpNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *const ocgpuHipBatchMemOpNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecBatchMemOpNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecChildGraphNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    childGraph: ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecChildGraphNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, childGraph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, childGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecDestroy(
    graphExec: ocgpuHipGraphExec_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphExecDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graphExec) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graphExec,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecEventRecordNodeSetEvent(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecEventRecordNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecEventWaitNodeSetEvent(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecEventWaitNodeSetEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecExternalSemaphoresSignalNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *const ocgpuHipExternalSemaphoreSignalNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecExternalSemaphoresSignalNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecExternalSemaphoresWaitNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *const ocgpuHipExternalSemaphoreWaitNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecExternalSemaphoresWaitNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecGetFlags(
    graphExec: ocgpuHipGraphExec_t,
    flags: *mut u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphExecGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graphExec, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graphExec, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecHostNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipHostNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecHostNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecKernelNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipKernelNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecKernelNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecMemcpyNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    pNodeParams: *mut ocgpuHipMemcpy3DParms,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecMemcpyNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecMemcpyNodeSetParams1D(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    dst: *mut c_void,
    src: *const c_void,
    count: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecMemcpyNodeSetParams1D
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, dst, src, count, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, dst, src, count, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecMemcpyNodeSetParamsFromSymbol(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    dst: *mut c_void,
    symbol: *const c_void,
    count: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecMemcpyNodeSetParamsFromSymbol
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, dst, symbol, count, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, dst, symbol, count, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecMemcpyNodeSetParamsToSymbol(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    symbol: *const c_void,
    src: *const c_void,
    count: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecMemcpyNodeSetParamsToSymbol
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, symbol, src, count, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, symbol, src, count, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecMemsetNodeSetParams(
    hGraphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipMemsetParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExecMemsetNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecNodeSetParams(
    graphExec: ocgpuHipGraphExec_t,
    node: ocgpuHipGraphNode_t,
    nodeParams: *mut ocgpuHipGraphNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphExecNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graphExec, node, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graphExec, node, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExecUpdate(
    hGraphExec: ocgpuHipGraphExec_t,
    hGraph: ocgpuHipGraph_t,
    hErrorNode_out: *mut ocgpuHipGraphNode_t,
    updateResult_out: *mut ocgpuHipGraphExecUpdateResult,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphExecUpdate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hGraph, hErrorNode_out, updateResult_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hGraph, hErrorNode_out, updateResult_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExternalSemaphoresSignalNodeGetParams(
    hNode: ocgpuHipGraphNode_t,
    params_out: *mut ocgpuHipExternalSemaphoreSignalNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExternalSemaphoresSignalNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, params_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, params_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExternalSemaphoresSignalNodeSetParams(
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *const ocgpuHipExternalSemaphoreSignalNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExternalSemaphoresSignalNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExternalSemaphoresWaitNodeGetParams(
    hNode: ocgpuHipGraphNode_t,
    params_out: *mut ocgpuHipExternalSemaphoreWaitNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExternalSemaphoresWaitNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, params_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, params_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphExternalSemaphoresWaitNodeSetParams(
    hNode: ocgpuHipGraphNode_t,
    nodeParams: *const ocgpuHipExternalSemaphoreWaitNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphExternalSemaphoresWaitNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphGetEdges(
    graph: ocgpuHipGraph_t,
    from: *mut ocgpuHipGraphNode_t,
    to: *mut ocgpuHipGraphNode_t,
    numEdges: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphGetEdges else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, from, to, numEdges) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, from, to, numEdges);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphGetNodes(
    graph: ocgpuHipGraph_t,
    nodes: *mut ocgpuHipGraphNode_t,
    numNodes: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphGetNodes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, nodes, numNodes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, nodes, numNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphGetRootNodes(
    graph: ocgpuHipGraph_t,
    pRootNodes: *mut ocgpuHipGraphNode_t,
    pNumRootNodes: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphGetRootNodes else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, pRootNodes, pNumRootNodes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, pRootNodes, pNumRootNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphHostNodeGetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *mut ocgpuHipHostNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphHostNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphHostNodeSetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipHostNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphHostNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphInstantiate(
    pGraphExec: *mut ocgpuHipGraphExec_t,
    graph: ocgpuHipGraph_t,
    pErrorNode: *mut ocgpuHipGraphNode_t,
    pLogBuffer: *mut c_char,
    bufferSize: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphInstantiate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphExec, graph, pErrorNode, pLogBuffer, bufferSize) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphExec, graph, pErrorNode, pLogBuffer, bufferSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphInstantiateWithFlags(
    pGraphExec: *mut ocgpuHipGraphExec_t,
    graph: ocgpuHipGraph_t,
    flags: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphInstantiateWithFlags
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphExec, graph, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphExec, graph, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphInstantiateWithParams(
    pGraphExec: *mut ocgpuHipGraphExec_t,
    graph: ocgpuHipGraph_t,
    instantiateParams: *mut ocgpuHipGraphInstantiateParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphInstantiateWithParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pGraphExec, graph, instantiateParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pGraphExec, graph, instantiateParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphKernelNodeCopyAttributes(
    hSrc: ocgpuHipGraphNode_t,
    hDst: ocgpuHipGraphNode_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphKernelNodeCopyAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hSrc, hDst) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hSrc, hDst);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphKernelNodeGetAttribute(
    hNode: ocgpuHipGraphNode_t,
    attr: ocgpuHipLaunchAttributeID,
    value: *mut ocgpuHipLaunchAttributeValue,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphKernelNodeGetAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphKernelNodeGetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *mut ocgpuHipKernelNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphKernelNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphKernelNodeSetAttribute(
    hNode: ocgpuHipGraphNode_t,
    attr: ocgpuHipLaunchAttributeID,
    value: *const ocgpuHipLaunchAttributeValue,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphKernelNodeSetAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hNode, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hNode, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphKernelNodeSetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipKernelNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphKernelNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphLaunch(
    graphExec: ocgpuHipGraphExec_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphLaunch else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graphExec, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graphExec, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphLaunch_spt(
    graphExec: ocgpuHipGraphExec_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphLaunch_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graphExec, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graphExec, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemAllocNodeGetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *mut ocgpuHipMemAllocNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphMemAllocNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemFreeNodeGetParams(
    node: ocgpuHipGraphNode_t,
    dev_ptr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphMemFreeNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, dev_ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, dev_ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemcpyNodeGetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *mut ocgpuHipMemcpy3DParms,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphMemcpyNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemcpyNodeSetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipMemcpy3DParms,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphMemcpyNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemcpyNodeSetParams1D(
    node: ocgpuHipGraphNode_t,
    dst: *mut c_void,
    src: *const c_void,
    count: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphMemcpyNodeSetParams1D
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, dst, src, count, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, dst, src, count, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemcpyNodeSetParamsFromSymbol(
    node: ocgpuHipGraphNode_t,
    dst: *mut c_void,
    symbol: *const c_void,
    count: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphMemcpyNodeSetParamsFromSymbol
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, dst, symbol, count, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, dst, symbol, count, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemcpyNodeSetParamsToSymbol(
    node: ocgpuHipGraphNode_t,
    symbol: *const c_void,
    src: *const c_void,
    count: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphMemcpyNodeSetParamsToSymbol
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, symbol, src, count, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, symbol, src, count, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemsetNodeGetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *mut ocgpuHipMemsetParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphMemsetNodeGetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphMemsetNodeSetParams(
    node: ocgpuHipGraphNode_t,
    pNodeParams: *const ocgpuHipMemsetParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphMemsetNodeSetParams
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pNodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pNodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeFindInClone(
    pNode: *mut ocgpuHipGraphNode_t,
    originalNode: ocgpuHipGraphNode_t,
    clonedGraph: ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphNodeFindInClone
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pNode, originalNode, clonedGraph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pNode, originalNode, clonedGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeGetDependencies(
    node: ocgpuHipGraphNode_t,
    pDependencies: *mut ocgpuHipGraphNode_t,
    pNumDependencies: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphNodeGetDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pDependencies, pNumDependencies) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pDependencies, pNumDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeGetDependentNodes(
    node: ocgpuHipGraphNode_t,
    pDependentNodes: *mut ocgpuHipGraphNode_t,
    pNumDependentNodes: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphNodeGetDependentNodes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pDependentNodes, pNumDependentNodes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pDependentNodes, pNumDependentNodes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeGetEnabled(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    isEnabled: *mut u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphNodeGetEnabled else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, isEnabled) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, isEnabled);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeGetType(
    node: ocgpuHipGraphNode_t,
    pType: *mut ocgpuHipGraphNodeType,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphNodeGetType else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, pType) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, pType);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeSetEnabled(
    hGraphExec: ocgpuHipGraphExec_t,
    hNode: ocgpuHipGraphNode_t,
    isEnabled: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphNodeSetEnabled else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hGraphExec, hNode, isEnabled) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hGraphExec, hNode, isEnabled);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphNodeSetParams(
    node: ocgpuHipGraphNode_t,
    nodeParams: *mut ocgpuHipGraphNodeParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphNodeSetParams else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(node, nodeParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (node, nodeParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphReleaseUserObject(
    graph: ocgpuHipGraph_t,
    object: ocgpuHipUserObject_t,
    count: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphReleaseUserObject
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, object, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, object, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphRemoveDependencies(
    graph: ocgpuHipGraph_t,
    from: *const ocgpuHipGraphNode_t,
    to: *const ocgpuHipGraphNode_t,
    numDependencies: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphRemoveDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, from, to, numDependencies) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, from, to, numDependencies);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphRetainUserObject(
    graph: ocgpuHipGraph_t,
    object: ocgpuHipUserObject_t,
    count: u32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphRetainUserObject
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graph, object, count, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graph, object, count, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphUpload(
    graphExec: ocgpuHipGraphExec_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphUpload else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(graphExec, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (graphExec, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphicsMapResources(
    count: i32,
    resources: *mut ocgpuHipGraphicsResource_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphicsMapResources
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, resources, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (count, resources, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphicsResourceGetMappedPointer(
    devPtr: *mut *mut c_void,
    size: *mut usize,
    resource: ocgpuHipGraphicsResource_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphicsResourceGetMappedPointer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, size, resource) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr, size, resource);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphicsSubResourceGetMappedArray(
    array: *mut ocgpuHipArray_t,
    resource: ocgpuHipGraphicsResource_t,
    arrayIndex: u32,
    mipLevel: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphicsSubResourceGetMappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(array, resource, arrayIndex, mipLevel) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (array, resource, arrayIndex, mipLevel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphicsUnmapResources(
    count: i32,
    resources: *mut ocgpuHipGraphicsResource_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGraphicsUnmapResources
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, resources, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (count, resources, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGraphicsUnregisterResource(
    resource: ocgpuHipGraphicsResource_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipGraphicsUnregisterResource
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(resource) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (resource,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipImportExternalMemory(
    extMem_out: *mut ocgpuHipExternalMemory_t,
    memHandleDesc: *const ocgpuHipExternalMemoryHandleDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipImportExternalMemory
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extMem_out, memHandleDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (extMem_out, memHandleDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipImportExternalSemaphore(
    extSem_out: *mut ocgpuHipExternalSemaphore_t,
    semHandleDesc: *const ocgpuHipExternalSemaphoreHandleDesc,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipImportExternalSemaphore
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSem_out, semHandleDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (extSem_out, semHandleDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipIpcGetEventHandle(
    handle: *mut ocgpuHipIpcEventHandle_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipIpcGetEventHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipIpcGetMemHandle(
    handle: *mut ocgpuHipIpcMemHandle_t,
    devPtr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipIpcGetMemHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, devPtr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle, devPtr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelGetLibrary(
    library: *mut ocgpuHipLibrary_t,
    kernel: ocgpuHipKernel_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelGetLibrary else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(library, kernel) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (library, kernel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelGetName(
    name: *mut *const c_char,
    kernel: ocgpuHipKernel_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelGetName else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(name, kernel) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (name, kernel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchCooperativeKernelMultiDevice(
    launchParamsList: *mut ocgpuHipLaunchParams,
    numDevices: i32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipLaunchCooperativeKernelMultiDevice
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(launchParamsList, numDevices, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (launchParamsList, numDevices, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchHostFunc(
    stream: ocgpuHipStream_t,
    fn_: ocgpuHipHostFn_t,
    userData: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchHostFunc else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, fn_, userData) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, fn_, userData);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchHostFunc_spt(
    stream: ocgpuHipStream_t,
    fn_: ocgpuHipHostFn_t,
    userData: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchHostFunc_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, fn_, userData) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, fn_, userData);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchKernelExC(
    config: *const ocgpuHipLaunchConfig_t,
    fPtr: *const c_void,
    args: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchKernelExC else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(config, fPtr, args) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (config, fPtr, args);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryEnumerateKernels(
    kernels: *mut ocgpuHipKernel_t,
    numKernels: u32,
    library: ocgpuHipLibrary_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryEnumerateKernels
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(kernels, numKernels, library) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (kernels, numKernels, library);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryGetKernel(
    pKernel: *mut ocgpuHipKernel_t,
    library: ocgpuHipLibrary_t,
    name: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryGetKernel else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pKernel, library, name) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pKernel, library, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryGetKernelCount(
    count: *mut u32,
    library: ocgpuHipLibrary_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryGetKernelCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(count, library) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (count, library);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryLoadData(
    library: *mut ocgpuHipLibrary_t,
    code: *const c_void,
    jitOptions: *mut ocgpuHipJitOption,
    jitOptionsValues: *mut *mut c_void,
    numJitOptions: u32,
    libraryOptions: *mut ocgpuHipLibraryOption,
    libraryOptionValues: *mut *mut c_void,
    numLibraryOptions: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryLoadData else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                library,
                code,
                jitOptions,
                jitOptionsValues,
                numJitOptions,
                libraryOptions,
                libraryOptionValues,
                numLibraryOptions,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            library,
            code,
            jitOptions,
            jitOptionsValues,
            numJitOptions,
            libraryOptions,
            libraryOptionValues,
            numLibraryOptions,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryLoadFromFile(
    library: *mut ocgpuHipLibrary_t,
    fileName: *const c_char,
    jitOptions: *mut ocgpuHipJitOption,
    jitOptionsValues: *mut *mut c_void,
    numJitOptions: u32,
    libraryOptions: *mut ocgpuHipLibraryOption,
    libraryOptionValues: *mut *mut c_void,
    numLibraryOptions: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryLoadFromFile else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                library,
                fileName,
                jitOptions,
                jitOptionsValues,
                numJitOptions,
                libraryOptions,
                libraryOptionValues,
                numLibraryOptions,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            library,
            fileName,
            jitOptions,
            jitOptionsValues,
            numJitOptions,
            libraryOptions,
            libraryOptionValues,
            numLibraryOptions,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryUnload(library: ocgpuHipLibrary_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryUnload else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(library) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (library,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLinkAddData(
    state: ocgpuHipLinkState_t,
    type_: ocgpuHipJitInputType,
    data: *mut c_void,
    size: usize,
    name: *const c_char,
    numOptions: u32,
    options: *mut ocgpuHipJitOption,
    optionValues: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLinkAddData else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                state,
                type_,
                data,
                size,
                name,
                numOptions,
                options,
                optionValues,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            state,
            type_,
            data,
            size,
            name,
            numOptions,
            options,
            optionValues,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLinkAddFile(
    state: ocgpuHipLinkState_t,
    type_: ocgpuHipJitInputType,
    path: *const c_char,
    numOptions: u32,
    options: *mut ocgpuHipJitOption,
    optionValues: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLinkAddFile else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state, type_, path, numOptions, options, optionValues) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (state, type_, path, numOptions, options, optionValues);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLinkComplete(
    state: ocgpuHipLinkState_t,
    hipBinOut: *mut *mut c_void,
    sizeOut: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLinkComplete else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state, hipBinOut, sizeOut) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (state, hipBinOut, sizeOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLinkCreate(
    numOptions: u32,
    options: *mut ocgpuHipJitOption,
    optionValues: *mut *mut c_void,
    stateOut: *mut ocgpuHipLinkState_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLinkCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numOptions, options, optionValues, stateOut) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numOptions, options, optionValues, stateOut);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLinkDestroy(state: ocgpuHipLinkState_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLinkDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(state) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (state,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocArray(
    array: *mut ocgpuHipArray_t,
    desc: *const ocgpuHipChannelFormatDesc,
    width: usize,
    height: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(array, desc, width, height, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (array, desc, width, height, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocFromPoolAsync(
    dev_ptr: *mut *mut c_void,
    size: usize,
    mem_pool: ocgpuHipMemPool_t,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocFromPoolAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, size, mem_pool, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, size, mem_pool, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemAdvise(
    dev_ptr: *const c_void,
    count: usize,
    advice: ocgpuHipMemoryAdvise,
    device: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemAdvise else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, count, advice, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, count, advice, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemCreate(
    handle: *mut ocgpuHipMemGenericAllocationHandle_t,
    size: usize,
    prop: *const ocgpuHipMemAllocationProp,
    flags: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, size, prop, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle, size, prop, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemExportToShareableHandle(
    shareableHandle: *mut c_void,
    handle: ocgpuHipMemGenericAllocationHandle_t,
    handleType: ocgpuHipMemAllocationHandleType,
    flags: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemExportToShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(shareableHandle, handle, handleType, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (shareableHandle, handle, handleType, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetAccess(
    flags: *mut u64,
    location: *const ocgpuHipMemLocation,
    ptr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemGetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags, location, ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flags, location, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetAllocationGranularity(
    granularity: *mut usize,
    prop: *const ocgpuHipMemAllocationProp,
    option: ocgpuHipMemAllocationGranularity_flags,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemGetAllocationGranularity
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(granularity, prop, option) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (granularity, prop, option);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetAllocationPropertiesFromHandle(
    prop: *mut ocgpuHipMemAllocationProp,
    handle: ocgpuHipMemGenericAllocationHandle_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemGetAllocationPropertiesFromHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(prop, handle) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (prop, handle);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetHandleForAddressRange(
    handle: *mut c_void,
    dptr: ocgpuHipDeviceptr_t,
    size: usize,
    handleType: ocgpuHipMemRangeHandleType,
    flags: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemGetHandleForAddressRange
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, dptr, size, handleType, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle, dptr, size, handleType, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemImportFromShareableHandle(
    handle: *mut ocgpuHipMemGenericAllocationHandle_t,
    osHandle: *mut c_void,
    shHandleType: ocgpuHipMemAllocationHandleType,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemImportFromShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, osHandle, shHandleType) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle, osHandle, shHandleType);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemMap(
    ptr: *mut c_void,
    size: usize,
    offset: usize,
    handle: ocgpuHipMemGenericAllocationHandle_t,
    flags: u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemMap else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, offset, handle, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size, offset, handle, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemMapArrayAsync(
    mapInfoList: *mut ocgpuHipArrayMapInfo,
    count: u32,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemMapArrayAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mapInfoList, count, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mapInfoList, count, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolCreate(
    mem_pool: *mut ocgpuHipMemPool_t,
    pool_props: *const ocgpuHipMemPoolProps,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, pool_props) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, pool_props);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolDestroy(mem_pool: ocgpuHipMemPool_t) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolExportPointer(
    export_data: *mut ocgpuHipMemPoolPtrExportData,
    dev_ptr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolExportPointer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(export_data, dev_ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (export_data, dev_ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolExportToShareableHandle(
    shared_handle: *mut c_void,
    mem_pool: ocgpuHipMemPool_t,
    handle_type: ocgpuHipMemAllocationHandleType,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemPoolExportToShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(shared_handle, mem_pool, handle_type, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (shared_handle, mem_pool, handle_type, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolGetAccess(
    flags: *mut ocgpuHipMemAccessFlags,
    mem_pool: ocgpuHipMemPool_t,
    location: *mut ocgpuHipMemLocation,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolGetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(flags, mem_pool, location) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (flags, mem_pool, location);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolGetAttribute(
    mem_pool: ocgpuHipMemPool_t,
    attr: ocgpuHipMemPoolAttr,
    value: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolImportFromShareableHandle(
    mem_pool: *mut ocgpuHipMemPool_t,
    shared_handle: *mut c_void,
    handle_type: ocgpuHipMemAllocationHandleType,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemPoolImportFromShareableHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, shared_handle, handle_type, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, shared_handle, handle_type, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolImportPointer(
    dev_ptr: *mut *mut c_void,
    mem_pool: ocgpuHipMemPool_t,
    export_data: *mut ocgpuHipMemPoolPtrExportData,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolImportPointer
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, mem_pool, export_data) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, mem_pool, export_data);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolSetAccess(
    mem_pool: ocgpuHipMemPool_t,
    desc_list: *const ocgpuHipMemAccessDesc,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolSetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, desc_list, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, desc_list, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolSetAttribute(
    mem_pool: ocgpuHipMemPool_t,
    attr: ocgpuHipMemPoolAttr,
    value: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPoolTrimTo(
    mem_pool: ocgpuHipMemPool_t,
    min_bytes_to_hold: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPoolTrimTo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mem_pool, min_bytes_to_hold) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mem_pool, min_bytes_to_hold);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemRangeGetAttribute(
    data: *mut c_void,
    data_size: usize,
    attribute: ocgpuHipMemRangeAttribute,
    dev_ptr: *const c_void,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemRangeGetAttribute
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(data, data_size, attribute, dev_ptr, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (data, data_size, attribute, dev_ptr, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemRangeGetAttributes(
    data: *mut *mut c_void,
    data_sizes: *mut usize,
    attributes: *mut ocgpuHipMemRangeAttribute,
    num_attributes: usize,
    dev_ptr: *const c_void,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemRangeGetAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(data, data_sizes, attributes, num_attributes, dev_ptr, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (data, data_sizes, attributes, num_attributes, dev_ptr, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemRelease(
    handle: ocgpuHipMemGenericAllocationHandle_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemRelease else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemRetainAllocationHandle(
    handle: *mut ocgpuHipMemGenericAllocationHandle_t,
    addr: *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemRetainAllocationHandle
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(handle, addr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (handle, addr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemSetAccess(
    ptr: *mut c_void,
    size: usize,
    desc: *const ocgpuHipMemAccessDesc,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemSetAccess else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ptr, size, desc, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ptr, size, desc, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy(
    dst: *mut c_void,
    src: *const c_void,
    sizeBytes: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2D(
    dst: *mut c_void,
    dpitch: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dpitch, src, spitch, width, height, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dpitch, src, spitch, width, height, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DArrayToArray(
    dst: ocgpuHipArray_t,
    wOffsetDst: usize,
    hOffsetDst: usize,
    src: ocgpuHipArray_const_t,
    wOffsetSrc: usize,
    hOffsetSrc: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DArrayToArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dst, wOffsetDst, hOffsetDst, src, wOffsetSrc, hOffsetSrc, width, height, kind,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dst, wOffsetDst, hOffsetDst, src, wOffsetSrc, hOffsetSrc, width, height, kind,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DAsync(
    dst: *mut c_void,
    dpitch: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dpitch, src, spitch, width, height, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dpitch, src, spitch, width, height, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DAsync_spt(
    dst: *mut c_void,
    dpitch: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DAsync_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dpitch, src, spitch, width, height, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dpitch, src, spitch, width, height, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DFromArray(
    dst: *mut c_void,
    dpitch: usize,
    src: ocgpuHipArray_const_t,
    wOffset: usize,
    hOffset: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DFromArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dpitch, src, wOffset, hOffset, width, height, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dpitch, src, wOffset, hOffset, width, height, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DFromArrayAsync(
    dst: *mut c_void,
    dpitch: usize,
    src: ocgpuHipArray_const_t,
    wOffset: usize,
    hOffset: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DFromArrayAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dst, dpitch, src, wOffset, hOffset, width, height, kind, stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dst, dpitch, src, wOffset, hOffset, width, height, kind, stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DFromArrayAsync_spt(
    dst: *mut c_void,
    dpitch: usize,
    src: ocgpuHipArray_const_t,
    wOffsetSrc: usize,
    hOffsetSrc: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemcpy2DFromArrayAsync_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dst, dpitch, src, wOffsetSrc, hOffsetSrc, width, height, kind, stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dst, dpitch, src, wOffsetSrc, hOffsetSrc, width, height, kind, stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DFromArray_spt(
    dst: *mut c_void,
    dpitch: usize,
    src: ocgpuHipArray_const_t,
    wOffset: usize,
    hOffset: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DFromArray_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dpitch, src, wOffset, hOffset, width, height, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dpitch, src, wOffset, hOffset, width, height, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DToArray(
    dst: ocgpuHipArray_t,
    wOffset: usize,
    hOffset: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DToArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, wOffset, hOffset, src, spitch, width, height, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, wOffset, hOffset, src, spitch, width, height, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DToArrayAsync(
    dst: ocgpuHipArray_t,
    wOffset: usize,
    hOffset: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DToArrayAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dst, wOffset, hOffset, src, spitch, width, height, kind, stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dst, wOffset, hOffset, src, spitch, width, height, kind, stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DToArrayAsync_spt(
    dst: ocgpuHipArray_t,
    wOffset: usize,
    hOffset: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DToArrayAsync_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dst, wOffset, hOffset, src, spitch, width, height, kind, stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dst, wOffset, hOffset, src, spitch, width, height, kind, stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2DToArray_spt(
    dst: ocgpuHipArray_t,
    wOffset: usize,
    hOffset: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2DToArray_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, wOffset, hOffset, src, spitch, width, height, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, wOffset, hOffset, src, spitch, width, height, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy2D_spt(
    dst: *mut c_void,
    dpitch: usize,
    src: *const c_void,
    spitch: usize,
    width: usize,
    height: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy2D_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, dpitch, src, spitch, width, height, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, dpitch, src, spitch, width, height, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3D(p: *const ocgpuHipMemcpy3DParms) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (p,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3DAsync(
    p: *const ocgpuHipMemcpy3DParms,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (p, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3DAsync_spt(
    p: *const ocgpuHipMemcpy3DParms,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3DAsync_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (p, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3DBatchAsync(
    numOps: usize,
    opList: *mut ocgpuHipMemcpy3DBatchOp,
    failIdx: *mut usize,
    flags: u64,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3DBatchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numOps, opList, failIdx, flags, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numOps, opList, failIdx, flags, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3DPeer(
    p: *mut ocgpuHipMemcpy3DPeerParms,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3DPeer else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (p,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3DPeerAsync(
    p: *mut ocgpuHipMemcpy3DPeerParms,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3DPeerAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (p, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy3D_spt(p: *const ocgpuHipMemcpy3DParms) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy3D_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(p) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (p,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyAsync(
    dst: *mut c_void,
    src: *const c_void,
    sizeBytes: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyAsync_spt(
    dst: *mut c_void,
    src: *const c_void,
    sizeBytes: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyAsync_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyAtoA(
    dstArray: ocgpuHipArray_t,
    dstOffset: usize,
    srcArray: ocgpuHipArray_t,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyAtoA else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dstArray, dstOffset, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyAtoD(
    dstDevice: ocgpuHipDeviceptr_t,
    srcArray: ocgpuHipArray_t,
    srcOffset: usize,
    ByteCount: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyAtoD else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstDevice, srcArray, srcOffset, ByteCount) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dstDevice, srcArray, srcOffset, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyAtoH(
    dst: *mut c_void,
    srcArray: ocgpuHipArray_t,
    srcOffset: usize,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyAtoH else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, srcArray, srcOffset, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, srcArray, srcOffset, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyAtoHAsync(
    dstHost: *mut c_void,
    srcArray: ocgpuHipArray_t,
    srcOffset: usize,
    ByteCount: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyAtoHAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstHost, srcArray, srcOffset, ByteCount, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dstHost, srcArray, srcOffset, ByteCount, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyBatchAsync(
    dsts: *mut *mut c_void,
    srcs: *mut *mut c_void,
    sizes: *mut usize,
    count: usize,
    attrs: *mut ocgpuHipMemcpyAttributes,
    attrsIdxs: *mut usize,
    numAttrs: usize,
    failIdx: *mut usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyBatchAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dsts, srcs, sizes, count, attrs, attrsIdxs, numAttrs, failIdx, stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dsts, srcs, sizes, count, attrs, attrsIdxs, numAttrs, failIdx, stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyDtoA(
    dstArray: ocgpuHipArray_t,
    dstOffset: usize,
    srcDevice: ocgpuHipDeviceptr_t,
    ByteCount: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyDtoA else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcDevice, ByteCount) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dstArray, dstOffset, srcDevice, ByteCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyFromArray(
    dst: *mut c_void,
    srcArray: ocgpuHipArray_const_t,
    wOffset: usize,
    hOffset: usize,
    count: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyFromArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, srcArray, wOffset, hOffset, count, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, srcArray, wOffset, hOffset, count, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyFromArray_spt(
    dst: *mut c_void,
    src: ocgpuHipArray_const_t,
    wOffsetSrc: usize,
    hOffset: usize,
    count: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyFromArray_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, wOffsetSrc, hOffset, count, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, wOffsetSrc, hOffset, count, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyFromSymbol(
    dst: *mut c_void,
    symbol: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyFromSymbol else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, symbol, sizeBytes, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, symbol, sizeBytes, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyFromSymbolAsync(
    dst: *mut c_void,
    symbol: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyFromSymbolAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, symbol, sizeBytes, offset, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, symbol, sizeBytes, offset, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyFromSymbolAsync_spt(
    dst: *mut c_void,
    symbol: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemcpyFromSymbolAsync_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, symbol, sizeBytes, offset, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, symbol, sizeBytes, offset, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyFromSymbol_spt(
    dst: *mut c_void,
    symbol: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyFromSymbol_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, symbol, sizeBytes, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, symbol, sizeBytes, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyHtoA(
    dstArray: ocgpuHipArray_t,
    dstOffset: usize,
    srcHost: *const c_void,
    count: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyHtoA else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcHost, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dstArray, dstOffset, srcHost, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyHtoAAsync(
    dstArray: ocgpuHipArray_t,
    dstOffset: usize,
    srcHost: *const c_void,
    ByteCount: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyHtoAAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dstArray, dstOffset, srcHost, ByteCount, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dstArray, dstOffset, srcHost, ByteCount, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyParam2D(pCopy: *const ocgpuHip_Memcpy2D) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyParam2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pCopy,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyParam2DAsync(
    pCopy: *const ocgpuHip_Memcpy2D,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyParam2DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pCopy, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pCopy, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyToArray(
    dst: ocgpuHipArray_t,
    wOffset: usize,
    hOffset: usize,
    src: *const c_void,
    count: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyToArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, wOffset, hOffset, src, count, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, wOffset, hOffset, src, count, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyToSymbol(
    symbol: *const c_void,
    src: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyToSymbol else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, src, sizeBytes, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, src, sizeBytes, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyToSymbolAsync(
    symbol: *const c_void,
    src: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyToSymbolAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, src, sizeBytes, offset, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, src, sizeBytes, offset, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyToSymbolAsync_spt(
    symbol: *const c_void,
    src: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyToSymbolAsync_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, src, sizeBytes, offset, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, src, sizeBytes, offset, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyToSymbol_spt(
    symbol: *const c_void,
    src: *const c_void,
    sizeBytes: usize,
    offset: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyToSymbol_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, src, sizeBytes, offset, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, src, sizeBytes, offset, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpyWithStream(
    dst: *mut c_void,
    src: *const c_void,
    sizeBytes: usize,
    kind: ocgpuHipMemcpyKind,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpyWithStream else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, kind, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, kind, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemcpy_spt(
    dst: *mut c_void,
    src: *const c_void,
    sizeBytes: usize,
    kind: ocgpuHipMemcpyKind,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemcpy_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dst, src, sizeBytes, kind) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dst, src, sizeBytes, kind);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMipmappedArrayCreate(
    pHandle: *mut ocgpuHipMipmappedArray_t,
    pMipmappedArrayDesc: *mut ocgpuHIP_ARRAY3D_DESCRIPTOR,
    numMipmapLevels: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMipmappedArrayCreate
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pHandle, pMipmappedArrayDesc, numMipmapLevels) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pHandle, pMipmappedArrayDesc, numMipmapLevels);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMipmappedArrayDestroy(
    hMipmappedArray: ocgpuHipMipmappedArray_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMipmappedArrayDestroy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hMipmappedArray) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hMipmappedArray,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMipmappedArrayGetLevel(
    pLevelArray: *mut ocgpuHipArray_t,
    hMipMappedArray: ocgpuHipMipmappedArray_t,
    level: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMipmappedArrayGetLevel
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pLevelArray, hMipMappedArray, level) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pLevelArray, hMipMappedArray, level);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleGetTexRef(
    texRef: *mut *mut ocgputextureReference,
    hmod: ocgpuHipModule_t,
    name: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleGetTexRef else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, hmod, name) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, hmod, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLaunchCooperativeKernelMultiDevice(
    launchParamsList: *mut ocgpuHipFunctionLaunchParams,
    numDevices: u32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipModuleLaunchCooperativeKernelMultiDevice
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(launchParamsList, numDevices, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (launchParamsList, numDevices, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipModuleLoadDataEx(
    module: *mut ocgpuHipModule_t,
    image: *const c_void,
    numOptions: u32,
    options: *mut ocgpuHipJitOption,
    optionValues: *mut *mut c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipModuleLoadDataEx else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(module, image, numOptions, options, optionValues) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (module, image, numOptions, options, optionValues);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipPointerGetAttribute(
    data: *mut c_void,
    attribute: ocgpuHipPointer_attribute,
    ptr: ocgpuHipDeviceptr_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipPointerGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(data, attribute, ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (data, attribute, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipPointerGetAttributes(
    attributes: *mut ocgpuHipPointerAttribute_t,
    ptr: *const c_void,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipPointerGetAttributes
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attributes, ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (attributes, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipPointerSetAttribute(
    value: *const c_void,
    attribute: ocgpuHipPointer_attribute,
    ptr: ocgpuHipDeviceptr_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipPointerSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(value, attribute, ptr) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (value, attribute, ptr);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipSignalExternalSemaphoresAsync(
    extSemArray: *const ocgpuHipExternalSemaphore_t,
    paramsArray: *const ocgpuHipExternalSemaphoreSignalParams,
    numExtSems: u32,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipSignalExternalSemaphoresAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSemArray, paramsArray, numExtSems, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (extSemArray, paramsArray, numExtSems, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamAddCallback(
    stream: ocgpuHipStream_t,
    callback: ocgpuHipStreamCallback_t,
    userData: *mut c_void,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamAddCallback else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, callback, userData, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, callback, userData, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamAddCallback_spt(
    stream: ocgpuHipStream_t,
    callback: ocgpuHipStreamCallback_t,
    userData: *mut c_void,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamAddCallback_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, callback, userData, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, callback, userData, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamBatchMemOp(
    stream: ocgpuHipStream_t,
    count: u32,
    paramArray: *mut ocgpuHipStreamBatchMemOpParams,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamBatchMemOp else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, count, paramArray, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, count, paramArray, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamBeginCapture(
    stream: ocgpuHipStream_t,
    mode: ocgpuHipStreamCaptureMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamBeginCapture else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, mode) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, mode);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamBeginCaptureToGraph(
    stream: ocgpuHipStream_t,
    graph: ocgpuHipGraph_t,
    dependencies: *const ocgpuHipGraphNode_t,
    dependencyData: *const ocgpuHipGraphEdgeData,
    numDependencies: usize,
    mode: ocgpuHipStreamCaptureMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipStreamBeginCaptureToGraph
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                stream,
                graph,
                dependencies,
                dependencyData,
                numDependencies,
                mode,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            stream,
            graph,
            dependencies,
            dependencyData,
            numDependencies,
            mode,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamBeginCapture_spt(
    stream: ocgpuHipStream_t,
    mode: ocgpuHipStreamCaptureMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamBeginCapture_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, mode) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, mode);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamEndCapture(
    stream: ocgpuHipStream_t,
    pGraph: *mut ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamEndCapture else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, pGraph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, pGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamEndCapture_spt(
    stream: ocgpuHipStream_t,
    pGraph: *mut ocgpuHipGraph_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamEndCapture_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, pGraph) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, pGraph);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetAttribute(
    stream: ocgpuHipStream_t,
    attr: ocgpuHipLaunchAttributeID,
    value_out: *mut ocgpuHipLaunchAttributeValue,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, attr, value_out) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, attr, value_out);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetCaptureInfo(
    stream: ocgpuHipStream_t,
    pCaptureStatus: *mut ocgpuHipStreamCaptureStatus,
    pId: *mut u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetCaptureInfo
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, pCaptureStatus, pId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, pCaptureStatus, pId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetCaptureInfo_spt(
    stream: ocgpuHipStream_t,
    pCaptureStatus: *mut ocgpuHipStreamCaptureStatus,
    pId: *mut u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetCaptureInfo_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, pCaptureStatus, pId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, pCaptureStatus, pId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetCaptureInfo_v2(
    stream: ocgpuHipStream_t,
    captureStatus_out: *mut ocgpuHipStreamCaptureStatus,
    id_out: *mut u64,
    graph_out: *mut ocgpuHipGraph_t,
    dependencies_out: *mut *const ocgpuHipGraphNode_t,
    numDependencies_out: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetCaptureInfo_v2
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                stream,
                captureStatus_out,
                id_out,
                graph_out,
                dependencies_out,
                numDependencies_out,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            stream,
            captureStatus_out,
            id_out,
            graph_out,
            dependencies_out,
            numDependencies_out,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetCaptureInfo_v2_spt(
    stream: ocgpuHipStream_t,
    captureStatus_out: *mut ocgpuHipStreamCaptureStatus,
    id_out: *mut u64,
    graph_out: *mut ocgpuHipGraph_t,
    dependencies_out: *mut *const ocgpuHipGraphNode_t,
    numDependencies_out: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipStreamGetCaptureInfo_v2_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                stream,
                captureStatus_out,
                id_out,
                graph_out,
                dependencies_out,
                numDependencies_out,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            stream,
            captureStatus_out,
            id_out,
            graph_out,
            dependencies_out,
            numDependencies_out,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamIsCapturing(
    stream: ocgpuHipStream_t,
    pCaptureStatus: *mut ocgpuHipStreamCaptureStatus,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamIsCapturing else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, pCaptureStatus) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, pCaptureStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamIsCapturing_spt(
    stream: ocgpuHipStream_t,
    pCaptureStatus: *mut ocgpuHipStreamCaptureStatus,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamIsCapturing_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, pCaptureStatus) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, pCaptureStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamSetAttribute(
    stream: ocgpuHipStream_t,
    attr: ocgpuHipLaunchAttributeID,
    value: *const ocgpuHipLaunchAttributeValue,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, attr, value) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, attr, value);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamUpdateCaptureDependencies(
    stream: ocgpuHipStream_t,
    dependencies: *mut ocgpuHipGraphNode_t,
    numDependencies: usize,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipStreamUpdateCaptureDependencies
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, dependencies, numDependencies, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, dependencies, numDependencies, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexObjectCreate(
    pTexObject: *mut ocgpuHipTextureObject_t,
    pResDesc: *const ocgpuHIP_RESOURCE_DESC,
    pTexDesc: *const ocgpuHIP_TEXTURE_DESC,
    pResViewDesc: *const ocgpuHIP_RESOURCE_VIEW_DESC,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexObjectCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexObject, pResDesc, pTexDesc, pResViewDesc) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pTexObject, pResDesc, pTexDesc, pResViewDesc);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexObjectDestroy(
    texObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexObjectDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texObject,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexObjectGetResourceDesc(
    pResDesc: *mut ocgpuHIP_RESOURCE_DESC,
    texObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexObjectGetResourceDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResDesc, texObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pResDesc, texObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexObjectGetResourceViewDesc(
    pResViewDesc: *mut ocgpuHIP_RESOURCE_VIEW_DESC,
    texObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipTexObjectGetResourceViewDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pResViewDesc, texObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pResViewDesc, texObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexObjectGetTextureDesc(
    pTexDesc: *mut ocgpuHIP_TEXTURE_DESC,
    texObject: ocgpuHipTextureObject_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexObjectGetTextureDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pTexDesc, texObject) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pTexDesc, texObject);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetAddress(
    dev_ptr: *mut ocgpuHipDeviceptr_t,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetAddressMode(
    pam: *mut ocgpuHipTextureAddressMode,
    texRef: *const ocgputextureReference,
    dim: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetAddressMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pam, texRef, dim) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pam, texRef, dim);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetArray(
    pArray: *mut ocgpuHipArray_t,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArray, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pArray, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetBorderColor(
    pBorderColor: *mut f32,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetBorderColor
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pBorderColor, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pBorderColor, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetFilterMode(
    pfm: *mut ocgpuHipTextureFilterMode,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetFilterMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pfm, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pfm, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetFlags(
    pFlags: *mut u32,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFlags, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pFlags, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetFormat(
    pFormat: *mut ocgpuHipArray_Format,
    pNumChannels: *mut i32,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetFormat else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFormat, pNumChannels, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pFormat, pNumChannels, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetMaxAnisotropy(
    pmaxAnsio: *mut i32,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetMaxAnisotropy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pmaxAnsio, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pmaxAnsio, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetMipMappedArray(
    pArray: *mut ocgpuHipMipmappedArray_t,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetMipMappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pArray, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pArray, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetMipmapFilterMode(
    pfm: *mut ocgpuHipTextureFilterMode,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipTexRefGetMipmapFilterMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pfm, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pfm, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetMipmapLevelBias(
    pbias: *mut f32,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefGetMipmapLevelBias
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pbias, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pbias, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefGetMipmapLevelClamp(
    pminMipmapLevelClamp: *mut f32,
    pmaxMipmapLevelClamp: *mut f32,
    texRef: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipTexRefGetMipmapLevelClamp
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pminMipmapLevelClamp, pmaxMipmapLevelClamp, texRef) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pminMipmapLevelClamp, pmaxMipmapLevelClamp, texRef);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetAddress(
    ByteOffset: *mut usize,
    texRef: *mut ocgputextureReference,
    dptr: ocgpuHipDeviceptr_t,
    bytes: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetAddress else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ByteOffset, texRef, dptr, bytes) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ByteOffset, texRef, dptr, bytes);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetAddress2D(
    texRef: *mut ocgputextureReference,
    desc: *const ocgpuHIP_ARRAY_DESCRIPTOR,
    dptr: ocgpuHipDeviceptr_t,
    Pitch: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetAddress2D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, desc, dptr, Pitch) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, desc, dptr, Pitch);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetAddressMode(
    texRef: *mut ocgputextureReference,
    dim: i32,
    am: ocgpuHipTextureAddressMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetAddressMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, dim, am) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, dim, am);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetArray(
    tex: *mut ocgputextureReference,
    array: ocgpuHipArray_const_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(tex, array, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (tex, array, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetBorderColor(
    texRef: *mut ocgputextureReference,
    pBorderColor: *mut f32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetBorderColor
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, pBorderColor) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, pBorderColor);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetFilterMode(
    texRef: *mut ocgputextureReference,
    fm: ocgpuHipTextureFilterMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetFilterMode else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, fm) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, fm);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetFlags(
    texRef: *mut ocgputextureReference,
    Flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetFlags else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, Flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetFormat(
    texRef: *mut ocgputextureReference,
    fmt: ocgpuHipArray_Format,
    NumPackedComponents: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetFormat else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, fmt, NumPackedComponents) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, fmt, NumPackedComponents);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetMaxAnisotropy(
    texRef: *mut ocgputextureReference,
    maxAniso: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetMaxAnisotropy
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, maxAniso) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, maxAniso);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetMipmapFilterMode(
    texRef: *mut ocgputextureReference,
    fm: ocgpuHipTextureFilterMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipTexRefSetMipmapFilterMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, fm) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, fm);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetMipmapLevelBias(
    texRef: *mut ocgputextureReference,
    bias: f32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetMipmapLevelBias
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, bias) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, bias);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetMipmapLevelClamp(
    texRef: *mut ocgputextureReference,
    minMipMapLevelClamp: f32,
    maxMipMapLevelClamp: f32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipTexRefSetMipmapLevelClamp
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, minMipMapLevelClamp, maxMipMapLevelClamp) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, minMipMapLevelClamp, maxMipMapLevelClamp);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipTexRefSetMipmappedArray(
    texRef: *mut ocgputextureReference,
    mipmappedArray: *mut ocgpuHipMipmappedArray,
    Flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipTexRefSetMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(texRef, mipmappedArray, Flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (texRef, mipmappedArray, Flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipThreadExchangeStreamCaptureMode(
    mode: *mut ocgpuHipStreamCaptureMode,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipThreadExchangeStreamCaptureMode
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mode) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mode,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipUnbindTexture(
    tex: *const ocgputextureReference,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipUnbindTexture else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(tex) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (tex,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipUserObjectCreate(
    object_out: *mut ocgpuHipUserObject_t,
    ptr: *mut c_void,
    destroy: ocgpuHipHostFn_t,
    initialRefcount: u32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipUserObjectCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(object_out, ptr, destroy, initialRefcount, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (object_out, ptr, destroy, initialRefcount, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipUserObjectRelease(
    object: ocgpuHipUserObject_t,
    count: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipUserObjectRelease else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(object, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (object, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipUserObjectRetain(
    object: ocgpuHipUserObject_t,
    count: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipUserObjectRetain else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(object, count) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (object, count);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipWaitExternalSemaphoresAsync(
    extSemArray: *const ocgpuHipExternalSemaphore_t,
    paramsArray: *const ocgpuHipExternalSemaphoreWaitParams,
    numExtSems: u32,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipWaitExternalSemaphoresAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(extSemArray, paramsArray, numExtSems, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (extSemArray, paramsArray, numExtSems, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipConfigureCall(
    gridDim: ocgpudim3,
    blockDim: ocgpudim3,
    sharedMem: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipConfigureCall else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(gridDim, blockDim, sharedMem, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (gridDim, blockDim, sharedMem, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
#[allow(clippy::many_single_char_names)]
pub unsafe extern "C" fn ocgpuHipCreateChannelDesc(
    x: i32,
    y: i32,
    z: i32,
    w: i32,
    f: ocgpuHipChannelFormatKind,
) -> ocgpuHipChannelFormatDesc {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return {
                // SAFETY: ABI v1 defines the missing aggregate/POD sentinel as a zeroed object representation.
                unsafe { core::mem::zeroed::<ocgpuHipChannelFormatDesc>() }
            };
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipCreateChannelDesc else {
            return {
                // SAFETY: ABI v1 defines the missing aggregate/POD sentinel as a zeroed object representation.
                unsafe { core::mem::zeroed::<ocgpuHipChannelFormatDesc>() }
            };
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(x, y, z, w, f) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (x, y, z, w, f);
        {
            // SAFETY: ABI v1 defines the missing aggregate/POD sentinel as a zeroed object representation.
            unsafe { core::mem::zeroed::<ocgpuHipChannelFormatDesc>() }
        }
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtLaunchKernel(
    function_address: *const c_void,
    numBlocks: ocgpudim3,
    dimBlocks: ocgpudim3,
    args: *mut *mut c_void,
    sharedMemBytes: usize,
    stream: ocgpuHipStream_t,
    startEvent: ocgpuHipEvent_t,
    stopEvent: ocgpuHipEvent_t,
    flags: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtLaunchKernel else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                function_address,
                numBlocks,
                dimBlocks,
                args,
                sharedMemBytes,
                stream,
                startEvent,
                stopEvent,
                flags,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            function_address,
            numBlocks,
            dimBlocks,
            args,
            sharedMemBytes,
            stream,
            startEvent,
            stopEvent,
            flags,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipIpcOpenEventHandle(
    event: *mut ocgpuHipEvent_t,
    handle: ocgpuHipIpcEventHandle_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipIpcOpenEventHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(event, handle) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (event, handle);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipIpcOpenMemHandle(
    devPtr: *mut *mut c_void,
    handle: ocgpuHipIpcMemHandle_t,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipIpcOpenMemHandle else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(devPtr, handle, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (devPtr, handle, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchCooperativeKernel(
    f: *const c_void,
    gridDim: ocgpudim3,
    blockDimX: ocgpudim3,
    kernelParams: *mut *mut c_void,
    sharedMemBytes: u32,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchCooperativeKernel
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(f, gridDim, blockDimX, kernelParams, sharedMemBytes, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (f, gridDim, blockDimX, kernelParams, sharedMemBytes, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchCooperativeKernel_spt(
    f: *const c_void,
    gridDim: ocgpudim3,
    blockDim: ocgpudim3,
    kernelParams: *mut *mut c_void,
    sharedMemBytes: u32,
    hStream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipLaunchCooperativeKernel_spt
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(f, gridDim, blockDim, kernelParams, sharedMemBytes, hStream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (f, gridDim, blockDim, kernelParams, sharedMemBytes, hStream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchKernel(
    function_address: *const c_void,
    numBlocks: ocgpudim3,
    dimBlocks: ocgpudim3,
    args: *mut *mut c_void,
    sharedMemBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchKernel else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                function_address,
                numBlocks,
                dimBlocks,
                args,
                sharedMemBytes,
                stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            function_address,
            numBlocks,
            dimBlocks,
            args,
            sharedMemBytes,
            stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLaunchKernel_spt(
    function_address: *const c_void,
    numBlocks: ocgpudim3,
    dimBlocks: ocgpudim3,
    args: *mut *mut c_void,
    sharedMemBytes: usize,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLaunchKernel_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                function_address,
                numBlocks,
                dimBlocks,
                args,
                sharedMemBytes,
                stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            function_address,
            numBlocks,
            dimBlocks,
            args,
            sharedMemBytes,
            stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMalloc3D(
    pitchedDevPtr: *mut ocgpuHipPitchedPtr,
    extent: ocgpuHipExtent,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMalloc3D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pitchedDevPtr, extent) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pitchedDevPtr, extent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMalloc3DArray(
    array: *mut ocgpuHipArray_t,
    desc: *const ocgpuHipChannelFormatDesc,
    extent: ocgpuHipExtent,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMalloc3DArray else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(array, desc, extent, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (array, desc, extent, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMallocMipmappedArray(
    mipmappedArray: *mut ocgpuHipMipmappedArray_t,
    desc: *const ocgpuHipChannelFormatDesc,
    extent: ocgpuHipExtent,
    numLevels: u32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMallocMipmappedArray
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(mipmappedArray, desc, extent, numLevels, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (mipmappedArray, desc, extent, numLevels, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemAdvise_v2(
    dev_ptr: *const c_void,
    count: usize,
    advice: ocgpuHipMemoryAdvise,
    location: ocgpuHipMemLocation,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemAdvise_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, count, advice, location) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, count, advice, location);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPrefetchAsync_v2(
    dev_ptr: *const c_void,
    count: usize,
    location: ocgpuHipMemLocation,
    flags: u32,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPrefetchAsync_v2 else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptr, count, location, flags, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptr, count, location, flags, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset3D(
    pitchedDevPtr: ocgpuHipPitchedPtr,
    value: i32,
    extent: ocgpuHipExtent,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset3D else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pitchedDevPtr, value, extent) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pitchedDevPtr, value, extent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset3DAsync(
    pitchedDevPtr: ocgpuHipPitchedPtr,
    value: i32,
    extent: ocgpuHipExtent,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset3DAsync else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pitchedDevPtr, value, extent, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pitchedDevPtr, value, extent, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset3DAsync_spt(
    pitchedDevPtr: ocgpuHipPitchedPtr,
    value: i32,
    extent: ocgpuHipExtent,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset3DAsync_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pitchedDevPtr, value, extent, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pitchedDevPtr, value, extent, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemset3D_spt(
    pitchedDevPtr: ocgpuHipPitchedPtr,
    value: i32,
    extent: ocgpuHipExtent,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemset3D_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pitchedDevPtr, value, extent) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pitchedDevPtr, value, extent);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevResourceGenerateDesc(
    phDesc: *mut ocgpuHipDevResourceDesc_t,
    resources: *mut ocgpuHipDevResource,
    nbResources: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevResourceGenerateDesc
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(phDesc, resources, nbResources) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (phDesc, resources, nbResources);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevSmResourceSplit(
    result: *mut ocgpuHipDevResource,
    nbGroups: u32,
    input: *const ocgpuHipDevResource,
    remainder: *mut ocgpuHipDevResource,
    flags: u32,
    groupParams: *mut ocgpuHipDevSmResourceGroupParams,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDevSmResourceSplit else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(result, nbGroups, input, remainder, flags, groupParams) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (result, nbGroups, input, remainder, flags, groupParams);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDevSmResourceSplitByCount(
    result: *mut ocgpuHipDevResource,
    nbGroups: *mut u32,
    input: *const ocgpuHipDevResource,
    remainder: *mut ocgpuHipDevResource,
    flags: u32,
    minCount: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDevSmResourceSplitByCount
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(result, nbGroups, input, remainder, flags, minCount) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (result, nbGroups, input, remainder, flags, minCount);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetDevResource(
    device: ocgpuHipDevice_t,
    resource: *mut ocgpuHipDevResource,
    type_: ocgpuHipDevResourceType,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetDevResource
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, resource, type_) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDeviceGetExecutionCtx(
    ctx: *mut ocgpuHipExecutionCtx_t,
    device: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDeviceGetExecutionCtx
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvMemDiscardAndPrefetchBatchAsync(
    dptrs: *mut ocgpuHipDeviceptr_t,
    sizes: *mut usize,
    count: usize,
    prefetchLocs: *mut ocgpuHipMemLocation,
    prefetchLocIdxs: *mut usize,
    numPrefetchLocs: usize,
    flags: u64,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipDrvMemDiscardAndPrefetchBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dptrs,
                sizes,
                count,
                prefetchLocs,
                prefetchLocIdxs,
                numPrefetchLocs,
                flags,
                stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dptrs,
            sizes,
            count,
            prefetchLocs,
            prefetchLocIdxs,
            numPrefetchLocs,
            flags,
            stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipDrvMemDiscardBatchAsync(
    dptrs: *mut ocgpuHipDeviceptr_t,
    sizes: *mut usize,
    count: usize,
    flags: u64,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipDrvMemDiscardBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptrs, sizes, count, flags, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dptrs, sizes, count, flags, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxDestroy(
    ctx: ocgpuHipExecutionCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxDestroy else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxGetDevResource(
    ctx: ocgpuHipExecutionCtx_t,
    resource: *mut ocgpuHipDevResource,
    type_: ocgpuHipDevResourceType,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipExecutionCtxGetDevResource
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, resource, type_) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxGetDevice(
    device: *mut i32,
    ctx: ocgpuHipExecutionCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxGetDevice
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, ctx);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxGetId(
    ctx: ocgpuHipExecutionCtx_t,
    ctxId: *mut u64,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxGetId else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, ctxId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, ctxId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxRecordEvent(
    ctx: ocgpuHipExecutionCtx_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxRecordEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxStreamCreate(
    stream: *mut ocgpuHipStream_t,
    greenctx: ocgpuHipExecutionCtx_t,
    flags: u32,
    priority: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxStreamCreate
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(stream, greenctx, flags, priority) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (stream, greenctx, flags, priority);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxSynchronize(
    ctx: ocgpuHipExecutionCtx_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxSynchronize
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx,);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExecutionCtxWaitEvent(
    ctx: ocgpuHipExecutionCtx_t,
    event: ocgpuHipEvent_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExecutionCtxWaitEvent
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, event) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, event);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtDisableLogging() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtDisableLogging else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtEnableLogging() -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtEnableLogging else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch() }
    }
    #[cfg(not(feature = "hip"))]
    {
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipExtSetLoggingParams(
    log_level: usize,
    log_size: usize,
    log_mask: usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipExtSetLoggingParams else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(log_level, log_size, log_mask) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (log_level, log_size, log_mask);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetProcAddress_spt(
    symbol: *const c_char,
    pfn: *mut *mut c_void,
    hipVersion: i32,
    flags: u64,
    symbolStatus: *mut ocgpuHipDriverProcAddressQueryResult,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetProcAddress_spt else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(symbol, pfn, hipVersion, flags, symbolStatus) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (symbol, pfn, hipVersion, flags, symbolStatus);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGreenCtxCreate(
    ctx: *mut ocgpuHipExecutionCtx_t,
    desc: ocgpuHipDevResourceDesc_t,
    device: i32,
    flags: u32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGreenCtxCreate else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(ctx, desc, device, flags) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (ctx, desc, device, flags);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelGetAttribute(
    pi: *mut i32,
    attrib: ocgpuHipFunction_attribute,
    kernel: ocgpuHipKernel_t,
    dev: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelGetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pi, attrib, kernel, dev) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pi, attrib, kernel, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelGetFunction(
    pFunc: *mut ocgpuHipFunction_t,
    kernel: ocgpuHipKernel_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelGetFunction else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pFunc, kernel) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pFunc, kernel);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelGetParamInfo(
    kernel: ocgpuHipKernel_t,
    paramIndex: usize,
    paramOffset: *mut usize,
    paramSize: *mut usize,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelGetParamInfo else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(kernel, paramIndex, paramOffset, paramSize) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (kernel, paramIndex, paramOffset, paramSize);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipKernelSetAttribute(
    attrib: ocgpuHipFunction_attribute,
    value: i32,
    kernel: ocgpuHipKernel_t,
    dev: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipKernelSetAttribute else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(attrib, value, kernel, dev) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (attrib, value, kernel, dev);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryGetGlobal(
    dptr: *mut *mut c_void,
    bytes: *mut usize,
    library: ocgpuHipLibrary_t,
    name: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryGetGlobal else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, library, name) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dptr, bytes, library, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipLibraryGetManaged(
    dptr: *mut *mut c_void,
    bytes: *mut usize,
    library: ocgpuHipLibrary_t,
    name: *const c_char,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipLibraryGetManaged else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dptr, bytes, library, name) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dptr, bytes, library, name);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemDiscardAndPrefetchBatchAsync(
    dptrs: *mut *mut c_void,
    sizes: *mut usize,
    count: usize,
    prefetchLocs: *mut ocgpuHipMemLocation,
    prefetchLocIdxs: *mut usize,
    numPrefetchLocs: usize,
    flags: u64,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMemDiscardAndPrefetchBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dptrs,
                sizes,
                count,
                prefetchLocs,
                prefetchLocIdxs,
                numPrefetchLocs,
                flags,
                stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dptrs,
            sizes,
            count,
            prefetchLocs,
            prefetchLocIdxs,
            numPrefetchLocs,
            flags,
            stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemDiscardBatchAsync(
    dev_ptrs: *mut *mut c_void,
    sizes: *mut usize,
    count: usize,
    flags: u64,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemDiscardBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(dev_ptrs, sizes, count, flags, stream) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (dev_ptrs, sizes, count, flags, stream);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemGetMemPool(
    pool: *mut ocgpuHipMemPool_t,
    location: *mut ocgpuHipMemLocation,
    type_: ocgpuHipMemAllocationType,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemGetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(pool, location, type_) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (pool, location, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemPrefetchBatchAsync(
    dev_ptrs: *mut *mut c_void,
    sizes: *mut usize,
    count: usize,
    prefetch_locs: *mut ocgpuHipMemLocation,
    prefetch_loc_idxs: *mut usize,
    num_prefetch_locs: usize,
    flags: u64,
    stream: ocgpuHipStream_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemPrefetchBatchAsync
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe {
            __ocgpu_dispatch(
                dev_ptrs,
                sizes,
                count,
                prefetch_locs,
                prefetch_loc_idxs,
                num_prefetch_locs,
                flags,
                stream,
            )
        }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (
            dev_ptrs,
            sizes,
            count,
            prefetch_locs,
            prefetch_loc_idxs,
            num_prefetch_locs,
            flags,
            stream,
        );
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMemSetMemPool(
    location: *mut ocgpuHipMemLocation,
    type_: ocgpuHipMemAllocationType,
    pool: ocgpuHipMemPool_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipMemSetMemPool else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(location, type_, pool) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (location, type_, pool);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipMipmappedArrayGetMemoryRequirements(
    memoryRequirements: *mut ocgpuHipArrayMemoryRequirements,
    mipmap: ocgpuHipMipmappedArray_t,
    device: ocgpuHipDevice_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipMipmappedArrayGetMemoryRequirements
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(memoryRequirements, mipmap, device) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (memoryRequirements, mipmap, device);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipOccupancyMaxActiveClusters(
    numClusters: *mut i32,
    f: *const c_void,
    config: *const ocgpuHipLaunchConfig_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipOccupancyMaxActiveClusters
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(numClusters, f, config) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (numClusters, f, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipOccupancyMaxPotentialClusterSize(
    clusterSize: *mut i32,
    f: *const c_void,
    config: *const ocgpuHipLaunchConfig_t,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api
            .raw_table_ref()
            .ocgpuHipOccupancyMaxPotentialClusterSize
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(clusterSize, f, config) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (clusterSize, f, config);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipStreamGetDevResource(
    hStream: ocgpuHipStream_t,
    resource: *mut ocgpuHipDevResource,
    type_: ocgpuHipDevResourceType,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipStreamGetDevResource
        else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(hStream, resource, type_) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (hStream, resource, type_);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipChooseDevice(
    device: *mut i32,
    prop: *const ocgpuHipDeviceProp_tR0600,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipChooseDevice else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(device, prop) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (device, prop);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}

/// Backend-native flat leaf export with the exact generated raw-table signature.
/// Missing symbols use the manifest's deterministic sentinel policy.
///
/// # Safety
/// All pointer arguments must satisfy the corresponding vendor ABI contract.
#[cfg(feature = "flat-c-exports")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ocgpuHipGetDeviceProperties(
    prop: *mut ocgpuHipDeviceProp_tR0600,
    deviceId: i32,
) -> ocgpuHipError_t {
    #[cfg(feature = "hip")]
    {
        let Ok(__ocgpu_api) = ocgpu_core::raw::hip::load_unvalidated() else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        let Some(__ocgpu_dispatch) = __ocgpu_api.raw_table_ref().ocgpuHipGetDeviceProperties else {
            return OCGPU_ERROR_SYMBOL_UNAVAILABLE;
        };
        // SAFETY: The caller upholds this exact vendor leaf's ABI contract.
        unsafe { __ocgpu_dispatch(prop, deviceId) }
    }
    #[cfg(not(feature = "hip"))]
    {
        let _ = (prop, deviceId);
        OCGPU_ERROR_SYMBOL_UNAVAILABLE
    }
}
